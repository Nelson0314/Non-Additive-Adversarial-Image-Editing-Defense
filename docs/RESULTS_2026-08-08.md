# 2026-08-08 實測紀錄 —— 共同約束的裁決，與階段一重做

> 承接 `RESULTS_2026-08-07.md`。本日只做兩件事：裁決「用什麼當加性與非加性的
> 共同失真約束」，以及據此起跑階段一的重做（v14r）。
>
> **本日最重要的一項是 §1.3：它更正了 `RESULTS_2026-08-07` §6 的方向。**

---

## 1. ST-LPIPS 不能當共同約束（判準事前宣告）

### 1.1 為什麼要找替代品

第一階段以 `LPIPS(x_def, x) = τ` 為共同貨幣。但 τ=0.20 上實測到：

| | PSNR | SSIM | DISTS | L∞ |
|---|---|---|---|---|
| 非加性（位移場） | 21.7–23.8 dB | 0.762–0.857 | 0.068–0.074 | 0.68–0.80 |
| 加性（三篇） | 38.1–43.8 dB | 0.984–0.994 | 0.017–0.028 | 0.017–0.197 |

四個指標全部零重疊。「同一個 LPIPS」顯然不對應「同樣的失真」，故去找是否有
指標能公正地通約兩者。

### 1.2 測了什麼、判準是什麼

Ghildyal & Liu，**ECCV 2022**，*Shift-tolerant Perceptual Similarity Metric*
（arXiv:2207.13686，程式碼 `github.com/abhijay9/ShiftTolerant-LPIPS`）。該文
指出 LPIPS 對人眼看不見的微小錯位過度敏感，並以 anti-aliasing 濾波與 pooling
的改造做出對位移寬容、同時仍與人類判斷一致的版本。

**判準（跑之前宣告，見 `scripts/diag_stlpips.py` 的 docstring）**：

- 可採用：兩類的區間**重疊**，或跨類別均值比 < 1.5×
- 不可採用：仍分開 ≥ 2×

在 LPIPS 已被段 2 固定為 0.20 的那一批 `x_def` 上重算（三張圖 × 七個條件）。

### 1.3 結果與裁決

| 指標 | 非加性區間 | 加性區間 | 均值比 | 判定 |
|---|---|---|---|---|
| **ST-LPIPS** | 0.0265–0.0965 | 0.0024–0.0201 | **3.67×** | **不可採用**（分離） |
| LPIPS（段 2 實際使用） | 0.1954–0.2040 | 0.1914–0.2132 | 1.00× | 重疊（本來就被固定） |

原始資料：`runs/v14_merged/stlpips_tau0.2.csv`。

原因可以指認：ST-LPIPS 的寬容度是為**次像素到數像素的全域位移**校準的，
而位移場在 τ=0.20 的 `disp_max` 已是 11.31 px 的**空間變化形變**——那不是
它設計要寬容的對象。

#### 這推翻了 `RESULTS_2026-08-07` §6 的方向

原文寫「以 LPIPS 匹配失真，對非加性方法**不利**」。加上 ST-LPIPS 之後，
**五個獨立指標**（PSNR、SSIM、DISTS、L∞、ST-LPIPS）在同一個 LPIPS 上一致地
把非加性判重 2–46 倍；而使用者的人眼判讀是位移場在 τ=0.20「可見但可接受」、
加性在 43.8 dB「看不見」。

最簡約的解釋不是四個指標都錯，而是：

> **在同一個 LPIPS 上，兩者的人眼可辨失真本來就不相等，非加性明顯較高。**

也就是說 **LPIPS 對齊其實對非加性寬鬆**：我們拿到了更多的可辨預算，
而且仍然輸了。這比原文的說法對結果更不利，但那是資料指向的方向。
**論文若要引用「對齊軸不公平」這個論點，必須改寫成這個方向。**

### 1.4 由此得到的處置

1. **不再找指標。** 沒有純量能讓兩者可通約，因為它們在人眼上本來就不等價
   ——那不是度量的瑕疵，是實情。
2. **LPIPS 維持為運作約束。** 它是文獻標準，且對我們寬鬆；換成一個對自己
   有利的軸反而需要辯護。
3. **報整個向量。** 第三層主張本來就要求「報全部指標不挑選」。
4. **人眼 2AFC 是唯一名副其實的軸**，待使用者有時間時做：對每個條件找出
   「剛好變得礙眼」的振幅，用它當共同約束。

### 1.5 附帶記錄：安裝

`pip install stlpips-pytorch` 另需 `ipython` 與 `torchsummary`（套件未宣告）。
其 `weights/vST0.0/` **只含 shift_tolerant 權重**，沒有 vanilla，故對照改用
專案段 2 實際使用的 `piq.LPIPS`——那也才是「換掉共同貨幣會怎樣」的正確對照。

---

## 2. 階段一重做（v14r）

### 2.1 變因

| 項目 | 第一階段（v14） | 重做（v14r） | 理由 |
|---|---|---|---|
| **strength** | 0.6 | **0.4** | 攻擊區間在 0.5 飽和、防禦效果在 0.4 後崩掉，0.4 是唯一兩者兼具的點（`RESULTS_2026-08-07` §6c） |
| **tau_acut** | 0.04 | **0.16** | 由 `τ_train=0.20` 依 `0.8 × τ` 導出 |
| **tau_chroma** | 0.8 | **3.2** | 由 `16 × τ` 導出 |
| 其餘 | — | 完全相同 | 模型、解析度、精度、格點、種子 |

只跑 v14（SD v1.4 / 512²）。**不跑 b3**：SDXL 的攻擊在 strength 0.6 的區間
只有 0.038，降到 0.4 會趨近於零，保證沒有訊號。

### 2.2 為什麼重跑段 0 而不複製現成校準表

`Calibration.REQUIRED_CONTEXT` 是 `(model, resolution, guidance, steps, gpu,
precision)`，**不含 `strength`**，技術上可以直接複製 v14 的表省下 4.8 小時。

**但沒有這樣做。** 原本那張表是在 `tau_acut = 0.04` **正在綁定**的情況下
探出來的（第一階段六個訓練格的 `fid_acut` 全部頂在 0.0401–0.0404），而本批
已把該門檻放寬到 0.16。沿用一張在「已不存在的約束」下探出的表，正是本專案
一再吃虧的靜默沿用型態。

> **順帶記錄一個潛在缺陷**：`strength` 不在 `REQUIRED_CONTEXT` 裡，而學習率
> 是**透過該 strength 的編輯鏈**探測出來的。換 strength 而沿用校準表不會
> 觸發 `CalibrationMismatch`。要嘛把 `strength` 加進 `REQUIRED_CONTEXT`，
> 要嘛在每次換 strength 時明確重跑段 0。

### 2.3 執行

```bash
BATCH=v14r bash scripts/shard.sh calib  bird_03 cat_02 dog_03
BATCH=v14r bash scripts/shard.sh fanout bird_03 cat_02 dog_03
BATCH=v14r bash scripts/shard.sh merge  bird_03 cat_02 dog_03
```

`shard.sh` 的 `v14r*` profile 已就緒（commit `17099d5c7`）：與 `v14` 同一組
模型設定，只多 `--strength 0.4`。門檻不必指定，`run_stage` 會由 `--tau-train`
依比例導出並印在 log 上（實測輸出 `[thresholds] tau_acut = 0.16`、
`tau_chroma = 3.2`）。

`v14r*` 必須排在 `v14*` **之前**——bash 的 `case` 取第一個相符者。

### 2.4 狀態（2026-08-08 04:03）

段 0 於 GPU 0 執行中（tmux `wacv-calib`，PID 3031471），格點 3294 格。
預估段 0 約 4.8 h、段 1–3 三卡平行約 7.5 h。

### 2.5 跑完之後要看什麼

1. **`lr.N2` 是否仍落在 0.02–0.1。** 門檻放寬會改變損失地景，探測結果可能
   與第一階段不同——**那正是重跑段 0 的目的**，不要視為異常。
2. **段 1 的 `train.csv`，`fid_acut` 的最大值。** 第一階段是精確頂在 0.0401
   –0.0404。本批若改為頂在 **0.16**，表示約束仍然綁定、只是位置移了；若
   末端 `fid_lpips` 接近 0.20 而 `fid_acut` 明顯低於 0.16，才是真的解開了。
3. **τ=0.20 的 `x_def` 用人眼看過再跑段 3。** 放寬門檻買到的是訓練自由度，
   代價可能是同一個 LPIPS 下的可見失真變差。使用者先前判讀「τ=0.20 可接受」
   是在**受約束的 φ** 上做的，放寬後未必仍成立。這一步很便宜（段 1 約 1 h），
   而段 3 要 7 h，值得先看。

---

## 3. 共用工作區的風險（操作事項）

2026-08-08 起有**兩個 session 在同一個工作目錄**同時工作（本 session 跑
階段一重做，另一 session 跑階段二 inpainting）。實際遇到的情形：

- 對方的 commit（`672572a48`、`2f7d7f6ac`）出現在本 session 的 `git log` 中；
- 對方**未提交的編輯中狀態**使完整 `pytest` 出現 4 個失敗（侷限於
  `tests/test_inpaint.py`），那不是回歸；
- `shard.sh` 被雙方各自修改（對方改了 calib 的 session 命名，本 session 加了
  `v14r*` profile）。

**處置**：commit 一律用**明確路徑**，不用 `git add -A`——否則會把對方的
半成品掃進自己的 commit。測試門檻改用相關子集
（`tests/test_grid.py tests/test_objective.py tests/test_executors.py`）。

**建議**：兩個 session 各用一個 git worktree，或各用一條分支。共用工作目錄
沒有任何機制能防止互相污染。

### 3.1 只開新分支不足以隔離（2026-08-08 09:50 實測）

`HANDOVER` §8.1 的處置是「接手 v14r 的 session 先 `git checkout -b`」。該步驟
已執行（`claude/stage1-redo-v14r`，由 `c26b6efa` 分出），**但沒有達到隔離的
效果**，原因是分支是**工作目錄的屬性**而不是 session 的屬性：

- `git checkout -b` 改變的是 `C:\WACV` 這個工作目錄的 HEAD，兩個 session
  共用它，於是另一 session 之後的 commit（`55fc26ac`、`4f1ddde4`、
  `a77e57d8`）**全部落在新分支上**；
- 該 session 再把同樣的內容推到 `origin/claude/e20-fidelity-constraint`，
  結果是兩個遠端分支指向同一串 commit，只有名字不同。

也就是說 §8.1 的「之後只 push 自己這一條」在共用工作目錄下無法成立。**唯一
有效的形式是 §8.1 括號裡那個更徹底的做法：`git worktree add` 另開工作目錄**，
因為那才會給出各自獨立的 HEAD。本輪不改採，理由是切換共用目錄的分支會直接
打斷另一 session 正在進行的工作，而兩邊的改動在內容上不衝突（對方動
`src/` 的 inpainting 路徑與 `docs/HANDOVER`，本 session 動 `scripts/shard.sh`
與 `docs/RESULTS`）。

---

## 4. v14r 段 0 的結果（2026-08-08 08:51 完成）

03:58 起跑、08:51 收工，實耗 **4 小時 53 分**，log 尾端 `[exit 0]`。與
`HANDOVER` §8.2 的 4.8 h 預估相符。

### 4.1 前置量測

| 產物 | 值 | 判讀 |
|---|---|---|
| `[thresholds]`（log 第 1–2 行） | `tau_acut = 0.16`、`tau_chroma = 3.2` | 比例規則（0.8 τ、16 τ）由 `--tau-train 0.2` 正確導出，未在命令列明給 |
| `editable_filter.csv` | 三張圖 strength 0.4 全部 `passed=True` | 攻擊在 0.4 仍量得到編輯效果。這是整個重做的前提，若不成立則 0.4 這個工作點本身不可用 |
| `warp_reach.csv` | `lpips_at_bound` = 0.4655 / 0.5846 / 0.5416，`covers_train_tau = true` | `max_disp=8.0` 覆蓋 τ_train=0.20 與報告點 τ=0.35，不必調整 |
| `recon_floor.csv` | bird_03 0.1330、cat_02 **0.2398**、dog_03 0.1403 | 逐影像下限（`RESULTS_2026-08-07` §7 缺陷 4 修好後的行為）。cat_02 高於 τ=0.20，故 `rayscale/N3/cat_02/tau0.2` 本來就不可達——與 v14 相同，非本批新增的問題 |
| `micro_bench.csv` | `vae_roundtrip` 0.409 s、`sdedit_50steps` 8.27 s | — |
| `precision_equiv.csv` | `rel_l2 = 0.0` | 本批全程 fp32，與參考同一路徑 |

`strength.recommended` 仍是 **0.6**。那是 `calibrate_strength` 取「SigLIP 平均
編輯效果最大」的結果，即 `HANDOVER` §2.2 第 1 點指認的那個由建構上就會選到
防禦最無力一端的量。實跑值由 CLI 的 `--strength 0.4` 指定，`calibration.json`
的 `note` 欄也寫明「實跑值由 CLI 指定」。**這個欄位不可當作建議採用。**

### 4.2 學習率探測（`lr_probe.csv`，20 格）

20 格全部 `finite=True`、`diverged=False`（`diverged` 欄的存在是
`RESULTS_2026-08-07` §7 缺陷 1 的修正）。候選格點四個條件相同：
`[0.0001, 0.001, 0.005, 0.02, 0.1]`。

| 條件 | 選定 | 末端總損失（由小 lr 到大 lr） | 依據 |
|---|---|---|---|
| N1 | **0.1** | 0.05555 → 0.05539 → 0.05491 → 0.05401 → 0.05205 | 單調下降，取到格點上界 |
| **N2** | **0.1** | 0.6881 → 0.6826 → 0.6733 → 0.6537 → 0.6190 | 單調下降，取到格點上界 |
| N3_stage1 | 0.0001 | 0.0 → 0.0 → 34.84 → 1302.7 → 2669.7 | 0.005 起發散式增長；前兩格並列 0.0，取較小的 lr |
| N3_stage2 | 0.005 | 0.02079 → 0.01245 → **0.01209** → 0.01978 → 0.5358 | 內部極小值 |

**`HANDOVER` §8.3 第 1 項通過**：`lr.N2 = 0.1`，落在 0.02–0.1 的要求區間內，
與前兩批相同。門檻由 0.04 放寬到 0.16 並沒有把學習率推離原本的區間。

兩點附帶記錄：

1. **N1 與 N2 都取在格點上界。** 這不是本批新出現的型態（前兩批也選中 0.1），
   但它表示格點右端沒有被真正探到——真正的最優可能在 0.1 之外。這一點在
   第一階段沒有處理，本批沿用同一個格點以維持可比性。
2. **20 格裡只有 `N3_stage2 @ lr=0.1` 的 `final_L_fid` 非零**（0.5167），
   其餘 19 格全為 0.0。探測只跑 60 步，擾動幅度尚未碰到任何一道 hinge，
   故保真項不啟動。**這不能用來判斷約束是否仍然綁定**——§8.3 第 2 項要看的
   是段 1 的 `train.csv`，不是段 0。

### 4.3 段 1–2 的起跑（09:51）

段 3（`eval`）**刻意不跟著跑**，理由是 `HANDOVER` §8.3 第 3 項：τ=0.20 的
`x_def` 要先經人眼判讀。為此 `scripts/shard.sh` 的 `fanout` 加了 `STAGES`
覆寫（commit `19a70c5b`，預設值即原本寫死的三段）：

```bash
STAGES="train rayscale" BATCH=v14r bash scripts/shard.sh fanout bird_03 cat_02 dog_03
```

分片落在 GPU 1 / 2 / 3（GPU 0 已於 08:54 被另一 session 重啟的 `ip3` 段 0
取走，GPU 6 是他人的）。三個 tmux session 的實際指令經 `tmux list-panes`
確認為 `for S in train rayscale`，且三份 log 的開頭都印出
`tau_acut = 0.16 / tau_chroma = 3.2` 與 `--strength 0.4`。

---

## 5. 段 1–3 與段 0 的程式碼版本差異（起跑前的查證）

段 0 於 03:58 起跑時工作樹的 HEAD 是 `17099d5c`；段 1 起跑時已是
`4f1ddde4`——另一 session 在這 6 小時內提交了 10 個 commit，其中 5 個動到
`src/`（`baselines/dia.py`、`baselines/mist.py`、`defense/generator.py`、
`defense/optimize.py`、`models/sd.py`，共 +210 / −66 行）。**同一批實驗的
兩個段跑在不同程式碼上是一個無法歸因的變因**，故起跑前逐檔查過。

結論：**全部改動在 img2img 路徑上是恆等的**，因為三個入口都對
`is_inpainting` 提早返回：

| 位置 | img2img 下的行為 |
|---|---|
| `SDWrapper.conditioning_for()` | `if not self.is_inpainting: return contextlib.nullcontext()` |
| `SDWrapper._latent_in()` | `if not self.is_inpainting or zc.shape[1] != self.latent_channels: return zc` |
| `optimize._build_attn_step()` | 新增的 `if sd.is_inpainting:` 區塊不進入；`t_edit` 在 `strength=0.4` 下與改動前逐字同式 |

`_eps` 與 `unet_forward` 的唯一改動是把 `z.to(self.unet.dtype)` 包進
`_latent_in(...)`，而該函式在 4 通道權重下回傳原張量。`mist.py` 與 `dia.py`
的改動只是把原本的函式體包進 `with sd.conditioning_for(...)`，語句逐字不變、
僅縮排位移；該 context 在 SD v1.4 下是 `nullcontext`。`SDWrapper.__init__`
多一個 `self._inpaint_cond = None`，不參與任何 img2img 的計算。

CompVis/stable-diffusion-v1-4 的 UNet `in_channels` 是 4，故 `is_inpainting`
為 False，以上早退全部生效。

---

## 6. v14r 段 1–2 的結果（2026-08-08 13:32 全部完成）

三個分片 09:51 起於 GPU 1 / 2 / 3，13:32 前全部 `[exit 0]`，**零失敗格**。

| 分片 | train | rayscale | 段 1 耗時 |
|---|---|---|---|
| v14r_bird_03 | 7 done / 0 failed | 26 done / 0 failed / 2 skipped | 約 3 h 40 m |
| v14r_cat_02 | 7 done / 0 failed | 25 done / 0 failed / **3** skipped | 約 3 h 10 m |
| v14r_dog_03 | 7 done / 0 failed | 26 done / 0 failed / 2 skipped | 約 3 h 05 m |

**skipped 全部是 N3 在低於該影像重建下限的 τ 上。** bird_03（下限 0.1330）與
dog_03（0.1403）各跳過 τ=0.05 與 0.1；cat_02（下限 **0.2398**）連 τ=0.20 都
不可達，故多跳一格。與 v14 相同，屬既有的已知情形，不是本批的失敗。

### 6.1 段 1 遠超「約 1 h」的估計，成本集中在單一格

逐格耗時（`_cells/train__*.json` 的 `seconds`）：

| 條件 | bird_03 | cat_02 | dog_03 |
|---|---|---|---|
| N1 | 20.1 m | 9.9 m | 9.3 m |
| N2 | 64.6 m | 38.2 m | 36.3 m |
| N3 | 26.6 m | 31.8 m | 29.2 m |
| R | 0.0 m | 0.0 m | 0.0 m |
| **photoguard_c** | **約 107 m** | **107.4 m** | **107.5 m** |
| mist | 1.3 m | 1.3 m | 1.3 m |
| dia_r | 2.4 m | 2.4 m | 2.5 m |

`photoguard_c` 一格就佔整個段 1 的一半以上，且三張圖幾乎相同（107.4／107.5 m）。
`HANDOVER` §8.3 的「段 1 約 1 h」低估了約三倍，下一批排程時應以**三小時**計。

### 6.2 §8.3 第 2 項：約束確實解開，而且是完全反轉

判準是 `HANDOVER` §8.3 第 2 項：「若 `fid_acut` 改頂在 0.16，代表約束仍綁定、
只是位置移了；要看到末端 `fid_lpips` 接近 0.20 而 `fid_acut` 明顯低於 0.16，
才是真的解開。」兩批以同一段程式碼重算（資料：各批
`<條件>/<影像>/train.csv`）：

| 批次 | `tau_acut` | 六個位移場格的 `max fid_acut` | 被 acut 罰過的格數 | 六格的 `max fid_lpips_rel` |
|---|---|---|---|---|
| **v14** | 0.04 | 0.0401–0.0403（**全部精確頂在門檻**） | **6 / 6** | 0.052、0.067、0.179、0.171、0.084、0.200 |
| **v14r** | 0.16 | 0.0398–0.1036（最大值僅門檻的 65%） | **0 / 6** | 0.2002、0.2003、0.2004、0.2009、0.2010、0.2012 |

逐格：

| 圖 · 條件 | v14 步數 | v14 max_acut | v14 max_lpips_rel | v14r 步數 | v14r max_acut | v14r max_lpips_rel |
|---|---|---|---|---|---|---|
| bird_03 · N1 | 32 | 0.0402 | 0.0520 | **198** | 0.1036 | **0.2002** |
| bird_03 · N2 | 40 | 0.0403 | 0.0667 | **184** | 0.0794 | **0.2003** |
| cat_02 · N1 | 76 | 0.0401 | 0.1789 | 95 | 0.0398 | **0.2004** |
| cat_02 · N2 | 71 | 0.0401 | 0.1705 | 105 | 0.0418 | **0.2009** |
| dog_03 · N1 | 35 | 0.0402 | 0.0841 | 89 | 0.0787 | **0.2012** |
| dog_03 · N2 | 181 | 0.0401 | 0.2004 | 101 | 0.0707 | **0.2012** |

**判定：通過。** 第一階段六格**每一格**都被鈍化 hinge 罰過，其中五格連 LPIPS
0.20 都碰不到（最低 0.052）；重做之後**沒有任何一格被罰**，六格全部走到 0.20。
訓練步數同步變長（bird_03 的 N1 由 32 步到 198 步），確認先前正是被該門檻
提早擋下。這不是邊際改善，是型態的反轉。

### 6.3 N3 是例外，且失效的機制與 N1／N2 不同

| 圖 | v14 max_acut | v14 max_lpips_rel | v14r max_acut | v14r max_lpips_rel | v14r 步數 | acut 罰過 |
|---|---|---|---|---|---|---|
| bird_03 | 0.0245 | 0.0040 | 0.1105 | 0.0706 | 27 | 0 |
| cat_02 | 0.0343 | 0.0065 | 0.1651 | 0.0596 | 37 | 2 |
| dog_03 | 0.0404 | 0.0122 | 0.1833 | 0.0825 | 31 | 2 |

`max_lpips_rel` 由 0.004–0.012 提高到 0.060–0.083（7–17 倍），故放寬對 N3
**也有效**。但三格都在 27–37 步就停下，離 0.20 的預算仍差 2.5 倍以上，而
`max_steps` 是 250——**它停下來的原因不是保真門檻**（bird_03 那格 acut 一次都
沒罰），而是防禦目標本身的早停條件（`stop_min_steps=25`、`stop_patience=20`）。

也就是說「訓練從未在預算上最佳化」這個第一階段的缺陷，在 N1／N2 上已修好，
**在 N3 上仍然存在，但歸因不同**：N1／N2 是被保真約束擋下，N3 是防禦損失
本身先停止改善。兩者的處置不會相同，不可合併敘述。

### 6.4 兩個由本批新產生的觀察

**`scale_k` 大幅下降。** 段 2 的射線縮放要把訓練出的 φ 乘上 k 才到 τ=0.20：

| | bird_03 N1 | bird_03 N2 | bird_03 N3 | dog_03 N1 | dog_03 N3 |
|---|---|---|---|---|---|
| v14 | 3.125 | 2.500 | 6.500 | 2.062 | 4.500 |
| v14r | **1.125** | **1.094** | **1.125** | **1.188** | **1.500** |

v14 要放大 2–6.5 倍，v14r 幾乎不必放大。這與 §6.2 互相印證：訓練本身已經
走到預算，段 2 只做微調。**這也表示 v14 主表裡的 φ 有很大一部分不是訓練
出來的，而是事後乘上去的**——該事實應寫進論文對第一階段的說明。

**位移場開始頂到上限。** `disp_max_px` 由 v14 的 7.23–11.31 上升到 8.44–11.31，
而 **11.31 = 8 × √2**，正是 `--warp-max-disp 8.0` 在對角方向的界。v14 只有
dog_03 的 N2 一格到界，v14r 有 bird_03 的 N1／N2 與 dog_03 的 N2 三格到界、
dog_03 的 N1 逼近（11.05）。**若模組的表達上限成為新的綁定約束，那不是我們
打算讓它綁定的那一個**，下一批應先量 `warp_reach` 在更大 `max_disp` 下的
可達範圍再決定。本批不改動（改 `max_disp` 會進 `config_hash`，等於再開一批）。

**健全性檢查。** `R`（隨機對照，不訓練）與 dog_03 的 `photoguard_c` 在兩批之間
`tau_achieved`、`scale_k`、`disp_max_px`、`fid_psnr` 全部逐位相同，確認改變
只發生在門檻真正作用的地方。

### 6.5 §8.3 第 3 項：待使用者人眼判讀，段 3 停在原地

跨批次對照頁：**`runs/v14r_vs_v14_tau0.2.html`**（由本日新增的
`scripts/compare_batches.py` 產生）。三張影像 × 五個條件（N1／N2／N3／R／
photoguard_c），每列並排「原圖 · v14 的 x_def · v14r 的 x_def · 兩者的殘差」，
下方附 `tau_achieved`、`scale_k`、PSNR、SSIM、DISTS、L∞、`disp_max_px`。

**比較的前提已驗證**：所有列的 `tau_achieved` 都在 0.195–0.205，兩批確實在
同一個失真預算上。cat_02 的 N3 兩批皆缺圖（重建下限 0.2398 > 0.20），頁面
標紅而非略過。

數值上，同一 τ 下兩批的 PSNR 幾乎不變（例如 bird_03 N1 為 24.79 → 24.76、
dog_03 N1 為 20.75 → 20.88），故**若失真真的變差，變的不是 PSNR 抓得到的
那一種**；`disp_max_px` 則明顯上升，形變幅度確實變大。這正是需要人眼的地方。

**段 3（eval，約 7 h）刻意未起跑。** 判讀通過後再跑：

```bash
STAGES="eval" BATCH=v14r bash scripts/shard.sh fanout bird_03 cat_02 dog_03
BATCH=v14r   bash scripts/shard.sh merge  bird_03 cat_02 dog_03
```

### 6.6 §8.3 第 3 項：人眼判讀的結果（2026-08-08 14:00，使用者）

判讀在 `runs/v14r_vs_v14_tau0.2.html` 上做，原話三點：

1. 位移場類型的失真較大；
2. **提高門檻前後的差距看不太出來**；
3. **apa 提高門檻後的失真，人眼看依舊非常不明顯。**

條件與注入位置的對應（`src/experiment/executors.py:160–177`）：N1 與 N2 是
`site="warp"`（分別為 `targeted_attn` 與 `targeted_output`），R 是同一個 site
的隨機對照，**N3 是 `site="apa"`**。故第 1 點指 N1／N2／R，第 3 點指 N3。

**判定：通過。** 第 2 點正是這一項要問的問題——放寬 `tau_acut`（0.04 → 0.16）
買到了 §6.2 那一整組訓練自由度，而**沒有付出可見失真的代價**。使用者先前
「τ=0.20 可接受」的判讀在放寬後的 φ 上仍然成立。段 3 據此起跑（14:02，
GPU 1 / 2 / 3，`STAGES="eval"`）。

#### 這次判讀另外產生兩項記錄

**第 1 點是 §1.3 在非加性內部的重現。** §1.3 說的是「同一個 LPIPS 上加性與
非加性的人眼可辨失真不相等」。第 1 點與第 3 點合起來說明**兩個非加性 site
之間也不相等**：τ=0.20 下 warp 看得出來，apa「非常不明顯」。也就是說 LPIPS
的不可通約性不是「加性 vs 非加性」這條線，而是逐 site 的。論文若沿用 §1.3
的論述，範圍要改寫成這個較廣的形式。

**第 3 點與 `objective` 把 hinge 對象定為 `x_base` 的設計互相印證。** N3 在
τ=0.20 的絕對 LPIPS（對原圖）是 0.1968–0.2049，但其**相對於 `x_base` 的**
`fid_lpips_rel` 只有 0.060–0.083（§6.3）——差額是 VAE 來回的重建下限
（bird_03 0.1330、dog_03 0.1403），那是 `G(x; φ=0)` 本來就有的模糊，**不是
防禦加上去的**。使用者的判讀（「非常不明顯」）貼合 0.06–0.08 這個相對量而
不是 0.20 這個絕對量，等於一次獨立的證據：`objective.py` 第 66–68 行「四道
hinge 的對象都是 `x_base` 而非原圖」這個選擇與人眼一致。

（附帶：本 session 讀圖時把 N3 讀成「明顯較糊」，與使用者判讀不同。差別的
來源就是上一段——讀到的糊來自重建下限而非防禦擾動。以人眼判讀為準，此處
記錄該分歧與其成因。）

#### 一條未處理的線索

N3／apa 在 τ=0.20 上「人眼非常不明顯」，同時 §6.3 顯示它**只用掉三分之一
的預算就早停**。兩件事合起來表示 apa 在這個工作點上還有可見失真的餘裕，
而擋住它的是防禦損失自身的早停條件。這是下一輪值得查的方向，**本輪不動**
（改早停條件會進 `optim_params` 因而進 `config_hash`）。

---

## 7. v14r 段 3 與合併的結果：重做的前提不成立

段 3 於 14:02–17:54 執行（3 h 33 m ~ 3 h 52 m，估計為 7 h），三個分片
`[exit 0]`、**零失敗格**。合併 3330 格，`grid.csv` **2940 列**，與 v14 相同。

### 7.1 skipped 的歸屬（先釐清，因為 v14 也有同一組）

每個分片 87 格（cat_02 為 178），與 **v14 逐項相同**：

| 數量 | 原因 |
|---|---|
| 75 | `cnn_denoise_substitute` 淨化器不可用。`src/purify/ops.py:35` 記載它「非 NTIRE 2023 冠軍，冠軍不可得，為我方替代；**缺權重**」，`purify_available` 由設計上即為 False |
| 10 | N3 在低於該影像重建下限的 τ（eval，τ=0.05／0.1 × 5 seed） |
| 2 | 同上（rayscale） |

cat_02 多出的 91 格是 N3 的 τ=0.20 也低於它的下限 0.2398。

**這一項要寫進論文**：五個淨化算子中有一個從頭到尾沒有資料，而報表上
「skipped」與「failed」的差別對讀者不明顯。第一階段宣稱「零失敗格」時
本來就含著這 87／178／87 格。

### 7.2 主張一（抗淨化）：`purify_advantage.py` 第五次拒絕出表

```
讀入 2940 列，來自 1 個批次
τ=0.2 上沒有任何 retention_usable 的列。
```

`retention_usable = (mean ≥ 3σ)`，其中 mean／σ 是同一個（條件, 影像, τ）在
identity 淨化下跨 seed 的 `effect_abs`（`executors.py:2100`）。20 個
（影像 × 條件）組合**無一通過**，與 v14 相同。且 v14r 的 σ 普遍**更大**：

| 條件 · 影像 | v14 mean / σ | v14r mean / σ |
|---|---|---|
| N1 · bird_03 | −0.0037 / 0.0048 | −0.0185 / **0.0188** |
| photoguard_c · bird_03 | −0.0073 / 0.0116 | +0.0177 / **0.0372** |
| photoguard_c · dog_03 | +0.0003 / 0.0117 | −0.0371 / **0.0217** |

### 7.3 主張二（抗編輯）：strength 0.4 下沒有任何方法有效

三張圖的 seed 併池（n=15，N3 為 n=10），identity 淨化。每格為
`mean (t = mean / SEM)`。**`effect_abs` 為正代表防禦有效**
（`executors.py:1379`：`effect = 對照側的語意對齊 − 防禦側的語意對齊`）。

| 條件 | τ=0.05 | τ=0.10 | τ=0.20 | τ=0.35 |
|---|---|---|---|---|
| N1 | +0.0006 (+0.17) | +0.0015 (+0.43) | −0.0018 (−0.31) | −0.0066 (−1.31) |
| N2 | +0.0011 (+0.26) | −0.0041 (−1.14) | −0.0024 (−0.64) | −0.0112 (**−2.39**) |
| N3 | — | — | −0.0060 (−1.16) | −0.0153 (**−3.04**) |
| R | +0.0052 (+1.56) | +0.0070 (+1.31) | +0.0017 (+0.40) | −0.0043 (−0.67) |
| mist | −0.0039 (**−2.41**) | −0.0012 (−0.82) | −0.0042 (−1.01) | −0.0134 (−1.37) |
| photoguard_c | −0.0109 (**−2.21**) | −0.0069 (−1.67) | −0.0083 (−0.97) | −0.0133 (−1.41) |
| dia_r | +0.0003 (+0.06) | +0.0021 (+0.48) | −0.0074 (−1.25) | −0.0154 (−1.96) |

**28 格裡沒有任何一格的正效果達到 t ≥ 2**（一般顯著性），更不用說判準要求的
`mean ≥ 3σ`（n=15 時等價於 t ≥ 11.6）。反而有四格的**負**效果達到
|t| ≥ 2，且都集中在最大預算 τ=0.35——即在那個工作點上，防禦讓編輯**更**照
著 prompt 走。

**這包含三個加性 baseline。** 也就是說 strength 0.4 這個工作點上「加性有效、
非加性無效」的對比並不存在——**兩者都無效**。

### 7.4 重做的前提本身就不成立（複核 §6c.2）

`HANDOVER` §2.2 的根因 1（「strength 選在防禦最無力的一端，0.4 才是對的點」）
只有一份證據：`RESULTS_2026-08-07` §6c.2 的
「Mist +0.0145、PhotoGuard-c +0.0134」。原始資料為
`runs/v14_bird_03/strength_sweep_tau0.2.csv`（bird_03 **單一影像**、
3 個 strength × 7 條件 × 10 seed = 210 列）。由該檔重算：

| 條件 | st=0.4 mean | **σ** | SEM | **t** | `mean ≥ 3σ` |
|---|---|---|---|---|---|
| Mist | +0.0145 | **0.0326** | 0.0103 | **+1.41** | 不通過 |
| PhotoGuard-c | +0.0134 | **0.0257** | 0.0081 | **+1.65** | 不通過 |

**§6c.2 的表只列均值、沒有列標準差，而標準差是均值的兩倍以上。** 兩者都
達不到 t=2，也都不通過本專案事前宣告的判準。該表唯一 |t| > 3 的量是
**R（隨機對照）的負效果**（st=0.3 時 t=−4.32、st=0.4 時 t=−3.01）。

換句話說：**根因 1 是把兩個落在雜訊裡的正數讀成了訊號**，而 §2.1 對主表
指認的正是同一個型態（「標準差普遍大於均值」）。v14r 用三張圖、事前宣告的
判準重測，得到與該變異一致的結果——沒有效果。

**根因 2（`tau_acut` 綁死訓練）與此無關，它是真的，且已由 §6.2 證實修好。**
兩個根因必須分開評價：本輪修好了一個真缺陷，同時證偽了另一個。

### 7.5 baseline 在自己的判準下仍然「有效」

`edit_lpips`（防禦側編輯對對照側編輯的位移量），identity、τ=0.20：

| 條件 | v14（st=0.6） | v14r（st=0.4） |
|---|---|---|
| photoguard_c | 0.4792 | 0.3910 |
| mist | 0.3923 | 0.3084 |
| dia_r | 0.3815 | 0.3217 |
| N1 | 0.3197 | 0.2539 |
| N2 | 0.3363 | 0.2503 |
| N3 | 0.3571 | 0.1578 |
| R | 0.3215 | 0.2427 |

用原作的判準（輸出位移）全部都「有效」；用我們的判準（編輯有沒有失敗）
全部無效。這是 `HANDOVER` §2.2 第 3 點記載的落差，在 strength 0.4 上同樣成立。
strength 0.4 下位移一律低於 0.6，因為攻擊本身擾動就較小。

### 7.6 這一輪確立了什麼、否定了什麼

| | 結論 | 證據 |
|---|---|---|
| **確立** | `tau_acut = 0.04` 確實綁死了訓練，改為 0.16 後完全解開 | §6.2：罰過的格數 6/6 → 0/6，六格全部到達 LPIPS 0.20 |
| **確立** | 放寬門檻沒有付出可見失真的代價 | §6.6：使用者人眼判讀 |
| **確立** | 段 2 的縮放在 v14 承擔了很大一部分的 φ | §6.4：`scale_k` 由 2.06–6.50 降到 1.09–1.50 |
| **否定** | 「strength 0.4 是防禦有效的工作點」 | §7.3、§7.4 |
| **仍無法判定** | 主張一（抗淨化） | §7.2，分母第五次落在雜訊裡 |

**下一輪不應該再調 strength。** 0.3／0.4／0.5／0.6 已全部量過，沒有任何一點
產生可與雜訊區分的正效果。問題不在工作點的位置。
