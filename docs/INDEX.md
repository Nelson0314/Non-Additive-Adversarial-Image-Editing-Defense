# 索引

本目錄的每一筆資料都有一個編碼、可以單獨讀完、不依賴閱讀順序。
編碼只用來互相指認，**不代表先後或依賴**。

| 前綴 | 意思 | 檔案 |
|---|---|---|
| `EXP-` | 一個實驗批次：設定、規模、狀態、產物路徑 | `EXPERIMENTS.md` |
| `FND-` | 一個測得的事實：量、值、來源批次、證據路徑 | `FINDINGS.md` |
| `DEC-` | 一個裁決：決定了什麼、依據、影響範圍 | `DECISIONS.md` |
| `DEF-` | 一個缺陷：症狀、根因、處置、釘住它的測試 | `DEFECTS.md` |
| `MTH-` | 一個條件／方法：參數化、損失、隨機對照 | `METHODS.md` |
| `MET-` | 一個判準或指標：定義、方向、陷阱 | `METRICS.md` |

**先讀 `MAINLINE.md`**：它說明現行主線是什麼、程式在哪、已知什麼。

`FINDINGS.md`／`DECISIONS.md`／`EXPERIMENTS.md` 只放**主線**條目。
與現行主線無關的舊條目在 `archive/LEGACY_*.md`，屬次要紀錄、不是判準來源
（降級不代表作廢）。工程與量測的教訓不降級——硬體在 `RUNBOOK.md`、
程式缺陷在 `DEFECTS.md`、量測陷阱留在主線 `FINDINGS.md`。

常設文件（不編碼，因為它們是操作與契約，不是資料）：

| 檔案 | 用途 |
|---|---|
| `MAINLINE.md` | **現行主線**：弱 baseline 的定義、最小程式集合、已知什麼、下一步空間 |
| `SYNTHESIS.md` | **三組配置橫過來看一次**：哪些變因固定、哪些改動、各自確立或否定了什麼 |
| `RUNBOOK.md` | 上機執行：環境、五段流程、監察、失敗處理 |
| `ARCHITECTURE.md` | 程式結構與模組介面契約 |
| `DESIGN.md` | 實驗設計：軸、條件、主張階層 |
| `reference/` | 外部論文的原始碼逐字查證，逐篇自足 |
| `archive/` | 逐次執行的原始紀錄。**不是判準來源**，見下 |
| `HANDOFF_*.md` | 交給新 session 的自足交接單，逐次一份 |

## `archive/` 的地位

裡面是 2026-08-05 到 08-08 的逐日紀錄（`RESULTS_*`、`HANDOVER_*`、
`LOGIC_CHECK`、`PRIOR_FINDINGS`）。它們按時間寫、彼此交互引用、後面的節次
會推翻前面的——正是本次整理要消除的形態。

**判準與結論一律以 `FINDINGS.md`／`DECISIONS.md` 為準。** archive 保留原文
只為兩件事：追溯某個數字當初怎麼量的，以及查那些尚未被歸納的細節。
若某個結論在 archive 裡而不在 `FINDINGS.md`，表示它**還沒有被複核歸納**，
引用前要自行查證。

## `runs/` 的地位

`runs/` 是唯一的證據來源，容器會被刪除、實驗無法重跑。每個 `EXP-` 條目
都寫明它的產物路徑。改動 `.gitignore` 的 `runs/` 區塊時必須用
`git status --porcelain --ignored` 確認沒有結果檔被排除。

## 主線編碼一覽

| 編碼 | 一句話 |
|---|---|
| `DEC-023` | 新主線與內部弱 baseline 的定義（原生 APA + targeted reward） |
| `DEC-021` | 換 reward 後把主項正規化回 cross-entropy 的量級 |
| `DEC-022` | 編輯強度 0.4→0.55：未防禦的編輯必須真的成功 |
| `EXP-apa1` | APA 原生階段一 LoRA 重現，對照我方 z*+decoder |
| `EXP-apa2` | APA 原生階段二完整重現，四軸消融 |
| `FND-001` | 生成路徑有逐影像的重建下限 |
| `FND-002` | BDIA 讓生成路徑幾乎不再額外收費 |
| `FND-015` | 分片目錄與 `_merged` 逐位元重複（工程教訓） |
| `FND-016` | 階段一的 LoRA 在 UNet，而重建誤差全部來自 VAE |
| `FND-022` | 原文的階段一在本管線上是空操作 |
| `FND-023` | 換掉階段一買到的是保真度與時間，不是防禦 |
| `FND-024` | 注意力抑制與「編輯失敗」之間沒有因果關係 |
| `FND-026` | DISTS 在 512² 先降採樣到 256²，使失真比較翻轉（量測陷阱） |
| `FND-027` | 原生階段一在 BDIA 上有害；DDIM 對照證明重現無誤 |
| `FND-028` | latent 球從未生效；同半徑 ≠ 同可見失真，指標與人眼相反 |
| `FND-029` | reward 決定位移、更新規則決定保真、沒有一個軸讓語意抵抗成立 |
| `FND-030` | 原文的分類器 reward 與抗文字編輯無交集 |
