# 測得的事實

每一筆自足：量是什麼、值多少、哪一批量的、證據在哪、成不成立。
沒有一筆需要先讀另一筆。

---

## FND-001 · 生成路徑有逐影像的重建下限

- **量**：`LPIPS(decode(encode(x)), x)`，走生成路徑的條件無論 φ 取什麼都要付
- **值**（SD v1.4／512²／fp32）：bird_03 0.1330、dog_03 0.1403、**cat_02 0.2398**
- **來源**：EXP-b1 起，各批段 0 的 `calib/recon_floor.csv`
- **後果**：cat_02 的 0.2398 高於 τ=0.20，故 site apa 在該影像的 τ≤0.20 上
  **結構上不可達**，標為 skipped 而非 failed
- **狀態**：成立，逐批複驗

## FND-002 · BDIA + `t_max 200` 讓生成路徑幾乎不再額外收費

- **量**：`G(x; φ=0)` 對原圖的 LPIPS
- **值**（SDXL／1024²／bf16／k_inv=10）：DDIM 走滿 0.7325／0.6736／0.6917；
  BDIA + t_max 200 **0.1092／0.2208／0.0976**；純 VAE 來回下限 0.0986／0.2130／0.0889
- **來源**：EXP-b2
- **後果**：`--exact-inversion --t-max 200` 成為 SDXL 批次的必要旗標。
  fp32 下 BDIA 與純 VAE 來回逐位相同，`--t-max` 屆時是無關參數
- **狀態**：成立

## FND-015 · 分片目錄與 `_merged` 逐位元重複

- **量**：blob SHA 逐檔比對
- **值**：v14 18420/18434 相同、v14r 18421/18424、b3 40777/40788；
  不同的僅每片的 `run.log`、`grid.csv`、`progress.json` 等少數小檔
- **後果**：DEC-009（刪除分片目錄，保留差異檔）——已執行
- **附註**：git 是內容定址的，逐位元相同的 blob 本來就只存一份，
  故刪除**不會縮小 `.git`**，只減少檔案數與簽出體積
- **狀態**：成立

## FND-016 · 階段一的 LoRA 裝在 UNet 上，而重建誤差全部來自 VAE

- **量**：`align.csv` 的逐步 `fid_lpips`、`align_loss`、`align_tau_lpips`
- **值**（EXP-ip20，apa／horse_00）：第 0 步 LPIPS 0.15806、`align_loss`
  1.02e-04；第 199 步 LPIPS 0.15768、`align_loss` 0.0。200 步共移動 **0.0004**
- **機制**：`optimize.align` 把四道 hinge 的門檻設成該影像自己的重建下限
  （`recon_floor_thresholds`，`align_tau_lpips = 0.15806` 恰為第 0 步的值），
  故 φ=0 一開始就滿足約束，梯度為零
- **更根本的原因**：下限來自 `decode(encode(x))` 這條 VAE 來回，而 LoRA 在
  UNet 裡，結構上碰不到它。APA 用 DDIM 反演，其階段一補的是**反演誤差**；
  本專案用 BDIA 精確反演，那一半的誤差本來就不存在（fp32 下與純 VAE 來回
  逐位相同）
- **代價**：每個 apa 格花 `align_seconds` ≈ 940 s 跑一個註定不動的階段
- **後果**：DEC-016（階段一改到 latent 與解碼器上）
- **狀態**：成立

## FND-022 · 原文的階段一在本管線上是空操作，四個原因各自獨立成立

- **來源**：EXP-s3t20，`runs/s3t20_merged/apa/*/align.csv`（3 圖 × 200 步）
- **原文的階段一**：訓練 UNet 的 LoRA 以壓低 `G(x;φ)` 對原圖的重建誤差
- **四個原因**：
  1. **它要修的誤差不存在。** APA 用 DDIM 反演，階段一補的是反演誤差；
     本專案用 BDIA，實測 `path_minus_vae_lpips = 0.00e+00`，生成路徑與純
     VAE 來回**逐位元相同**
  2. **剩下的誤差在 VAE，而 LoRA 在 UNet。** UNet 只作用於中間的去噪段，
     碰不到編碼與解碼那兩端
  3. **門檻設在起點上。** 四道 hinge 的門檻取自該影像自己的 VAE 來回下限，
     因 (1) 而恰等於 `G(x;φ=0)`，故 `align_loss` 從第 0 步起即為零
     （horse_00 與 woman_03 為 0.000000、horse_03 為 0.000095）
  4. **損失為零但參數仍在動。** `clamp(x, min=0)` 在 `x=0` 處梯度為 1.0
     （實測），四道 hinge 全坐在邊界上，φ 每步漂移。horse_03 前六步：
     0.000095 → 0.018099（PSNR 項）→ 0.453907（銳利度項）→ 0.078092 →
     0.000000 → 0.018711，在可行域邊界上來回彈跳
- **淨結果**：LPIPS 移動 0.0015 / 0.0014 / **0.0000**；`align_best_step`
  為 0 / 4 / 0，還原最佳步等於還原初始值，200 步被完整丟棄
- **成本**：`align_seconds` 909.69 秒，而同一格的防禦訓練只花 567.11 秒
- **通則**：hinge 是一個力不是一個保證。損失為零只代表「目前沒有違規」，
  邊界上的梯度仍是 1，φ 會繼續漂。要保證可行需用投影或可行性過濾
- **狀態**：成立

## FND-023 · 換掉階段一買到的是保真度與時間，不是防禦

- **來源**：EXP-s3t20（沿用原文階段一）對 EXP-s3t20_r（換成 latent 對齊 +
  解碼器逐圖微調），配對 n=345（同影像、同淨化、同種子）
- **新作法**：解 `z*` 使 `decode(z*) ≈ x`（16,384 個變數）＋ 微調解碼器的
  GroupNorm affine 與 conv bias（35,715 個，佔解碼器 0.07%），並以硬停止
  條件避免解碼器背熟原圖
- **值**：

  | 指標 | 原文階段一 | 新作法 | 改後較優 |
  |---|---|---|---|
  | `fid_lpips` ↓ | 0.1536 | **0.1301** | **100%** |
  | `fid_dists` ↓ | 0.0797 | 0.0758 | 67% |
  | `fid_psnr` ↑ | 25.91 | 26.35 | 67% |
  | `edit_lpips` ↑ | 0.1416 | 0.1449 | **55%** |

- **判讀**：345 格全部保真變好；防禦的勝率 55%，與擲硬幣不可區分
- **更正一個先前的說法**：「壓低下限＝多出預算」**不成立**。評測預算軸是
  相對的（`target = floor + Δ`），下限已被減掉。真正的收益是兩件別的事：
  (a) 人眼判的是總量「下限 + 增量」，下限 0.128 → 0.080 使同一個 Δ 下的
  總失真少 15%；(b) τ=0.05／0.10 由結構上不可達變為可達
- **定位**：這是讓比較能夠成立的前置處理，不是貢獻點——沒有它，生成路徑
  背著與防禦無關的 0.13 起跳成本，與加性方法（起點 0）不對等
- **狀態**：成立

## FND-024 · 注意力抑制與「編輯失敗」之間沒有因果關係

- **四條獨立證據**：
  1. **在允許四倍失真的設定上訓練**（EXP-s3a，τ_LPIPS=0.50，`fid_lpips`
     實測 0.498–0.533）：抑制達 **89.7% / 93.8% / 89.3%**，三格皆正常收斂
     （54 / 72 / 90 步）。同一組 φ 的編輯成功率是 **100%**，而未防禦 67%、
     隨機對照 80%——**壓掉 94% 的注意力，編輯反而比不防禦更容易成功**
  2. **加性方法未針對 c_a 最佳化卻取得同量級的抑制**：PhotoGuard 在
     horse_03 上 −11.1%，而其 `edit_lpips` 是我方的 3.3 倍
  3. **timestep 掃描**：抑制在有施力與沒施力的 t 上差距僅 1–3 個百分點，
     取樣點數不是瓶頸（故 DEC-016 C2 的「2 → 4」買不到東西）
  4. **改用比例式損失**（`‖Att ⊙ M‖₁ / ‖Att‖₁`，EXP-s3t20_rd）：質量確實
     被移出遮罩（horse_00 −56%）而非整體攤平，編輯照樣成功
- **機制**：EXP-s3a §4.3 記載，該預算下 `x_def` 已被破壞到 img2img 直接
  把它當噪聲、照 prompt 重畫
- **與 DAYN 的關係**：DAYN Table 1 報的是**位移**，用那一層看本專案每個
  條件也都「有效」。「編輯被導離 prompt」是本專案多問的一層
- **狀態**：成立

## FND-026 · DISTS 在 512² 上先降採樣到 256²，使加性與非加性的失真比較翻轉

- **來源**：`piq 0.8.0` 的 `piq.DISTS.forward`，`min(H,W) > 256` 時雙線性
  降採樣至短邊 256。本專案為 512²，故**預算軸實際量在 256² 上**
- **值**（3 影像平均，防禦圖對原圖）：

  | 條件 | DISTS@256（現用） | DISTS@512 | 倍率 |
  |---|---|---|---|
  | 非加性（三條件） | 0.0751–0.0755 | 0.0544–0.0618 | 0.73–0.82 |
  | PhotoGuard-c | 0.0371 | **0.1579** | **4.27** |
  | DIA-R | 0.0410 | 0.1018 | 2.50 |
  | Mist | 0.0419 | 0.0696 | 1.65 |

- **判讀**：在 256 上量，非加性的失真是加性的 1.8–2 倍；在 512 上量則反轉為
  加性高出 1.1–2.6 倍。**順序完全翻轉**
- **機制**：降採樣 2 倍是低通濾波，而加性對抗擾動住在高頻（Mist 的 L∞ 僅
  0.0137），被抹掉大半；非加性擾動經 VAE 解碼後偏低頻，影響小
- **影響範圍**：只影響**預算軸**。`edit_lpips`（主指標）不受影響——已查證
  `piq.LPIPS` 無降採樣，位移量量在完整 512² 上
- **不是實作缺陷**：官方實作亦如此，且 DISTS 的 α/β 是在含降採樣的條件下
  於 KADID-10k 與 DTD 上學出來的，直接在 512 上算等於離開其校準分布
- **待裁決**：是否維持現狀（並列此表為限制）、改用 DISTS@512 重跑射線縮放、
  或兩者並報。**尚未裁決**
- **狀態**：成立（量測），處置未定

## FND-027 · APA 原生階段一在 BDIA 管線上不只無效，而是有害；DDIM 對照證明重現無誤

- **來源**：`runs/apa_native_probe/`（BDIA）、`runs/apa_native_ddim_control/`（DDIM 對照）
- **作法**：用 APA 官方階段一目標（Eq.6 的 denoising MSE、AdamW lr=1e-4、
  200 步、rank=8、noise_offset=0.1，`optimize.align_apa_native`）訓練 LoRA，
  與 φ=0 下限、以及本專案現行做法（z*+decoder）並列
- **值（BDIA，3 影像）**：LoRA 使每一項保真指標都**劣於什麼都不做**——
  LPIPS 0.165–0.178 → 0.197–0.215；現行做法為 0.082–0.089，三張圖七項指標全勝
- **DDIM 對照（同一組 φ，換反演方式）**：coot 的 DDIM 反演在 k=50／全範圍下
  **整張圖語意崩壞**，LoRA 把它救回（LPIPS 0.429 → 0.239）；panda／butterfly
  的 DDIM 起點沒有崩壞，LoRA 對它們同樣是負面（0.230→0.274、0.253→0.273）
- **機制**：VCA 修的是 DDIM 反演本身的誤差。BDIA 是代數精確反演，把該誤差
  消到 0（FND-016），於是沒有東西可修，只剩單樣本、高變異數訓練帶來的擾動
- **判讀**：這是「原文階段一在本管線上無用」的**直接證據**，且排除了實作錯誤
  ——同一份程式碼在誤差真的存在時有效
- **狀態**：成立

## FND-028 · APA 的 latent L∞ 球從未綁住任何東西，且同一半徑在不同影像上是不同的可見失真

- **來源**：`runs/apa_native_full_v3`、`v4`、`apa_exp2` 的逐迭代 log
- **值一（球是空操作）**：`ε_a = 0.4` 而 `µ × N = 0.04 × 10 = 0.4`，兩者**恰好
  相等**；每一格的 `linf` 逐迭代精確等於 `µ × iter`（0.040/0.080/…/0.400）。
  投影 `Π` 全程未曾生效，失真幅度完全由「步長 × 迭代數」決定，與 reward 無關
- **值二（同半徑 ≠ 同失真）**：同一組設定在三張圖上——panda 紋理變粗但主體
  完好（LPIPS 0.338）、butterfly 花紋略糊（0.267）、**coot 的鳥整隻被抹成
  褐色糊狀物**（0.289）
- **值三（指標排序與人眼相反）**：LPIPS 判 panda 比 coot 差；DISTS 三張幾乎
  相同（0.1877／0.1896／0.1896）；SSIM 判 coot（0.773）比 panda（0.750）
  **保存得更好**。三個指標沒有一個把「主體被消滅」排在最前
- **機制**：coot 受損的是畫面中央佔比不大的主體，周圍水面完好，全域平均的
  指標被稀釋；panda 是整張圖均勻的細微改動，全域指標反而更敏感
- **判讀**：latent 半徑不是保真約束——同一半徑對應到的可見失真取決於擾動落在
  哪個區域。但影像空間的**全域平均**指標同樣有盲區（與 FND-019 的集中度、
  DEC-016 B2 的最差區塊 hinge 同一件事）
- **狀態**：成立

## FND-029 · 階段二四個軸的效果分工：reward 決定位移、更新規則決定保真、沒有一個軸讓語意抵抗成立

- **來源**：`runs/apa_native_full_v4`、`apa_pj_eval`、`apa_exp2`、
  `apa_native_anchor`（96 格，butterfly + coot，編輯 strength 0.55）
- **reward（其餘固定）**：兩圖平均 `edit_lpips` targeted **0.435** ＞
  分類器 CE 0.290 ＞ 注意力抑制 0.289。全批位移前四名有三名用 targeted 形式
- **更新規則（其餘固定）**：LPIPS 平均 影像投影 0.171 ≈ DISTS+Adam 0.195
  ＜ latent球+sign 0.237 ＜ DISTS+sign 0.268。**關鍵是換掉 sign，不是換約束形式**
- **把失真項放進 loss 並取消球約束**：配 sign 反而更差（0.237→0.268，且 λ 由
  0.5 加到 128 時 LPIPS 由 0.232 單調升到 0.306）；配 Adam 才變好（→0.195）。
  機制：sign 丟掉梯度大小，幅度在 latent 上被寫死，λ 只改方向，而不同方向
  解碼出的可見失真差很多
- **階段一（其餘固定）**：我方 z*+decoder 在注意力與 targeted reward 下 8/8
  優於原生 LoRA；**但在分類器 reward 的 DDIM 兩格反轉**（0.315→0.319、
  0.280→0.309）。10/12 我方較好，非無條件
- **語意抵抗**：22 個非加性條件的 CLIP 掉幅**沒有一個超過 ±0.03**，換 reward、
  換約束、換更新規則、換階段一都不改變這件事。唯一為正的是 Mist
  （+0.041/+0.070），代價是最差保真度（LPIPS 0.537）
- **判讀**：位移量與語意抵抗是兩件事（與 FND-018／024 一致）。四個軸都試過
  之後，「把編輯推遠」做得到，「讓編輯不服從 prompt」做不到
- **狀態**：成立

## FND-030 · APA 原文的分類器 reward 與抗文字編輯沒有交集

- **來源**：`runs/apa_exp2`、`runs/apa_native_anchor`
- **作法**：階段二 reward 換成 APA 原文自己的替代分類器 cross-entropy
  （ResNet-50、untargeted、ImageNet 標籤取自官方 `data.json`），其餘維持原生
- **值**：失真是全批最高（LPIPS 0.280–0.412），而 CLIP 掉幅仍在 ±0.02、
  `edit_lpips` 0.163–0.389 並未優於其他 reward
- **判讀**：「誤導分類器」與「抵抗文字引導編輯」是兩個不相交的目標。這是負面
  結果，但它排除了「換上原生 reward 會有意外收穫」這個可能性
- **狀態**：成立

## FND-031 · cross-attention 目標注入在**防禦圖上**成立，但編輯照樣成功

- **來源**：`runs/routeB_ablation_0813_0214`（四格消融，SD v1.4／512²／
  `lo_aligned` 三張／RTX 3090／seed 0／四卡並行，牆鐘 8 分鐘）
- **四格**（其餘設定全部相同，只換 reward，符合 DEC-023 的紀律）：

  | 格 | reward | edit_lpips ↑ | fid_lpips ↓ | PSNR ↑ | 位移/失真 | 秒 |
  |---|---|---|---|---|---|---|
  | 1 | 像素 targeted → `gray.png` | **0.3465** | 0.1926 | 18.89 | **1.80** | 141 |
  | 2 | 像素 targeted → `obama.png` | 0.1743 | **0.1532** | 22.65 | 1.14 | 135 |
  | 3 | attention injection，β=0 | 0.2009 | 0.1888 | **26.55** | 1.06 | 147 |
  | 4 | injection ＋ suppression，β=1 | 0.2539 | 0.2768 | 23.61 | 0.92 | 150 |

- **正面結果（新現象，本專案首次觀察到）**：injection **在防禦圖上真的改變了
  主體的語意**，而不是把畫面弄糊。逐張可見：
  - `horse_00` 格 3：馬臉被抹除，換成長髮的深色人形輪廓，**草地與天空幾乎完好**
  - `man_00` 格 3：人臉保留，但多出**藍色圓點領帶與白襯衫**——正是 `obama.png`
    的服裝特徵，即身分層的注入
  - 訓練 log 的 `R` 由 −1.0000 單調升到 **+0.5161**，正規化未爆掉
- **負面結果（判準層）**：**四格都沒有阻止編輯**。`horse_00` 在格 1 與格 3 上
  編輯後都變成斑馬，只是格 1 的輸出灰暗、格 3 明亮而斑馬頭部殘留一團黑色。
  CLIP-T 掉幅四格皆在 ±0.03 內，與 FND-029 一致
- **指標互相矛盾，以人眼為準**：格 1 的 `edit_lpips` 最高（0.3465），但它的
  PSNR 也最低（18.89 dB）——逐張看是**整張圖蒙上灰霧**，位移買在全域劣化上，
  與 FND-017／018 的型態相同。格 3 的 PSNR 高出 **7.66 dB**，因為它的擾動
  **空間集中在主體**而背景不動，全域平均指標被稀釋（同 FND-019／FND-028）
- **格 2 的判讀**：只換目標圖（gray → obama）而不改 reward 形式，位移掉一半
  （0.3465 → 0.1743）而保真變好。**灰圖的高位移主要來自「推向無內容」造成的
  全域降對比**，不是防禦力
- **四格不在同一個失真預算上**，故 `edit_lpips` 的直接比較不成立。要做同失真
  比較需調 µ 或 N——`ε_a` 從未生效（FND-028），失真由「步長 × 迭代數」釘死，
  但**不同 reward 的方向在解碼後給出不同的可見失真**（0.15–0.28）
- **狀態**：成立

---

# 2026-08-13 的探索：全部放棄，歸檔

一天的探索，**四個方向全部否決**。實驗產物與報告已刪除，程式已退回。
取回：`git checkout 96657e17b -- <path>`（該 commit 含全部程式與 runs/）。

判準沒有變：這些方向**都沒有改善抗編輯**，CLIP 掉幅一律在 ±0.03 內，
與 FND-024／029／030 一致。位移量的增減不構成理由。

**一、cross-attention 目標注入（arXiv:2602.14679 Eq.5／6）。** 借用其
injection ＋ suppression 雙損失，換非加性載體。修正條件（注入項須用目標詞
`t_tar` 而非來源類別名）後，正規化 reward 只由 −1.0 走到 −0.94，防禦圖
幾乎無可見變化，位移量低於原本的 targeted。**優化不動。**

**二、分階段注入。** 10 次迭代在第 k 次切換 reward，掃 k∈{0,3,5,7,10} ×
兩種順序。`attn 3 → target 7` 的位移 0.1954 對弱 baseline 的 0.1743
（+12.1%、逐圖 3/3），LPIPS 幾乎相同而 PSNR 更好。k=0 與 k=10 兩個錨點與
既有的單一 reward 格對得上，實作無誤。**但位移贏了不代表抗編輯，放棄。**

**三、amortized 非加性 generator（GAN）與對淨化的 min-max。** 位移場
generator 對同失真隨機位移場的勝率 51.2%（n=168）——**訓練等同擲硬幣**。
min-max 也無效：內層 `worst` 恆為 identity，淨化從不削弱位移。

**四、顏色通道。** 24 張圖對齊同一 DISTS。等亮度色度擾動的位移比 RGB 獨立
低 31%；而只動亮度的位移（0.1664）幾乎等於 RGB 獨立（0.1674），色偏卻只有
0.11 對 2.96——**RGB 獨立那組的效果全部來自它順帶改變的亮度，色偏是白付的。**

**一個仍然有效的量測警訊。** 「加性 vs 非加性誰更抗淨化」這個問題，在 LPIPS
預算下與 DISTS 預算下答案相反（我方勝率 10.4% 對 93.8%），而同 LPIPS 時
PSNR／L∞／SSIM／VIF 全判加性低頻較好、只有 DISTS 與人眼一致。與 FND-026
同型。**日後做任何加性／非加性的比較，預算指標必須先講清楚。**

## FND-032 · site F 勝過同失真隨機 12/12，但位移量對加性仍是 0.90–0.93×

- **來源**：`runs/phaseA_full`（36 格 = 6 影像 × 2 預算 × 3 條件）、
  `runs/phaseB`（6 格）。SD v1.4／512²／fp32／seed 0／SDEdit strength 0.55
- **作法**：損失（encoder-targeted `‖E(x′) − E(gray)‖²`）、更新規則（sign）、
  步數（100）、種子、預算對齊程序（對半徑二分搜尋使最終 DISTS 落在目標上）
  **全部固定，唯一的變因是參數化**
- **值（六張影像平均）**：

  | 預算 | add | phase | phase_rand | phase/add | phase 逐格勝出 |
  |---|---|---|---|---|---|
  | DISTS 0.04 | 0.5148 | 0.4805 | 0.1790 | 0.93× | 3/6 |
  | DISTS 0.075 | 0.5998 | 0.5409 | 0.2084 | 0.90× | 0/6 |

- **成立的**：phase 勝過同失真隨機相位 **12/12**，倍率 2.7× 與 2.6×。這是本專案
  第一次有非加性參數化明確贏過同失真隨機——FND-004 是打平、FND-018 是落後。
  該比較**不受預算軸偏誤影響**：兩者是同一個參數化家族，指標偏差對雙方相同
- **不成立的**：位移量門檻是 1.00×。相對 FND-013 記的 0.31× 是三倍的效率改善，
  但仍未達標，且 DISTS 軸對 site F 有利（FND-034），故 0.90–0.93× 是樂觀上界
- **B 臂（latent 相位）無效**：`apa_phase` 0.3358 對 `apa_weak` 0.3459，三張圖
  持平或略差。機制一致——要利用的落差是「人眼與 DISTS 對紋理重取樣寬容、
  VAE encoder 對相位敏感」，而 latent 擾動已在 encoder 之後。**優勢是像素空間
  特有的**
- **語意抵抗**：CLIP 掉幅全部在 ±0.03 內，與 FND-024／029／030 一致
- **兩格 unreachable**：相位有構造上的天花板（|θ| ≤ π），bird_03 與 man_00 在
  0.075 上達不到，標記而非當成失敗（與 FND-001 同型）
- **狀態**：成立

## FND-033 · 抗淨化：site F 的衰減率低於加性，9/10 與 8/10 個算子

- **來源**：`runs/phaseA_full/retention.csv`（360 列 = 36 格 × 10 算子），
  每格 3 個編輯 seed。`retention = effect(淨化) / effect(identity)`，
  `effect` 為 `LPIPS(未防禦的編輯, 防禦後的編輯)`
- **值（六張影像平均，DISTS 0.04）**：

  | 淨化算子 | add | phase | phase 較高 |
  |---|---|---|---|
  | blur σ=1 | 0.7395 | 0.8515 | ✓ |
  | noise 0.05 | 1.0051 | 1.1461 | ✓ |
  | quantize 16 | 0.9185 | 0.9478 | ✓ |
  | jpeg 75 | 0.6047 | 0.6813 | ✓ |
  | jpeg 30 | 0.6790 | 0.7966 | ✓ |
  | crop_resize 0.1 | 1.0586 | 1.1119 | ✓ |
  | **C&R 串接（jpeg75 → 0.5× Lanczos）** | **0.5855** | **0.6940** | ✓ |
  | adverse_cleaner | 0.7540 | 0.9415 | ✓ |
  | impress | 0.8731 | 0.9320 | ✓ |

  DISTS 0.075 上為 8/9（`quantize16` 反轉：0.9208 對 0.8970）
- **分母可比**：`effect(identity)` add 0.5094／phase 0.4728（0.04）、
  0.5993／0.5277（0.075）。phase 的分母是 add 的 88–93%，故比值可比
- **`phase_rand` 的 retention（1.4–3.4）不可解讀**：它的分母只有 0.17–0.20，
  約為 add 的三分之一，任何淨化引入的雜訊都會把比值灌大。360 列中 5 格被
  3σ 閘標為不可用，**全部是 `phase_rand`**。這與 FND-018 的 `Ra` 是同一個陷阱，
  差別在於這次 phase 的分子是隨機的 2.7 倍，故可以確認那是分母造成的假象
- **判讀**：**主張一（非加性更抗淨化）在 site F 上成立**，且在最接近真實的
  C&R 串接上成立。FND-018 只能把優勢歸給「非加性 site」而不能歸給方法本身；
  這一批因為有可比的分母與明確勝過隨機的分子，可以歸給方法
- **狀態**：成立

## FND-034 · DISTS 與 LPIPS 都低估相位擾動的可見失真，GMSD 與 HaarPSI 抓得到

- **來源**：`runs/phase_sweep`（108 格 = 6 影像 × 2 條件 × 9 個半徑），
  十六項指標。人眼判定用 128² 區塊放大 3 倍的無損 PNG
- **值（六張影像平均，對到相近的 DISTS）**：

  | 條件 | 半徑 | DISTS | LPIPS ↓ | PSNR ↑ | HaarPSI ↑ | GMSD ↓ | MS-SSIM ↑ | NIQE ↓ |
  |---|---|---|---|---|---|---|---|---|
  | add | ε = 5/255 | 0.0470 | 0.4149 | 37.38 | 0.9520 | **0.0121** | 0.9866 | 4.28 |
  | phase | θ_max = 1.80 | 0.0523 | **0.2429** | 32.50 | 0.8245 | **0.0686** | 0.9710 | 4.04 |

- **人眼（放大 3 倍）**：phase 產生**塊狀／絎縫狀的中頻斑駁**——草地被打散成有
  邊界的斑塊、人字紋被抹成不規則色塊；add 是**細密規則的網格狀顆粒**，底下的
  結構保留著。**破壞結構比覆蓋顆粒更難看**
- **判讀**：同一個 DISTS 之下 LPIPS 判 phase 好 1.7 倍、**GMSD 判 phase 差 5.7 倍**。
  GMSD 量梯度幅度的相似度，即結構有沒有被破壞，與人眼一致；HaarPSI（0.952 對
  0.825）同向。DISTS 的設計目標明寫 tolerant to texture resampling，LPIPS 的深度
  特徵有同樣的副作用——**兩者都不該當 site F 的預算軸**
- **與 FND-026 的關係**：同型但不同一件事。FND-026 是 DISTS 的降採樣造成加性與
  非加性翻轉；這一條是 DISTS 與 LPIPS 對**紋理重取樣**本身的寬容
- **相位的失真天花板由 `r_min` 決定**：`r_min=0.25` 時 DISTS 最多 0.055、
  `0.12` 時 0.10、`0.05` 時 0.43——但 `0.05` 的幅度譜偏差由 0.03 衝到 0.41、
  PSNR 掉到 12 dB，**那已不是重排相位而是毀圖**，故定 `r_min = 0.12`
- **紋理閘的有效面積**：六張真實照片 0.279–0.465，風險三（自由度塌陷）不成立
- **狀態**：成立（量測），門檻待使用者以人眼裁定

---

# 自 `archive/LEGACY_findings.md` 升上來的六條（2026-08-13）

舊主線的紀錄已隨方向收斂刪除，但以下六條仍是現行判準的來源——site F 的
設計與讀法直接建立在它們之上，因此逐字保留於此。編碼不重用。

## FND-004 · 位移場與同失真隨機對照無法區分

- **量一**：`edit_lpips`（τ=0.20、identity）N1 **0.2539** 對 R **0.2427**，比值 **1.046**
- **量二**：語意失敗數（τ=0.35、15 樣本）N1 **4/15** 對 R **4/15**
- **旁證**：N1 的 4/15 低於未防禦對照的 5/15，即防禦之後編輯**更容易成功**
- **來源**：EXP-v14r（`runs/v14r_merged/grid.csv`、`runs/v14r_margin.csv`）
- **後果**：DEC-005（放棄位移場方向）
- **狀態**：成立

## FND-008 · 非加性不靠劣化製造免疫

- **量**：ΔNIQE（正值代表輸出比未防禦更糟）、銳利度比
- **值**（EXP-v14r）：位移場 N1 −0.423、N2 −0.391、R −0.292；
  加性 mist +1.331、photoguard_c +0.532（銳利度比 0.356）
- **判讀**：這是三層評測裡唯一站得住的非加性差異。**不是**「防禦得比較好」
- **狀態**：成立

## FND-013 · 非加性把失真換成輸出位移的效率約差三倍

- **量**：`edit_lpips`（對未防禦的編輯，愈高代表推得愈遠），identity
- **值**（EXP-s3a）：

  | τ | apa | Ra（隨機） | apa/Ra | 最佳 baseline | apa/最佳 |
  |---|---|---|---|---|---|
  | 0.20 | 0.123 | 0.098 | 1.25× | 0.394 photoguard | **0.31×** |
  | 0.35 | 0.242 | 0.184 | 1.31× | 0.523 photoguard | 0.46× |
  | 0.50 | 0.507 | 0.360 | 1.41× | 0.618 mist | 0.82× |

- **判讀**：次主張的兩道門檻，(b) 勝過隨機對照**每個 τ 都通過**；
  (a) ≥0.85×最佳 baseline **都不通過**。τ=0.50 追到 0.82× 是靠把圖毀掉換的
- **狀態**：成立

## FND-018 · 抗淨化：非加性勝過三個加性 baseline，但優勢來自參數化而非注意力損失

- **來源**：EXP-s3t20，`scripts/purify_advantage.py --readout edit_lpips --tau 0.04`
- **判定**：apa > photoguard_c、apa > mist、apa > dia_r，**7/7 算子嚴格成立**
- **閘門**：1425 列有 **1350 列**通過 3σ（改用 `effect_siglip` 時 65 組只過 1 組，
  見 DEC-004／DEC-006 與 `purify_advantage` 的 `READOUT_DEFAULT` 註解）
- **但三項必須並列的事實**：
  1. retention 的分母小：identity 上 apa 0.106、Ra 0.062、photoguard_c 0.356
  2. diffpure 150 上三者在雜訊內（逐影像 sd 0.018–0.039，彼此相差 0.014–0.024），
     該點的 0.21 是 DiffPure 自身的隨機發散底線
  3. **Ra 處處等於或高於 apa**（jpeg30 0.202 對 0.190；blur2 0.064 對 0.070）。
     Ra 是同一個 site 上沒有式 (5) 的隨機方向
- **後果**：主張一在「非加性 site」的層級成立；在「Lo 式 (5) 有貢獻」的層級
  **不成立**。DEC-016 的損失消融即為了分開這兩者
- **缺口**：`purify_advantage.main` 的 `RANDOM` 取 `grid.RANDOM_CONTROL`（位移場的
  `R`），site apa 的對照是 `Ra`，故 `apa > Ra` 這一項被靜默跳過而未列印。
  `RANDOM_BY_SITE` 已存在但未被 `main` 使用
- **狀態**：成立

## FND-019 · 跟著原圖細節走的是重建下限，不是防禦擾動

- **量**：32×32 區塊的殘差能量，與原圖梯度能量的 Pearson 相關；前 10% 區塊佔比
- **值**（EXP-s3t20，apa，Δ=0.04）：

  | 影像 | 純重建下限 `x_floor − x` | 防禦擾動 `x_def − x_floor` |
  |---|---|---|
  | horse_00 | 相關 0.974 | 相關 **0.300**，top10% 0.493 |
  | horse_03 | 相關 0.803 | 相關 0.679，top10% 0.402 |
  | woman_03 | 相關 0.919 | 相關 0.823，top10% 0.509 |

- **更正**：2026-08-10 稍早以 `x_def − x` 量得的「與細節相關 0.85–0.98」是
  **下限的紋路**，不是防禦的。Δ=0.04 上下限佔總失真一半以上
- **仍然成立的部分**：防禦擾動**在空間上高度集中**（前 10% 區塊佔四到五成能量），
  而三道保真 hinge（LPIPS、銳利度、色度）全是空間平均，集中幾乎不付代價
- **後果**：DEC-016 的 B2（最差區塊 hinge）
- **狀態**：成立

## FND-020 · 抗淨化的優勢在 inpainting 上重現，型態與 img2img 完全相同

- **來源**：EXP-ip20，`scripts/purify_advantage.py --readout edit_lpips --tau 0.04`
- **判定**：apa > photoguard_c、apa > mist、apa > dia_r，**7/7 算子嚴格成立**；
  1425 列有 **990 列**通過 3σ 閘門
- **與 FND-018 同一個 caveat**：Ra 的 retention 普遍高於 apa
  （diffpure 150：Ra 2.846、apa 1.580；crop_resize 0.1：2.168 對 1.308）。
  兩個威脅模型獨立給出同一個結論——**優勢來自非加性參數化，不是 Lo 式 (5)**
- **第 1 層（identity）**：photoguard_c LPIPS 0.3589、dia_r 0.2390、
  apa 0.1563、mist 0.1479、Ra 0.0919
- **第 3 層**：photoguard_c 的銳利度比 **0.4981**、apa 0.8156、mist 0.9455、
  dia_r 1.1984。與 FND-017 一致，photoguard_c 的位移買在劣化上
- **狀態**：成立
