# 交接 prompt（2026-08-01）

以下整段可直接貼進新 session。原文保留於此以便日後查閱。

---

WACV 專案接手 — 白盒非加性抗文字編輯防禦

## 先做這件事

讀 `docs/NEXT_SESSION.md`（2026-08-01 改寫，是主軸否定結果的完整交代），
再讀 `CLAUDE.md`（研究範圍、環境、工作紀律）與 `docs/INDEX.md`（哪份文件現行、
每個 run 目錄屬於哪個實驗）。要細節再讀 `docs/RESULTS_E20_fidelity.md` 與
`docs/RESULTS_E21-E22.md`。這幾份讀完就有完整脈絡，不需要翻 git log。

## 現況：主軸目前是否定的

原本的主軸是「在匹配人眼可辨失真下，非加性在抵抗文字編輯上勝過加性」。
上一階段把量測工具修好之後，這個結論**目前沒有證據支持**：

- E15 的「site S 領先 1.15×」是 LPIPS 對模糊不收費造成的假象。加上鈍化約束
  後，site S 失去 67.3% 的 net，site P 只掉 8.3%。
- 補救方向（site S 的 grid_sample 改 bicubic，銳利度 85%→99.9%）一度看起來
  可行（25 步下 1.14–1.15×），但把步數提到 100、兩臂都真正被約束綁住後，
  τ=0.05 反轉為 **0.85×**。site P 從多給的步數獲益 +51%，site S 只有 +13%。
- τ=0.02 的 1.15× 未被證實也未被推翻（驗證用的 E24 未產生資料即中止），
  **不應引用**。

## 這一階段要做的事

**重新思考方向。** 使用者在看到上述結果後決定停掉目前方向的實驗。
不要直接接著跑實驗，先討論方向。

`docs/NEXT_SESSION.md` §5 列了三條路（A：接受否定結果改寫論文重心；
B：先修協議再重跑確認；C：換非加性參數化），但那是上一階段的判斷，
不是限制。使用者要的是重新思考，可以提出這三條之外的。

## 不要重走的死路（全部有資料支撐，見 NEXT_SESSION §4）

- 對抗性強健的感知度量（E-LPIPS / R-LPIPS / LipSim）——解的是相反的失效
- NLPD、VIF、GMSD、HaarPSI 當保真約束——實測它們量的是位移不是模糊
- low rank——使用者 2026-07-30 明確排除
- site L / E / W（A 族）——VAE 重建地板高於加性運作點
- 在固定 25 步的網格上比較兩個 site——φ 量綱與 lr 都不同

## 哪些結論是硬的（可以直接拿來用）

有資料、有測試、可重現的八條列在 `docs/NEXT_SESSION.md` §3。最重要的三條：

1. 用單一純量定義「匹配失真」，在失真種類不同的兩族之間本來就不成立。
2. 判別一個指標「實際在收什麼費」的方法：四臂等 LPIPS 探針（模糊／雜訊／
   變形-雙線性／變形-雙三次），人眼比對頁在
   `runs/p1_iso_lpips_probe/compare.html`。
3. `src/metrics/local_acutance.py` 同時滿足「對位移不敏感」與「不可抵銷」，
   是目前唯一通過四臂判別的可微約束。

## 已知但未修的缺陷

- **固定步數是錯的協議**：它讓不同格子被不同的東西綁住，影響 E13 之後
  所有網格的可比性。正確做法是以「約束啟動並穩定」為停止準則。
- **cross-attention 的預設 `divergence` 模式梯度恆為零**（φ=0 時 KL 在
  最小值，梯度為 0）。該目標從未在 GPU 上跑過，跑了會靜默什麼都不做。
  `entropy` 模式正常。測試已把此缺陷釘住。

## 程式與資料狀態

- 分支 `claude/e20-fidelity-constraint`，9 個 commit，已推上 origin，
  **未併入 main**（未經授權不得併）。
- 測試基準 **186 passed / 1 skipped**。
- `src/defense/objective.py` 已是交集式雙 hinge（LPIPS ∩ 鈍化），
  `alpha_ssim` 已設 0（實測它在補貼模糊）。before/after 記在該檔 docstring
  的「修訂之三」。
- `site_warp.py` 有 `resample` 參數，預設仍為 bilinear 以保 E13–E19 可重現。

## 環境

- 本機 Python：`C:/Users/nelso/miniconda3/envs/wacv/python.exe`（不是 base）
- TWCC 容器 203.145.216.165:57683（實體代號 6057060）**仍存在但已閒置**。
  SSH 為金鑰認證不需密碼。**容器刪不掉**：API 金鑰有效但專案清單為空、
  所有路由回 `no Route matched`、SSH 橫幅顯示帳號到期，需由使用者從 TWCC
  網頁刪除。若開新容器，host/port 由使用者提供。
- `/work/nelson0314` 是 NFS，跨容器保留（conda env、repo、hf_cache）。
  執行前 `source /work/nelson0314/WACV/env.sh`。容器預裝的 NGC torch
  不支援 V100 的 sm_70，必須用 conda env 的 torch cu118。
- 遠端 `git pull` 常因 `runs/` 未追蹤檔 abort，先 `mv` 到
  `/work/nelson0314/pull_backup/` 再 pull。**`env.sh` 也是未追蹤檔，
  會被一併移走，記得還原**（repo 內副本 `scripts/twcc_env.sh`）。

## 工作要求

- 繁體中文、客觀學術語氣；程式碼關鍵字與套件名維持英文。commit message 用英文。
- 動手前先驗證假設（讀檔、跑指令），不要憑記憶猜 API。
- 修改論文方法要記 before/after：具體行號、原貌、原因。
- 架構或實驗設計先提計劃討論再寫程式。環境問題直接修掉，不用寫進報告。
- 宣告完成前必須實際跑過並看到成功輸出；失敗就說失敗。
- 未經明確授權不得把分支併入 main。
- 禁止用 try/except 或條件跳過掩蓋症狀，要找根本原因。
- 指標之間出現矛盾時，把前後影像做成比對頁給我親眼判斷，不要用數字自行定調。
- **推翻自己先前的判斷時，把錯誤的假設與推翻它的資料一起留在文件裡，
  不要改寫成正確版本**（上一階段有一個 p99/上界 0.97 的數字誤導了整個
  E22 的設計，記錄它如何誤導比讓後人再踩一次便宜）。
