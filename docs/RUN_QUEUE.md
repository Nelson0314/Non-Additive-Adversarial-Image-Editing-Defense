# 待跑清單

**全部在 NYCU BASIC lab 跑。** 卡是多人共用的，**每卡最多 2 個 process**
（`OPERATIONS.md`，第 3 個實測會 OOM）；所有派工腳本都內建
`bash scripts/free_cards.sh --assert "${DEVS[*]}" || exit 3`，指定的卡上只要有
別人的 process 就拒絕啟動。**卡號一律由參數給。**

順序是按「能改變結論的程度」排的，不是按成本。

單價（實測）：一次編輯 **46.8 秒**，故「一個條件 × 一張影像 × 一個算子」
＝ 3 種子 × 46.8 s ≈ **140 秒**；防禦一個十張工作點 **60–85 分鐘**。

## 正在跑

| 批次 | 內容 | 為什麼重要 |
|---|---|---|
| `ip2p_dct_unified/du_plane_08`、`du_plane_11` | 整併版的兩個小角度 | 前三個角度全部落在失真帶外，**沒有帶內的點就沒有等失真比較**，U1 既不能通過也不能否決 |

---

## 第一優先 · 三支純 CPU 探針，全部不花機時

**這三支任一支的結果都可能直接關掉一條路。** 與 `--coarsen` 那次同模式：上機前
的 CPU 檢查否證了機制，省下整批 GPU 時間。

### 1a. 整併設計甲的天花板：把旋轉平面限制在非零支撐上

整併版第一次嘗試的失敗原因已量出來：交付品質 0.85 的量化把通帶內 **81%** 的
係數清成零，旋轉只能在少數**大**係數之間搬能量，而平面上的另一個方向多半落在
原本是零的高頻格子——**L∞ 精確等於 1.000**，有像素被從全黑推到全白。

設計甲把平面限制在 `|α| > 0` 的支撐上。**可證偽的預測**：同一個 θ 下 L∞ 明顯
低於 1.000 且 DISTS 下降；若 L∞ 仍然飽和，第 2.1 節的歸因要撤回。
沿用 `scripts/dct_nonadditive_ceiling.py` 的模式（不最佳化、直接轉到角度上界）。

**成本**：半天，0 GPU。設計見 `runs/integration_design/README.md` §3.1。

### 1b. 整併設計丙的天花板：保長的整數位移

旋轉被綁在二維平面上，在稀疏的整數係數上那個平面很難挑。改成直接學整數位移
`δ`，約束 `‖α + δ‖ = ‖α‖`（閉式投影：實數位移 → 依比例縮回原長 → 取整）。
可行集由「一個二維平面」變成「整個 63 維球面」。

**判準**：天花板進不了失真帶就結案。**成本**：半天，0 GPU。§3.3。

### ~~1c. 模糊那一欄的可贏上界~~ — **已跑完，該欄結案**

`scripts/blur_band_ceiling.py`，十張、σ=1。事前判準是「最佳帶對現行工作帶的
比值低於 3 則結案」，**實測 1.54**（能量模型 2.23）。

存活率與編碼器敏感度是兩條**方向相反且都很陡**的曲線（跨帶分別差 250 倍與
三個數量級），乘積在整個頻域上是平的，極大值落在本方法**已經在用**的
0.884–1.061，只高 1.54 倍。**模糊底下沒有好的頻帶可以搬。**

證據與四項保留見 `runs/fixedpoint_blur/README.md`。

---

## 第二優先 · 多品質集成損失 ＋ 單一格點交付

**這是抗淨化那一側唯一還沒試、而且有文獻依據的機制。**

Shin & Song 的核心結論是**單一品質會過度特化**，而我們的 `--deliver-jpeg 0.85`
正是單一品質；症狀在資料上看得到（jpeg75 的 0.4370 掉到 jpeg30 的 0.2087）。
MBRS（ACM MM 2021）用「每個 mini-batch 隨機選真實／模擬／無失真」拿到同一個
效果。

**集成只能放在損失上，交付仍是單一格點**——放到交付上就是已否決的
`--purify-aware`（那三個變體把 JPEG 放進迴圈卻交付未壓縮的圖，量化格點的性質
完全沒拿到）。零件已實作：`purify_aware.make_eot_jpeg_transform`，**從未接上
交付**。

**事前判準**：QD 0.85 的交付下，jpeg30 的淨增益要高於現行的 0.1072，且 jpeg75
不得掉超過 10%。

**成本**：抽樣版每步成本幾乎不變（不是 K 倍前向），三個防禦點約 3.5 GPU-h，
抗淨化約 5.8 GPU-h。

---

## 第三優先 · 階段二擴到十張（**待裁定**）

篩選批三張已跑完（防禦 ＋ 抗淨化），四條事前判準都過：安全繩一次都沒觸發、
模糊與裁切兩欄一起升、贏過「只多跑步數」的對照、帳算得平（保守點賺 0.0327、
賠 0.0031）。等失真校正之後真正賺到的是**模糊 +9.2%、裁切 +5.6%**。

**要一起讀的兩件事**：(a) n = 3，幅度只有 5–9%；(b) **那兩欄本來就擋不下來**
——模糊 σ1 的空白地板 0.1559 是我們最好淨增益 0.0986 的 1.6 倍，裁切 10% 是
3.4 倍。

**還有一個已知缺陷要先修**：安全繩綁的是**訓練損失的增益**，不是實際位移。
兇那一點守住了 0.9495（沒觸發），但未淨化位移掉了 6.1%。要擴到十張的話應該
先把檢查換成綁在位移的代理上。

**成本**：防禦三點 ＋ 抗淨化，約 7 GPU-h。

---

## 第四優先 · 整併掃描補齊（**看第一優先與正在跑的結果**）

`du_plane_08`／`du_plane_11` 若拿到帶內的點且贏過事後投影版
（`nd_plane_25_qd`：DISTS 0.1617／位移 0.5679／6-of-10），整併方向成立，補齊
掃描並跑抗淨化；若輸，改走設計甲或丙。

**設計乙（把交付格點調細到 QD 0.95）只有在甲、丙都不成立時才值得**：它與我方
「降低交付品質換 jpeg30」的方向相反（QD 0.85→0.45 讓 jpeg30 漲 68%），
拉高 QD 等於把有效範圍讓給高品質那一端。

---

## 不建議再花機時的方向

| 方向 | 為什麼停 | 證據 |
|---|---|---|
| **裁切的專門機制** | 文獻上對付幾何攻擊的四條路（不變域、模板、自同步、特徵同步）**全部要求一個我方控制的解碼端**，而防護擾動沒有那個東西。我方的 log-periodic 候選就是「不變域」那一條：對 s=1.2488 方向存活 0.9726，但 1.15 與 1.30 已歸零 | `reference/SURVEY_NOISE_RESISTANCE.md` §1.4／§2、`log_periodic_probe` |
| **模糊的專門機制** | **可贏上界已量出：1.54 倍**，事前判準是 3。存活率與編碼器敏感度方向相反且都很陡，乘積在整個頻域上是平的，極大值就落在本方法已經在用的帶 | `fixedpoint_blur/README.md`、`phase_drift_diagnosis` |
| **位移場硬訓練** | 兩個端點都不成立：grid 16 到不了失真帶，grid 64 到得了但付 1.92 倍失真只換 65% 的位移 | `ip2p_warp_hard` |
| `--coarsen`（θ 空間平滑） | 上機前的 CPU 探針否證了機制：JPEG 保留率在三種設定下全部微降 | `ip2p_coarsen/probe*.csv` |
| AdvDrop | 最佳化與自身機制互斥，>50% 的 DCT 格梯度方向相反 | `ip2p_advdrop_ceiling` |
| `dct_aj85_eps3.2`／`eps4.5` | 相對 eps2.2 只多 +0.0055 位移（±0.0156），多付 0.045／0.081 DISTS | `ip2p_mainline/tables/` |

## 尚未動工但已查證的旋鈕

`reference/SURVEY_FREQUENCY.md` §1.14–1.15 與
`reference/SURVEY_NOISE_RESISTANCE.md` §1.2。依規矩**先提計劃討論再寫程式**。

1. **多品質集成損失**（Shin & Song 2017）→ 已升為第二優先。
2. **θ 半解析度參數化**已實作為 `--coarsen`，機制已被否證，見上表。
3. **兩階段可分離訓練（TSDL）／END² 雙解碼器**：**不可移植**，兩者都需要一個
   可以單獨再訓練的解碼器，本專案的「讀出端」是攻擊方的擴散模型。
