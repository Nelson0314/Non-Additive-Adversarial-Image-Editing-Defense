# E31 本機階段：判準補完、預算不可達、以及本機能做什麼

<!-- STATUS-BLOCK -->
| | |
|---|---|
| **狀態** | 現行。本機階段已完成；雲端的 R1／R2 尚未執行 |
| **日期** | 2026-08-02 至 08-03 |
| **硬體** | 本機 i5-12500H + RTX 2050 4 GB、torch 2.13.0+cu126、SD v1-4、512² |
| **承接** | `docs/RESULTS_E29_negative.md` §5 的兩個結構性問題 |
| **後續** | 雲端 R1／R2，見 `docs/NEXT_SESSION.md` |
| **資料** | `runs/p11_degrade_ladder`、`runs/p12_isr_rejudge`、`runs/p13_budget_probe`、`runs/p14_budget_thresholds`、`runs/e31_sources`、`runs/e31_strength_sweep`、`runs/logs/e31_*` |

> 主張的索引見 [`docs/LEDGER.md`](LEDGER.md)；檔案的索引見 [`docs/INDEX.md`](INDEX.md)；
> 比對頁見 [`docs/gallery.html`](gallery.html)。

**這一段全部在本機執行，零雲端 GPU 成本。** 產出是四項：判準補完、
一個把 τ=0.28 判為不可達的量測、逐預算的門檻、以及本機能力的兩個邊界。
其中兩項改變了 E31 的設計，一項改變了本機／雲端的分工。

---

## 1. 判準的另一半：資料早就在，只是沒人讀

E25 §1.1 把防禦成功的判準由 `net_lpips` 改為語意軸是對的。但
arXiv:2512.14320 定義的 ISR 是一個**聯集**——語意不符 prompt **或**明顯的
感知劣化——而 E25 之後只取了前半。

補後半時發現一件與 E25 同型的事：`results.csv` 自 E2 起就記了
`edit_niqe_a`（未防禦編輯的無參考品質）與 `edit_niqe_b`（防禦後編輯的），
出處是 `src/metrics/suite.py::full` 對 a、b 各報一次。**全專案沒有任何腳本
讀過它們。** E25 §1 發現 `edit_siglip_*` 從未被讀過，這是第二次。

`scripts/p12_isr_rejudge.py` 以聯集判準重判既有全部 run：

| | 值 |
|---|---|
| 可判定的格數 | 828 |
| 語意失敗 | **0** |
| 感知劣化（門檻 1.0 的暫定值） | 229 |
| 乾淨攻擊（`identity`、strength 0）那一列 | 36 格，ISR 成立 **0** 格 |

語意失敗 0 格與 E25 的 726 格 0 格一致，是同一個結論在更大樣本上的複現。

**唯一有訊號的一格判不了。** `runs/e29c_P_tau0.10` 的 identity 那一列
ΔNIQE = **+1.571**（3.104 → 4.675，NIQE 越低越好，即防禦後的編輯輸出確實
較差）。但該 run 用 `--limit 1`，n = 1，而判定要求 n ≥ 2——n = 1 時
`pstdev` 恆為 0，任何判定都自動成立，E25 曾因缺這個條件產生 24 格假陽性。
E31 網格用 2 張圖，n = 2 才判得動。

### 1.1 門檻還沒定

感知劣化的門檻不得憑指標聲譽選（本專案已踩過 ΔE00、NLPD、VIF 三次）。
`scripts/p11_degrade_ladder.py` 造了一道階梯：四個算子
（高斯模糊、JPEG、加性雜訊、色階量化）各四級，施加在**未防禦的編輯輸出**上，
四個候選無參考指標（`niqe`、`musiq`、`clipiqa`、`topiq_nr`）各評一次，
產出 `runs/p11_degrade_ladder/compare.html`。

來源取編輯輸出而非原圖：編輯輸出本身是擴散生成的，其無參考品質基線與自然
照片不同（實測 e29c 的 `edit_orig` NIQE 為 3.00）。

**待使用者判讀該頁**，指出每一列第一個「這張圖已經不能用」的欄位。

---

## 2. τ=0.28 在原約束集下不可達，且與防禦方法無關

E31 規格 §8 原本預期 τ=0.28 下 `local_acutance_dev`（門檻 0.04）與
`local_chroma_bias`（門檻 0.8）「可能翻上來成為綁定者」，並把處置留給雲端
實測。`scripts/p13_budget_probe.py` 用零成本先給出答案，比預期強得多。

作法是取 `runs/e29c_*` 已有的擾動 δ = x_def − x_base，沿射線等比放大直到
LPIPS 命中各個目標，在那一點量兩道約束。關鍵是加了一個**參照臂**：
i.i.d. 白高斯雜訊，沒有任何空間結構，是能造出來最中性的擾動。

| 預算 LPIPS | 白雜訊 acut | 白雜訊 chroma | site P 解 acut | site P 解 chroma |
|---|---|---|---|---|
| 0.10 | 0.0121 | 0.2143 | 0.0232 | 0.7694 |
| 0.15 | 0.0238 | 0.3701 | **0.0440 ⚠** | **1.2526 ⚠** |
| 0.20 | **0.0414 ⚠** | 0.5867 | 0.0718 ⚠ | 1.8096 ⚠ |
| 0.28 | **0.0875 ⚠** | **1.1284 ⚠** | 0.1302 ⚠ | 2.8275 ⚠ |

**連白雜訊都在 LPIPS 0.20 超過 τ_acut、在 0.28 同時超過兩道。** 也就是說
現行門檻把可達的 LPIPS 上限壓在 0.15–0.20，這是門檻的性質而非最佳化找到
什麼解的性質。在該約束集下，「文獻預算下防禦會不會成立」這個問題問不出來
——R1 的 gate 會失敗，但失敗的原因與防禦無關。

原因是兩道門檻都是**絕對值**，且都在 τ_lpips = 0.05 的量級上定的
（τ_acut 由 E20 的四臂探針、τ_chroma 由 E28 的人眼階梯）。預算放大 5.6 倍後，
任何擾動的局部梯度能量與色度位移都跟著放大。

**這一項的方法學價值。** 若沒有那個白雜訊參照臂，看到「site P 的解在
0.28 超標」會被讀成「這個解在該預算上買了鈍化與色偏」——那是第四次
「匹配失真是假的」。加了參照臂才分得出來：不是解的問題，是門檻的問題。
這與 E28 §1 判別 ΔE 族時用的是同一個手法（等 LPIPS 下比較不同失真型態）。

---

## 3. 兩道次要門檻改為逐預算

使用者於 2026-08-03 選定的處置。完整規則見規格 §12。

要點是這兩道門檻的職責本來就是相對的：它們不負責限制失真量——那是
τ_lpips 的事——而是**擋住最佳化去買 LPIPS 收費不足的那兩種失真**，
即模糊（E18／E19）與空間連貫的色偏（E27／E28）。職責相對，門檻就該相對。
原本寫成絕對值，是因為在那之前只有一個預算。

`scripts/p14_budget_thresholds.py` 沿用 E20 的多臂等 LPIPS 探針，把預算
加成一個軸：對每個 τ_lpips，把五個臂（實作直接沿用 `p9_chroma_probe.py` 的
`make_arm`）全部校準到該 LPIPS，量各臂的 acut 與 chroma，門檻定在兩群之間。

門檻在對數區間上的位置**不取幾何中點，而是沿用既有人眼定錨值的位置**：
先在定錨預算（0.05）算出 τ₀ 落在 [通過側上緣, 擋下側下緣] 的哪個位置，
再在每個預算用同一個位置取值。這樣規則是延伸使用者的判斷，不是取代它。
實測 τ_acut = 0.04 落在 pos = 0.147、τ_chroma = 0.8 落在 pos = 0.685，
兩者都在 [0, 1] 內。

三張影像、五個預算的結果：

| 預算 | τ_acut | (通過≤ / 擋下≥) | τ_chroma | (通過≤ / 擋下≥) |
|---|---|---|---|---|
| 0.05 | 0.0400 | 0.0322 / 0.1419 | 0.80 | 0.1387 / 1.7907 |
| 0.10 | 0.0594 | 0.0499 / 0.1637 | 1.29 | 0.2285 / 2.8472 |
| 0.15 | 0.0749 | 0.0650 / 0.1700 | 1.77 | 0.3141 / 3.9283 |
| 0.20 | 0.0900 | 0.0802 / 0.1763 | 2.30 | 0.4013 / 5.1218 |
| 0.28 | 0.1648 | 0.1608 / 0.1896 | 3.40 | 0.6292 / 7.3897 |

**gate 因此可以過**：site P 的解在 τ=0.28 是 acut 0.1302、chroma 2.8275，
兩者都在門檻內。τ=0.28 這一臂由不可跑變成可跑。

順帶一個獨立的佐證：在定錨預算上，單純取幾何中點（pos = 0.5）在 chroma 軸
給出 **0.802**，對 E28 由人眼定出的 **0.8**。該規則在那一軸上獨立重現了
人眼的判斷。

### 3.1 但 acut 軸的分離度隨預算塌掉

`scripts/p14_separation.py`，資料在 `runs/p14_budget_thresholds/separation.csv`。
比值 = 擋下側下緣 / 通過側上緣，小於 1 即分不開。

| 預算 | 現行歸屬 | 只用 noise 對 blur |
|---|---|---|
| 0.05 | 4.41 | 18.73 |
| 0.10 | 3.28 | 11.27 |
| 0.15 | 2.61 | 7.91 |
| 0.20 | 2.20 | 5.88 |
| 0.28 | **1.18** | 3.82 |

塌掉的原因具體可指：τ=0.28 時 `noise`（0.1608）、`warp_bilinear`（0.1650）、
`warp_bicubic`（0.1649）三者的 acut 全部收斂，只有真正的高斯模糊（0.6142）
還分得開。位移大到那個量級時插值核心已不重要，兩個變形臂都在鈍化。
chroma 軸沒有這個問題，各種歸屬下比值都維持在 11.7–12.9。

這暴露了一個更一般的問題：**臂的歸屬是在 τ=0.05 上由人眼判定的**——E20 的
比對頁看的是 0.4–0.6 px 的位移，而 τ=0.28 的位移是數倍大。把該判定外推到
別的預算並不當然成立。兩個處置選項與取捨寫在規格 §12.4，**尚未決定**。
在決定之前 driver 用的是較保守的那一組，不會靜默放寬。

---

## 4. 本機能力的兩個邊界

兩項都是實測，不是推論。不設降級退路：OOM 就讓它 OOM。

| 工作 | 結果 | 出處 |
|---|---|---|
| **無梯度** 512² SDEdit（n_edit=10、w=7.5） | **可跑**。峰值 4873 MB（超過 4 GB 實體，靠 Windows 共享記憶體外溢）、單次 222.5 s | `runs/logs/e31_local_probe.log` |
| **含梯度** 256² 訓練（n_edit=10、k_inv=10、rank 16） | **不可用於網格**。178.04 s/step、峰值 5628 MB | `runs/logs/e31_train_probe.log` |

第二項的意涵要算一下才看得出來：H100 在 512² 上是 2.36 s/step，本機在
四分之一的像素上是 178 s/step，即 **75 倍慢**。單張影像跑滿 150 步是
7.4 小時，而網格是 12 個點 × 2 張影像 = 24 次最佳化，上限約 178 小時；
開平台停止後實測多在 70–100 步，仍逾 100 小時。**本機不能用來跑縮小版的
網格去風險**，雲端那一輪不可省。320² 與 384² 沒有續測——結論已經成立，
再測只是重複。

第一項的意涵相反：**評測側可以整個留在本機**。比對頁、劣化階梯、判定、
報表都不必占用雲端時間。本階段的五項產出全部是這樣做的。

### 4.1 本機的三個實務陷阱（已修，記下來免得重踩）

1. **`PYTHONIOENCODING=utf-8` 必加。** Windows 預設 cp950 編不了 `²`，
   症狀是一支已經跑完的腳本在**印出結果時**才炸掉，前面的計算全部白做。
2. **不要並行跑兩個 GPU 工作，也不要讓 CPU 密集工作與 GPU 工作並行。**
   實測 `p14`（CPU 上的 LPIPS）與 `e31_make_edits`（GPU）並行時，後者單張
   耗時由 222 s 拉長到 30 分鐘以上——CPU 那側把 GPU 工作的 Python 執行緒
   餓住了。`scripts/drivers/local_night.sh` 因此把它們串起來。
3. **`pyiqa` 的 `clipiqa` 與 `topiq_nr` 缺相依。** 需補
   `clip-anytorch`、`ftfy`、`wcwidth`、`facexlib`，並把 `setuptools` 由 83
   降到 80 以取回 `pkg_resources.packaging`。四個都用 `--no-deps` 裝。

---

## 5. 攻擊強度掃描（遷移設定，為下界）

`scripts/e31_strength_sweep.py`，資料在 `runs/e31_strength_sweep/`。

用既有的防禦圖在 strength 0.2／0.3／0.4／0.5 下被編輯，量 SigLIP 與 NIQE。
三臂（未防禦、site P、site C）共用同一個 ε。

**兩個限制，兩個都會改變可以下的結論。**

1. **這是遷移不是匹配設定。** 防禦圖在 strength = 0.5 下訓練，在其他
   strength 上評測。白盒下訓練與攻擊的 strength 必須一致，那是 E31 網格要
   量的。匹配設定應優於遷移設定，故本項量到的是**下界**。
2. **兩臂不在匹配失真上，故 P 與 C 之間不可比。** `e29c_P_tau0.10` 的
   `final_lpips` 是 0.0856，而 `e29c_C_tau0.10` 只有 0.0386——site C 那一格
   是被色度 hinge 綁住而非被 τ 綁住（E29 §3），兩者停在不同的失真上。
   **只有各臂自己隨 strength 的趨勢是乾淨的**，跨臂的差異有 2.2 倍的
   失真量差在裡面。

### 5.1 未防禦基線隨 strength 單調上升

| strength | 未防禦 SigLIP | 未防禦 NIQE |
|---|---|---|
| 0.2 | 0.0587 | 3.416 |
| 0.3 | 0.0619 | 3.177 |
| 0.4 | 0.0973 | 2.785 |
| 0.5 | 0.1090 | 2.941 |

即 strength 越低，未防禦的編輯本身越不服從 prompt。這是要先擺出來的：
低 strength 下「語意不符」比較容易達成，但**威脅本身也比較弱**。

### 5.2 降低 strength 沒有幫到加性防禦，反而相反

| strength | Δsiglip(P) | Δsiglip(C) | Δniqe(P) | Δniqe(C) |
|---|---|---|---|---|
| 0.2 | +0.0164 | −0.0122 | +0.567 | −0.227 |
| 0.3 | **+0.0524** | −0.0120 | **+0.960** | −0.135 |
| 0.4 | +0.0165 | −0.0155 | +0.842 | +0.028 |
| 0.5 | +0.0049 | +0.0005 | +0.780 | −0.211 |

E31 規格 §5 把 strength 納入網格的理由是推論：在 strength = 0.5 的全域編輯下
輸出主要由 prompt 重新生成，故「語意不符」幾乎不可達成，而這**很可能是 E29
為負的最大單一原因**。

**本項的量測與該推論相反。** site P 的 Δsiglip 在每一個 strength 上都是正的
（防禦後**更**服從 prompt），且在文獻慣用的 0.3 上**最大**（+0.0524，是
strength=0.5 下 +0.0049 的十倍）。也就是說降低 strength 不但沒有讓防禦
變好，反而讓 ICIP 2025 觀察到的那個反向現象變得更明顯。

### 5.3 兩臂在 ISR 的不同半邊上有訊號

這是本項最值得記的一件事：

- **site P（加性）只在劣化那一半有訊號。** Δniqe 在四個 strength 上一致為正
  （+0.57 到 +0.96），而 Δsiglip 一致為正（對防禦不利）。
- **site C（非加性）只在語意那一半有訊號。** Δsiglip 在 0.2/0.3/0.4 上一致
  微負（−0.012 到 −0.016），而 Δniqe 幾乎為零或負（即輸出沒有變差）。

兩者在同一個判準的兩半上分別有微弱訊號，而且方向互補。若 E31 的網格重現
這個型態，那對「加性與非加性在做不同的事」是一個具體的證據，比
`edit_shift` 的數值差異有內容得多。

**但這個型態跨影像不成立，見 §5.4。** 上表全部來自 car_00 一張影像。
`scripts/p15_editpage_degrade.py` 在兩張影像上重算時，site C 的 Δsiglip
在 car_00 是 +0.0074、在 car_01 是 −0.0678——**符號相反**。

**限制。** 單一影像（car_00）、n = 1，依 n ≥ 2 規則不可判定；且是遷移設定。
上列數字只能當作趨勢與下界，不可寫成結論。

### 5.4 該型態跨影像不成立（p15，n = 2）

`scripts/p15_editpage_degrade.py` 對 `runs/e29_edit_page/` 的影像重算。
那個目錄有 car_00 與 car_01 兩張，是既有資料裡唯一 n = 2 的地方。

| 頁面 / 臂 | n | ΔSigLIP | Δ劣化 | 語意 | 劣化 | ISR |
|---|---|---|---|---|---|---|
| `e29_edit_page` / `e29_C_lr0.3` | 2 | −0.0302 | +0.365 | 否 | 否 | 否 |
| `e29_edit_page` / `e29_P_lr0.03` | 2 | +0.0092 | +0.103 | 否 | 否 | 否 |
| `e29_edit_page` / `e29b_C_lr0.2` | 2 | −0.0021 | +0.234 | 否 | 否 | 否 |
| `e29c_edit_page` / `e29c_C_tau0.10` | 1 | −0.0071 | −0.101 | 否 | 否 | 否 |
| `e29c_edit_page` / `e29c_P_tau0.10` | 1 | +0.0129 | +1.582 | 否 | 否 | 否 |

逐圖看，§5.3 的型態不成立：

| 臂 | car_00 的 Δsiglip | car_01 的 Δsiglip |
|---|---|---|
| `e29_C_lr0.3` | **+0.0074** | **−0.0678** |
| `e29b_C_lr0.2` | +0.0012 | −0.0053 |
| `e29_P_lr0.03` | +0.0196 | −0.0013 |

**同一個防禦在兩張影像上的符號相反。** §5.3 的「site C 一致微負」只成立於
強度掃描那一組，而那一組是同一張影像（car_00）的四個 strength。跨影像看，
該型態沒有支撐。上文不刪除，此處限縮它的適用範圍。

**這同時是 n ≥ 2 規則的具體案例。** `e29_C_lr0.3` 的平均 Δsiglip 是 −0.0302，
看起來像語意失敗，但標準差 0.0376 大於它，判定正確地否決。若只有 car_01
那一張（−0.0678），會被判成很強的語意失敗——那正是 E25 §1.2 在
`e6_stepsP`／`e6_stepsLA` 上產生 24 格假陽性的機制。

### 5.5 對 E31 網格的意涵

strength 這一軸**仍然要跑**，但預期要調整。本項量的是遷移：防禦在 0.5 下
訓練、在 0.3 上評測。匹配設定（在 0.3 下訓練也在 0.3 下攻擊）可能得到完全
不同的解——低 strength 下擾動被稀釋得少，最佳化的著力點不同。

但「E29 為負主要是 strength 造成的」這個解釋，現在有一份與它相反的量測。
若網格的 strength=0.3 那幾格也是負的，那個解釋就可以排除，而不是留著當
未檢驗的替代說法。

完整資料見 `runs/e31_strength_sweep/sweep.csv`、`deltas.csv` 與 `compare.html`。

---

## 6. 三族判準之間的相關性（既有資料，n = 217）

`scripts/p16_criterion_correlation.py`，資料在
`runs/p16_criterion_correlation/correlation.csv`。

`docs/SURVEY.md` §8 把「同一批資料在三族判準下的結果如何互相換算」列為
沒有人回答過的問題。本專案的 `results.csv` 自 E2 起就把三族的欄位記在同一列，
材料是現成的。取無淨化那一列（乾淨攻擊）、依 `guidance_scale` 分組，
算等級相關（Spearman ρ）：

| guidance | n | ρ(距離, 語意) | ρ(距離, 劣化) | ρ(語意, 劣化) |
|---|---|---|---|---|
| 1.0（30 個 run） | 217 | **0.140** | **−0.207** | **0.014** |
| 7.5（3 個 run） | 3 | 1.000 | 0.500 | 0.500 |

w=7.5 那一列 **n = 3，太少，不可引用**——E29 之前的 run 都沒有 CFG，而
E29 只跑了三格含評測的。E31 的網格會補上 12 格。

w=1 那一組（n = 217）的三個數字是這一輪最直接的量化結果：

1. **ρ(距離, 語意) = 0.140。** 文獻主流的距離判準與「輸出服不服從 prompt」
   幾乎不相關。E25 的「726 格語意失敗 0 格」說的是第 2 族沒有正例；
   這個數字說的是更強的事——第 1 族的高分**不預測**第 2 族的高分。
2. **ρ(距離, 劣化) = −0.207，是負的。** 把輸出推得離良性編輯越遠，
   輸出本身反而**越不劣化**。這直接抵觸距離判準背後的預設
   （「離良性編輯越遠 = 防禦越成功」）。
3. **ρ(語意, 劣化) = 0.014。** ISR 的兩半彼此獨立。這是對「判準應取聯集」
   的支持——兩者若高度相關，取聯集就沒有意義。

`guidance_scale` 欄是 E26 才加的，缺欄的 33 個 run 一律推定為 1.0。
這不是猜測：E26 §3 確立 `src/models/sd.py` 在那之前全專案沒有
classifier-free guidance，等同 w=1。腳本會把推定的部分明確印出來。

**這一項的限制。** w=1 是一個編輯幾乎不發生的設定（E26），故上述相關性
描述的是「攻擊沒起作用時三族判準的關係」。它足以說明第 1 族與另兩族量的
不是同一件事，但不足以推論 w=7.5 下的關係。後者要等 E31 的網格。

---

## 7. 程式改動

四項，都記了 before/after 在對應檔案的 docstring。

| 檔案 | 改動 | 為什麼 |
|---|---|---|
| `src/defense/optimize.py` | `plateau_stop` 加 `monitor_key`；`optimize_crossattn` 接上平台停止 | 該路徑是唯一還跑固定步數的（E21–E23 §5.4 的死路）。但它的 `edit_shift` 恆為 NaN（無 y_orig 可比），直接接上會**永遠不停**——NaN 的比較恆為 False。改監看 `attn_div`，並讓缺鍵與 NaN 都拋出 |
| `src/metrics/suite.py` | `pairwise` 加 `rms` 與 `frac_gt_16_255` | 「本專案預算比文獻低 5–8 倍」只在 LPIPS 上成立，L∞ 反而是文獻的 6 倍。單一軸撐不起跨論文的預算比較 |
| `scripts/run_defense.py` | 新增 `--tau_acut`；修好 `--help` | 逐預算的門檻要能由命令列指定。`--help` 原本就是壞的：兩個 help 字串含未跳脫的 `%`，argparse 會拿去做格式化 |
| `data/targets/gray.png` | 新增 | `targeted` 模式的目標影像，與 PhotoGuard 的 encoder attack 一致 |

測試：**276 passed / 1 skipped / 0 failed**（基準 253 + 新增 23）。

---

## 8. 這一段確立的事

1. `results.csv` 的無參考品質欄位自 E2 起存在而從未被讀過。以 ISR 聯集判準
   重判 828 格，語意失敗 0 格；唯一有訊號的一格（ΔNIQE +1.571）因 n = 1 判不了。
2. **τ=0.28 在原約束集下不可達，且與防禦方法無關**——連 i.i.d. 白雜訊都超標。
   這個判定靠的是那個參照臂，沒有它會被誤讀成第四次「匹配失真是假的」。
3. 兩道次要門檻改為逐預算後 gate 可以過。定錨規則在 chroma 軸上獨立重現了
   E28 的人眼判定（0.802 對 0.8）。
4. acut 軸的分離度隨預算塌掉（0.28 時只剩 1.18 倍），原因是變形臂在大位移下
   本身就在鈍化。臂的歸屬不可跨預算外推，處置尚未決定。
5. 本機跑得動無梯度的 512² 推論，跑不動任何規模可用的訓練（256² 已 75 倍慢）。
   評測側整個留在本機，訓練側全部上雲端。

---

## 9. 沒有做的事與理由

- **雲端的 R1／R2 沒有跑。** 本階段的目的就是在燒雲端時間之前把設計問題
  找出來，而確實找到了兩個（τ=0.28 不可達、本機不能去風險）。
- **acut 軸的歸屬沒有改。** 那是實驗設計層級的變更，依專案規範須先討論。
  現行的較保守，gate 仍然過得了。
- **沒有補 MLLM 判準。** SIFM 報 MLLM 與人類的一致率 74%（人類彼此 76%），
  是比 NR 指標更可靠的作法，但需要外部模型。若感知劣化那一半成為結論的
  關鍵，值得補一次作為交叉驗證（見 `docs/SURVEY.md` §2）。
