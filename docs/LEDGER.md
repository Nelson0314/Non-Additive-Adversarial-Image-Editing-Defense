# 結論總帳

最後整理：2026-08-03（對齊基準論文之後）。

> **基準論文**：Lo, Yeo, Shuai, Cheng, *Distraction is All You Need:
> Memory-Efficient Image Immunization against Diffusion-Based Image Editing*,
> CVPR 2024。其約束（L∞ ≤ 0.06）、判準（Table 1 五指標）與 baseline
> （PhotoGuard 兩變體）已定為本專案的**必要對齊項**；本專案既有的語意判準、
> 感知劣化判準、LPIPS 綁定約束等一律保留為**改良項**。協定見
> `docs/specs/2026-08-03-lo-aligned-protocol.md`。

本檔是**主張**的索引，`docs/INDEX.md` 是**檔案**的索引，兩者互補：想知道
「某個結論現在還算不算數、出處在哪」查本檔；想知道「哪份文件現行、哪個 run
目錄屬於哪個實驗」查 INDEX。

收錄規則：

- 每一條都要有出處（文件 §節，必要時加 run 目錄）。沒有出處的判斷不入帳。
- **被推翻的條目不刪除**，移到 §7 並附上推翻它的資料。這是專案規範
  （`CLAUDE.md`：推翻自己先前的判斷時，把錯誤的假設與推翻它的資料一起留著）。
- 狀態只有三種：**現行**、**部分失效**、**已推翻**。

---

## 1. 量測方法：指標與判準

| # | 結論 | 出處 | 狀態 |
|---|---|---|---|
| 1.1 | `net_lpips`／`edit_shift` 不是防禦成功的判準。它量的是輸出移動了多少，不是編輯有沒有失敗 | E25 §1.2、E29 §4.3 | 現行 |
| 1.2 | CLIP 分不出「編輯有沒有發生」（edit_effect +0.0101 ± 0.0169，標準差大於均值），其 Δ 不可用於判定 | E25 §1.1 | 現行 |
| 1.3 | SigLIP 通過同一個 edit_effect 對照（+0.0276 ± 0.0237），可承擔語意軸的結論 | E25 §1.1 | 現行 |
| 1.4 | 語意失敗的判定需要 n ≥ 2。n = 1 時 sd 恆為 0，`\|mean\| > sd` 對任何負值自動成立，曾產生 24 格假陽性 | E25 §1.2 | 現行 |
| 1.5 | 判準應為 ISR 式的聯集：語意不符 prompt **或** 明顯的感知劣化。E25 之後只取了前半 | arXiv:2512.14320；E31 規格 §4 | 現行 |
| 1.6 | `results.csv` 自 E2 起就有 `edit_niqe_a`／`edit_niqe_b`，全專案從未讀過——與 E25 發現 `edit_siglip_*` 從未被讀過同型 | E31、`runs/p12_isr_rejudge/` | 現行 |
| 1.7 | 以 ISR 重判既有 828 格，語意失敗 0 格，與 E25 的 726 格 0 格一致 | `runs/p12_isr_rejudge/isr.csv` | 現行 |
| 1.8 | 指標之間矛盾時，把影像做成比對頁交人眼判斷。τ_acut、τ_chroma 兩個門檻與 E29 的否定結果都是這樣定/確認的 | E20 §7、E28 §2、E29 §4.1 | 現行 |
| 1.9 | 不得憑文獻聲譽選指標。ΔE00 是色差的國際標準，實測在等 LPIPS 下分不出加性雜訊與可見色偏 | E28 §1.1 | 現行 |
| 1.10 | **三類判準之間幾乎不相關**（w=1、n=217）：ρ(距離,語意)=0.140、ρ(距離,劣化)=**−0.207**、ρ(語意,劣化)=0.014。距離判準的高分不預測另兩類的高分，且與劣化**負相關**——推得越遠反而越不劣化 | `runs/p16_criterion_correlation/` | 現行 |
| 1.11 | ISR 兩半彼此獨立（ρ=0.014），這支持取聯集：兩者若高度相關，聯集就沒有意義 | 同上 | 現行 |
| 1.12 | **基準論文的判準是純第 1 類**：Lo et al. (CVPR 2024) Table 1 的 PSNR↓／SSIM↓／VIFp↓／FSIM↓／LPIPS↑ 五項全部量「免疫後編輯輸出 vs 原始編輯結果」的不相似度。沒有語意判準、沒有 CLIP、沒有 ISR、沒有使用者研究 | Lo et al. Table 1；新規格 §2.4 | 現行。**已定為本專案的必要判準** |
| 1.13 | `results.csv` 的 `edit_psnr`／`edit_ssim`／`edit_lpips` 自 E2 起就是 `suite.full(y_ref, y_def)` 的輸出，**定義與 Table 1 前三項完全一致**。缺的是 VIFp 與 FSIM，2026-08-03 補入 `suite.pairwise` | `src/metrics/suite.py`；新規格 §6 | 現行 |
| 1.14 | 本專案宣告 E29「全面為負」用的是語意軸，**那不是基準論文使用的判準**。同一批 run 在 Table 1 判準下有非零成績（見 3.11） | 新規格 §6 | 現行 |
| 1.15 | **語意軸挑得出「哪一個攻擊是為了破壞 prompt 服從度而設計的」，Table 1 的距離軸挑不出來。** L3 在 `runs/lo_baseline/` 的 72 格（24 張 × 3 攻擊 × 20 種子）上判定語意失敗：`semantic` 6/24、`pg_encoder` 1/24、`pg_diffusion` 0/24。`semantic` 是三者中唯一以 cross-attention 綁定為目標的，兩個 PhotoGuard 變體 的損失分別作用在 VAE latent 與輸出距離上，與 prompt 服從度無關——**語意軸的判定與各攻擊的作用機制完全一致**。而 Table 1 把 `semantic` 排最後（編輯 LPIPS 0.5593，對 0.6363／0.6070）。<br>**依 1.22 只留未防禦編輯明顯成功的 18 張之後，每一項都更乾淨**：`semantic` 的 Δsiglip 平均由 +0.00010 轉為 **−0.01239**（即它確實降低 prompt 服從度），語意失敗仍是 6 格（6/18 = 33%）；兩個 PhotoGuard 變體 的 Δsiglip 仍為正（+0.0066、+0.0091），語意失敗 1/18 與 0/18。**過濾之後結論變強而非變弱，這本身是該過濾為真的旁證** | `runs/l3_criterion_axes/` | 現行。**這是本專案最強的判準論證**：它完全在基準論文自己的協定、預算與方法上取得，不依賴本專案的方法有沒有效 |
| 1.16 | **在有效威脅模型下，三類判準的不一致比 w = 1 時更嚴重，且不一致的方向逐攻擊而異。** 約定：第 1 類成功 = 距離高、第 2 類成功 = Δsiglip 負、第 3 類成功 = Δniqe 正，故 1↔2 一致為 ρ 負、1↔3 一致為 ρ 正。逐種子（n = 480/攻擊）：`pg_encoder` ρ(距離,語意) = **−0.2814**（兩類一致）；`semantic` ρ(距離,劣化) = **−0.3496**（兩類**方向相反**——輸出被推得越遠反而越不劣化）；`pg_diffusion` 三個 ρ 全部接近 0。取代 1.10 的 ρ = −0.207（那是 w = 1、n = 217）。<br>**依 1.22 過濾到 18 張之後（n = 360/攻擊），第 1 類與第 3 類的不一致變成三個攻擊一致成立**：ρ(距離,劣化) 為 `semantic` −0.3570、`pg_diffusion` **−0.2164**（原 −0.0588）、`pg_encoder` **−0.1243**（原 −0.0199）。即**輸出被推得越遠反而越不劣化，這個反向關係在三個方法上都成立** | 同上；`runs/l3_criterion_axes_working/` | 現行 |
| 1.17 | **ISR 這個聯集判準在實務上由劣化那一半主導，而劣化那一半可以只靠模糊取得。** τ_degrade = 1.0 下：`pg_encoder` 劣化失敗 23/24、語意失敗 1/24；`pg_diffusion` 11/24 與 0/24；`semantic` 5/24 與 6/24。`pg_encoder` 的 Δniqe 平均是 **+5.81**，對 `pg_diffusion` 的 +0.73 與 `semantic` 的 **−0.40**。即以 ISR 計 `pg_encoder` 的「免疫成功率」達 96%，而那 96% 幾乎全部是「輸出看起來很糟」，不是「編輯被擋下來」。文獻回報的 ISR 79–97%（SIFM）與人類偏好勝率 41.9%（DiffusionGuard）之間的落差，這裡有一個具體的機制解釋 | 同上 | 現行。門檻 τ_degrade 尚未由人眼判讀（9.4），故本條報的是**逐門檻都成立的型態**：τ = 0.0/0.5/1.0/2.0/4.0 下三個攻擊的劣化失敗數為 `pg_encoder` 23/23/23/23/21、`pg_diffusion` 14/14/11/4/0、`semantic` 7/7/5/0/0。門檻怎麼定都是同一個排序，且 τ ≥ 2.0 時只剩 `pg_encoder` 有分數 |
| 1.18 | **同一個失真上的隨機擾動就能取得可觀的語意失效，而文獻沒有人報這個對照。** 2026-08-04 實測（horse_00、256²、`suppress`、2 步）：最佳化解在擾動 LPIPS 0.1385 上 Δsiglip = −0.0309 ± 0.0029，而**把高斯雜訊二分搜尋到同一個 LPIPS（0.1409）得到 −0.0186 ± 0.0053，依 E25 的規則同樣判為語意失敗**。即隨機方向就拿到 60% 的效果。這與 7.1（`e2_phi0` 推翻 site L）是同一課：**沒有同失真的未最佳化對照，任何正結果都不可解讀**。<br>**2026-08-04 更正**：本條原本寫「查過的四／五篇都沒有這個對照」，那是**錯的**——PhotoGuard 的 Table 6 就有一列 `Immunization baseline (Random noise)`。真正的差別在**匹配的軸不同**，見 1.24 | `runs/_smoke_gate2/summary.csv`；`scripts/gate_suppress.py::random_control` | 現行（已更正）。**2 步、1 張影像、5 個評測種子的初步值**，正式數字由 `runs/gate_suppress/` 取代 |
| 1.19 | 1.18 使閘門的問句必須改寫。原本問「語意軸會不會動」，正確的問法是**「在同一個可辨失真上，最佳化找到的方向比隨機擾動多取得多少語意失效」**——因為前者的答案已經是「會動，但隨機也會」。改寫後的問句與研究問題本身同構（匹配失真下的兩個條件比較），故閘門與正式實驗共用同一個判讀方式 | `docs/CONVERGENCE.md` §3 | 現行 |
| 1.20 | **在匹配失真上，隨機擾動在 Table 1 的距離判準上勝過最佳化的注意力防禦，在語意軸上只差 26%，而劣化軸上差 6 倍。** 閘門實測（horse_00、site PF、`suppress`、60 步、τ_lpips = 0.55、256²、5 個評測種子）：<br>　　最佳化：擾動 LPIPS 0.5352、**Δsiglip −0.0567 ± 0.0267**、Δniqe +0.44 ± 0.61、編輯 LPIPS **0.5299**<br>　　隨機（同 LPIPS 0.5314）：**Δsiglip −0.0419 ± 0.0250**、Δniqe **+2.71 ± 1.59**、編輯 LPIPS **0.5465**<br>三個判準給三種答案：距離軸判**隨機較好**、語意軸判最佳化較好但 n = 5 下兩者的標準差完全重疊、劣化軸判隨機「較好」六倍。最佳化取得的是**沒有劣化的語意失效**，隨機取得的是**劣化** | `runs/gate_suppress/summary.csv` | 現行。**n = 1 影像、5 個評測種子、256²**。horse_01 那一格被中止 |
| 1.21 | 1.20 給「本專案的 `suppress` 與基準論文的 semantic attack 有四處不同」（5.7）一個量化的落差：同一張 horse_00 上，L1 的 `semantic` 在 κ（擾動 LPIPS ≈ 0.51）取得 Δsiglip **−0.0722**，而本專案的 `suppress` 在略高的 0.5352 上只有 **−0.0567**。即論文的版本較強，方向與 5.7 列的四處差異一致（空間遮罩、跨層相加保留空間維度、L1 聚合、以 c_a 而非攻擊方 prompt 為對象） | `runs/l3_criterion_axes/cells.csv`；`runs/gate_suppress/summary.csv` | 現行。單張影像，且兩者的解析度不同（512² 對 256²） |
| 1.22 | **24 張裡有 6 張的未防禦編輯不算成功**（`edit_effect = SigLIP(y_ref, prompt) − SigLIP(x, prompt)` 未超過 E25 量到的標準差 0.0237）：`dog_03` 為 **−0.0007**（編輯反而更遠離 prompt）、`man_02` +0.0035、`cat_01` +0.0091、`dog_02` +0.0102、`woman_00` +0.0112、`bird_03` +0.0124。**在這些影像上量免疫效果沒有意義——防的是一個不會發生的事。** Attention Attack（ACM MM 2025）建資料集時明說要先過濾掉它們，本專案沒有做這道過濾 | `runs/l3_criterion_axes/edit_success.csv`；`scripts/l3_edit_success.py` | 現行。全 24 張的平均編輯效果為 +0.0415 ± 0.0234，優於 E25 的對照值 +0.0276 |
| 1.23 | **兩個條件共用同一組評測噪聲 ε 使比較是配對的，所需樣本數因此少約 7 倍。** 閘門的 5 個評測種子上，逐種子的配對差（最佳化 − 隨機）為 −0.0067／−0.0283／+0.0056／−0.0235／−0.0214，**平均 −0.01487、樣本標準差 0.01400、Cohen d = −1.06**（大效果），5 個種子中 4 個favour最佳化。若不配對而用各條件的標準差（≈0.026）估，需要約 48 個量測；配對之後**α = 0.05 雙尾、power 80% 只需 n ≈ 7，power 90% 需 n ≈ 10**。本專案的評測路徑本來就以結構保證共用 ε（`reference_edits` 把 noise 一起帶回），故這個增益是免費的 | `runs/gate_suppress/results.csv` | 現行。**這是規劃 L4′ 樣本數的直接依據** |
| 1.24 | **PhotoGuard 的隨機對照匹配的是振幅，本專案匹配的是感知失真，兩者的結論因此相反而不矛盾。** PhotoGuard 原文：「a naive immunization method that adds uniform random noise (**of the same intensity** as the perturbations used in our proposed immunization method)⋯**is not effective** at disrupting the SDM, and yields edits **almost identical** to those of non immunized images」，Table 6 的數字是隨機 SSIM 0.75／PSNR 19.21，對 encoder 0.58／14.91、diffusion 0.50／13.58。<br>本專案的對照把高斯雜訊**二分搜尋到與最佳化解相同的 LPIPS**，得到的結論是隨機取得 60–74% 的語意失效（1.18、1.20）。<br>**兩者一致**：2.19 已量到同一張影像上 PGD 解的 LPIPS 是 0.2935，而同振幅隨機 sign 只有 0.1497、同 RMS 高斯只有 0.1084——**匹配振幅時隨機的感知失真只有最佳化解的 1/2 到 1/3**。PhotoGuard 的隨機條件因此是在一個小得多的可辨失真上比較。**這正是本專案主張「匹配軸應該是人眼可辨失真而不是 L∞」的直接證據，而且證據來自 baseline 自己的實驗** | PhotoGuard（arXiv:2302.06588）Table 6 與 §4；`runs/p17_kappa_visibility/`；`runs/gate_suppress/` | 現行 |
| 1.25 | **「匹配失真」第四次被證明是假的，而且這一次兩個條件連參數化都相同。** 前三次是 site S 換到的是模糊、site C 換到的是色調偏移兩次（2.8）。這一次：`runs/gate_suppress/horse_00__PF__def.png`（最佳化，LPIPS 0.5352）與 `horse_00__PF__rand.png`（隨機高斯，LPIPS 0.5314）**是同一個 site、同一個像素加性參數化、同一個 LPIPS**，逐圖看卻是兩種東西——最佳化的是**沿內容走的漩渦狀紋理**（像被風格化），隨機的是**均勻高頻顆粒**（像雜訊嚴重的照片）。無參考品質指標與人眼一致：Δniqe **+0.44 對 +2.71**，相差 6 倍 | `runs/gate_suppress/`（三張 PNG 可直接比對） | 現行。單張影像 |
| 1.26 | **1.25 使 1.20 的讀法必須修正，而且方向對本專案有利。** 在同一個 LPIPS 上，最佳化條件的語意失效比隨機多 35%（−0.0567 對 −0.0419），**而它在劣化軸上的代價只有隨機的 1/6**（Δniqe +0.44 對 +2.71）。即**每單位可辨劣化所換得的語意失效，最佳化遠高於隨機**。「匹配 LPIPS」因此**低估**了最佳化的優勢——兩個條件的 LPIPS 相同但總可辨代價不同 | 同上 | 現行。**直接的設計後果**：隨機對照條件應與最佳化條件受**同一組**約束（三道 hinge），否則比較的是「三道約束下的最佳化」對「一道約束下的隨機」 |

## 2. 保真約束

| # | 結論 | 出處 | 狀態 |
|---|---|---|---|
| 2.1 | 用單一純量定義「匹配失真」，在失真種類不同的兩類之間不成立 | E20 §0 | 現行 |
| 2.2 | 等 LPIPS 多條件探針是判別「一個指標實際在收什麼費」的可用方法 | E20 §5.3、E28 §1 | 現行 |
| 2.3 | `local_acutance_dev` 對位移不敏感（位移把梯度在區塊內搬動、不搬出區塊），且先取絕對值再加權，無法被「一處變鈍、他處變銳」抵銷 | E20 §6 | 現行 |
| 2.4 | `local_chroma_bias` 分得出空間連貫的色偏與隨機色度雜訊；τ = 0.8 的人眼判讀準確 | E28 §1、E29 §4.1 | 現行 |
| 2.5 | NLPD、VIF、GMSD、HaarPSI、SSIM、DISTS、PSNR 量的是幾何位移不是鈍化，不可作為保真約束 | E20 §5.2 | 現行 |
| 2.6 | SSIM 把雜訊判得比等 LPIPS 的模糊貴約 2 倍，即先前的保真項在**補貼**模糊 | E20 §5.2 | 現行 |
| 2.7 | 三道約束（LPIPS、acut、chroma）彼此不循環，各擋一種失效 | E28 §3 | 現行 |
| 2.8 | 「匹配失真」已三次被證明是假的：site S 換到的是模糊、site C 換到的是色調偏移。每引入一個新參數化都要先用等 LPIPS 探針量現行約束對它收不收費 | E20、E27 §4、E28 | 現行 |
| 2.9 | τ_acut = 0.04 與 τ_chroma = 0.8 是**絕對值**且定在 τ_lpips = 0.05 的量級，把可達的 LPIPS 封在 0.15–0.20。i.i.d. 白高斯雜訊在 LPIPS 0.20 的 acut 已達 0.0414、0.28 時 0.0875 與 chroma 1.1284 | `runs/p13_budget_probe/`；E31 規格 §12 | 現行 |
| 2.10 | 兩道次要門檻的職責是相對的（擋住以模糊換分與以色偏換分），故門檻應隨預算而定 | E31 規格 §12.2 | 現行 |
| 2.11 | 以「既有人眼判讀定出的門檻值在對數區間上的位置」外推，chroma 軸獨立重現了 E28 的 0.8（規則給 0.802） | `runs/p14_budget_thresholds/`；E31 規格 §12.3 | 現行 |
| 2.12 | 逐預算門檻定出後 gate 可以過：τ=0.28 的門檻是 acut 0.1460、chroma 3.6653（六張影像），而 site P 的解是 0.1302 與 2.8275 | E31 規格 §12.4 | 現行 |
| 2.13 | **acut 軸的分離度隨預算塌掉**：0.05 時擋下側是通過側的 5.12 倍，0.28 時只剩 1.39 倍。機制是加性雜訊的 acut 隨振幅陡升（14 倍）而幾何變形飽和（1.28 倍） | `runs/p14_budget_thresholds/separation.csv` | 現行 |
| 2.14 | 條件的歸屬是在 τ=0.05 上由人眼判定的（0.4–0.6 px 的位移），**不可跨預算外推**。chroma 軸沒有這個問題（各歸屬下比值都是 11.2–18.1） | 同上 | 現行 |
| 2.15 | **基準論文只有一道約束：L∞ ≤ κ，κ = 0.06、N = 100，所有方法一致**。沒有 LPIPS 約束、沒有鈍化約束、沒有色度約束——匹配失真是靠共用同一個球來執行的 | Lo et al. §4.1；新規格 §2.3 | 現行。**已定為本專案的必要約束** |
| 2.16 | **L∞ 對非加性方法量不出可辨失真**：`e15_S_tau0.05`（空間變形）的 L∞ 是 0.5654，即 κ 的 9.4 倍，而實際位移不到一個像素。這是本專案 LPIPS 綁定約束的存在理由，也是「匹配人眼可辨失真」這個研究問題的立足點 | `runs/e15_S_tau0.05/`；新規格 §3.3 | 現行 |
| 2.17 | 兩套約束不可混在同一張表裡：L∞ 球是**必要的對照條件**（加性之間可比），LPIPS 綁定是**貢獻條件**（含非加性時才可比）。`report_table1.py` 強制併列 `L∞` 與 `×κ` 兩欄 | 新規格 §3.3 | 現行 |
| 2.18 | **κ = 0.06 對應 LPIPS ≈ 0.58、PSNR 27.1 dB、acut 0.4994**。即基準論文的失真預算是本專案跑過的最大 τ_lpips（0.10）的 5.8 倍、文獻回報 LPIPS 運作點（0.267–0.362）的近 2 倍 | `runs/p17_kappa_visibility/` | 現行。**單一影像、由 2 步解沿射線放大**，方向明確但數值待 L1 的真實解確認 |
| 2.19 | **對抗性結構本身有感知代價**：同一張影像上 PGD 解的 LPIPS 0.2935，同振幅隨機 sign 只有 0.1497、同 RMS 高斯雜訊只有 0.1084。即最佳化找出的方向比無結構擾動貴 2–3 倍 | 同上 | 現行 |
| 2.20 | 2.18 使 3.11（我們在 κ 上落後基準）多一個可能來源：**我們從未在接近 LPIPS 0.58 的預算上跑過任何東西**。同時也使「匹配人眼可辨失真」這個研究問題更站得住——領域慣用的 L∞ 預算對應到的失真不是不可見的 | 新規格 §4.2 | **部分失效**：前半隨 3.11 一起被 7.9 推翻（我們的加性實作沒有落後）。後半仍現行，且與 2.21 的人眼判讀互相支持 |
| 2.21 | **人眼判讀（使用者，2026-08-03）：κ = 0.06 是可見的。** 原話「最右邊那張算是一眼看出背景扭曲 但不會到令人很不舒服／倒數第二張比較可以接受」。即 L∞ 0.060（LPIPS 0.58）一眼可辨，L∞ 0.045（LPIPS 0.53）較可接受。對抗擾動文獻普遍把 L∞ 球描述為 imperceptible，該描述在此不成立 | `runs/p17_kappa_visibility/ladder.png`；新規格 §4.2.1 | 現行。單一影像、由 2 步解沿射線放大，L1 後須重做 |
| 2.22 | 2.21 是本專案第一個在 **L∞ 軸**上的人眼判讀；先前的兩個（τ_acut = 0.04、τ_chroma = 0.8）都定在 LPIPS 0.05 的量級。它不改動 L1 的設定（κ 是論文寫死的比較條件），用途在 L4 的匹配失真比較 | 同上 | 現行 |

## 3. 攻擊端與威脅模型

| # | 結論 | 出處 | 狀態 |
|---|---|---|---|
| 3.1 | 攻擊端必須有 classifier-free guidance。w = 1 下 SD v1.4 幾乎不服從 prompt，該設定下的「編輯」與「什麼都沒做」在指標上分不出來 | E26 §3 | 現行 |
| 3.2 | E2–E23 全部跑在 w = 1，即在防禦一個不存在的攻擊。那批的每一個 `net_lpips` 因此失效 | E26 | 現行 |
| 3.3 | 在 τ ≤ 0.10 的失真預算下，`untargeted` 目標對 stock SD v1.4 的 SDEdit（w=7.5、strength=0.5）沒有防禦效果，加性與非加性皆然 | E29 §7 | 現行，**但適用範圍須限縮**：該判定用的是語意軸，不是基準論文的判準（見 1.14、3.11） |
| 3.4 | 該否定結果已被獨立複現：ICIP 2025（arXiv:2506.04394）量到受保護影像編輯後對 prompt 的對齊度**沒有下降**（Actual Change ≥ 0）的比例，跨兩張表與兩個指標為 **54.12–77.54%** | 原文逐項核對（2026-08-04），見 SURVEY §1 | 現行。**先前的引用有誤**：寫成「測 PhotoGuard／Mist／Glaze，61.82–67.79%」把兩張表混為一談——61.82–67.79% 只是 Table 1 的 PAC-S++ 一列且只測 PhotoGuard，同表的 CLIP-S 是 54.23–61.54%；Mist 與 Glaze 在 Table 2（風格化，非影像編輯） |
| 3.5 | 文獻的標準失真預算是 ε = 16/255 的 L∞ 球；PhotoGuard 的 SDEdit 評測用 strength 0.2／0.3，本專案用 0.5 | E31 規格 §2.4 | 現行 |
| 3.6 | 「本專案預算比文獻低 5–8 倍」只在 LPIPS 上成立。RMS 約差 2 倍，**L∞ 反而是文獻的 6 倍**——擾動是稀疏尖峰型，5.3% 的像素超過該球而中位數只有 5/255 | E31 規格 §3 | 現行 |
| 3.7 | **降低 strength 沒有幫到加性防禦，反而相反**：site P 的 Δsiglip 在 0.2/0.3/0.4/0.5 上都是正的（防禦後更服從 prompt），且在文獻慣用的 0.3 上最大（+0.0524，是 0.5 下 +0.0049 的十倍）。**遷移設定、n=1，為趨勢不是結論** | `runs/e31_strength_sweep/` | 現行 |
| 3.8 | 未防禦編輯的 prompt 服從度隨 strength 單調上升（0.0587→0.1090）。低 strength 下語意不符較易達成，但威脅本身也較弱 | 同上 | 現行 |
| 3.9 | 在**單一影像**（car_00）的強度掃描上，site P 只在劣化那一半有訊號（Δniqe +0.57～+0.96）、site C 只在語意那一半（Δsiglip −0.012～−0.016）。**但 p15 顯示該型態跨影像不成立**：`e29_C_lr0.3` 的 Δsiglip 在 car_00 是 +0.0074、car_01 是 −0.0678，符號相反 | `runs/e31_strength_sweep/`、`runs/p15_editpage_degrade/per_image.csv` | 現行（已限縮）。**且兩個條件不在匹配失真上**：site P 的 LPIPS 0.0856、site C 0.0386，跨條件不可比 |
| 3.10 | **n ≥ 2 規則的具體案例**：`e29_C_lr0.3` 兩張圖的 Δsiglip 平均 −0.0302、標準差 0.0376，故正確地判為非語意失敗。只看 car_01（−0.0678）會誤判成強烈的語意失敗 | `runs/p15_editpage_degrade/` | 現行 |
| 3.11 | **在基準論文的預算（L∞ ≤ 0.06）上，本專案的加性實作落後 PhotoGuard**。落在 κ 上的 `e8_rank_tau0.05`（L∞ 0.0606）是 LPIPS 0.3466／PSNR 24.96，對 PhotoGuard diffusion 的 0.4056／18.26，PSNR 差 6.7 dB；距 semantic attack 的 0.5901／15.15 更遠。**第一層（重現）尚未通過** | `docs/RESULTS_TABLE1.md`；新規格 §6 | **已推翻，見 7.9。** 現行的判定是 3.15（兩個 PhotoGuard 變體 已重現）與 3.16（semantic 未重現，協定只跑了一半） |
| 3.12 | `e29c_P_tau0.10` 在 Table 1 三指標上（16.55／0.5288／0.4735）優於兩個 PhotoGuard baseline，**但用掉 6.2 倍的 L∞ 預算，不構成對照** | 同上 | 現行 |
| 3.13 | 基準論文**未公布 strength 與 guidance_scale**，本文與補充材料都沒有。本專案依 3.1 一律用 w = 7.5 | Lo et al.；補充材料 §A | 現行 |
| 3.14 | 基準論文的評測是 **150 張生成影像 × 20 個隨機種子平均**，本專案既有 run 全部是單種子。這是落差來源清單的第一項 | 補充材料 §A；新規格 §6 | 現行 |
| 3.15 | **兩個 PhotoGuard 變體 基準方法已重現**。同一 κ = 0.06、N = 100、24 張真實照片 × 20 種子：pg_encoder 18.24／0.5518／0.2857／0.6903／0.6363（論文 18.84／0.6318／0.2118／0.7757／0.4131），五項中四項優於論文；pg_diffusion 18.81／0.5351／0.0959／0.7784／0.6070（論文 18.26／0.6504／0.2656／0.7693／0.4056），三項明顯優。**基準實作不是弱化版**，可作為後續比較對象 | `runs/lo_baseline/`；`docs/RESULTS_TABLE1.md` | 現行 |
| 3.16 | **semantic attack 未重現**：20.23／0.5473／0.0955／0.7854／0.5593，對論文的 15.15／0.4470／0.1462／0.6584／0.5901，五項中四項劣。式 (3)(4)(5) 的解讀、PGD 符號、損失收斂（下降 87%）已逐項驗過。原因是**協定只跑了一半**——補充材料 §A 每個物件有兩個編輯 prompt 且 Table 1 兩者一起平均，`runs/lo_baseline/` 只跑 `--prompt_index 0`，即 c_a 不出現在攻擊方 prompt 裡的那半，正文 §4.3 把該情況列為額外優點即承認它較難。兩個 PhotoGuard 變體 不使用 c_a，故不受影響 | `runs/lo_baseline/`；commit `e05ff02` | 現行。使用者決定不追，semantic 改引用論文原數據並標註僅供參考。<br>**2026-08-04 排除掉一個候選解釋**：依 1.22 只留未防禦編輯明顯成功的 18 張後重算，三個攻擊的 Table 1 五項全部只動了 0.2 dB／0.01 的量級（`semantic` 由 20.23/0.5473/0.0955/0.7854/0.5593 變成 20.46/0.5403/0.0888/0.7832/0.5558）。**「資料集裡有編輯本來就失敗的影像」不是 semantic 未重現的原因。**<br>另一個值得記的讀法：**`semantic` 的 LPIPS 其實接近重現**（0.5558 對論文 0.5901，達 94%），落差集中在 PSNR（20.46 對 15.15，差 5.3 dB）與 FSIM。即「五項中四項劣」掩蓋了「論文最強調的那一項幾乎追平」 | 同上 |
| 3.17 | **本專案的方法從未在有效威脅模型下大規模量過**。掃過全部 `runs/*/env.json`，`guidance_scale ≥ 2` 的只有 `e29c_P_tau0.10`、`e29c_C_tau0.10`、`e27_evaltiming` 三個，且各為單張影像、單種子。3.11 拿 `e8_rank_tau0.05` 去比論文因此無效——該批屬於 3.2 判定作廢的 w = 1 資料 | 全庫 `env.json` 掃描（2026-08-03） | 現行。**推翻 3.11 的比較，見 7.9** |
| 3.18 | 在**同一份資料、同一條評測路徑**（24 張、strength 0.3、w = 7.5、20 種子）上，site PF 綁在 τ_lpips = 0.10 收斂到擾動 LPIPS 0.0992，編輯 LPIPS 0.2490／PSNR 31.59；同一評測下 pg_encoder 是擾動 LPIPS 0.5514、編輯 LPIPS 0.6363。即**用 1/5.4 的可辨失真取得 0.41 倍的效果**。失真未匹配，不是勝負判定 | `runs/ours_lo/`；`runs/lo_baseline/` | 現行但須加註。n = 1 影像（man_00），2026-08-03 機器時間用盡。**該格不是純粹由 LPIPS 綁住的**：鈍化 hinge 在 48 步中啟動 31 步，而其門檻 0.04 依 6.17 是 τ_lpips = 0.05 的值、該預算應為 0.0598。另一個收斂的格是 man_03（122 步，編輯 LPIPS 0.2524） |
| 3.19 | 同一設定下的非加性位置（site S、bicubic、grid 32）為編輯 LPIPS 0.0414，比 PF 弱約六倍，且**只用掉 16% 的 LPIPS 預算就跑滿 150 步上限**（未收斂，依 6.4 不可用於跨 site 比較）。綁住它的不是 LPIPS 而是鈍化約束 | `runs/ours_lo/` | **已推翻，見 7.10。** 數字（0.0414、16%、跑滿上限）為真，「綁住它的是鈍化約束」為假——三道 hinge 一次都沒啟動 |
| 3.20 | **看圖之後的修正：τ_lpips = 0.10 下兩個位置都沒有真正阻止編輯。** `runs/figs/2026-08-03_l4_ours_vs_baseline.png`：man_00 的編輯 prompt 是「a woman」，未防禦編輯把人變成女性，而 PF 與 S 的防禦後編輯**同樣是女性、與未防禦結果幾乎一樣**；man_01、man_02 同。相對地 pg_encoder 的防禦後編輯整張糊到無法辨識。編輯 LPIPS 0.2593（PF）聽起來像「有部分效果」，肉眼是「編輯照樣成功，只是細節略有不同」——**該指標在此高估了防禦效果**，判讀必須配圖 | `runs/figs/2026-08-03_l4_ours_vs_baseline.png`；`runs/ours_lo/` | 現行 |
| 3.21 | 3.20 與 3.3 一致並把其適用範圍擴大：3.3 原本只說 `untargeted` 在語意軸上無效，現在在**基準論文的距離軸**上、對 site PF 與 site S、在同一份資料與同一條評測路徑下，同樣是無效。差別在 3.3 是 n = 1 單種子，本條是 n = 3–4 影像 × 20 種子並有圖佐證 | 同上 | 現行 |
| 3.22 | 保真側是成功的：PF 的防禦圖在 1:1 下與原圖幾乎無法分辨（擾動 LPIPS 0.0928），而 pg_encoder 的免疫圖有明顯雜訊（0.5123）。**問題不在保真約束太緊，而在該預算下的防禦目標函數做不出效果** | 同上 | 現行 |
| 3.23 | **L4 的非加性位置沒有任何一格可用。** site S 的三格全部跑滿 150 步上限，而依 6.18 那不是收斂的判定：三道 hinge 在 450 個訓練步裡**一次都沒有啟動**（`fid_pen_lpips`／`fid_pen_acut`／`fid_pen_chroma` 全為 0/150），末端 LPIPS 只有 0.0163–0.0250，即預算 0.10 的 16–25%。原因由 6.16 給出：該條件跑在其校準學習率的 1/12.5。**L4 目前只有 site PF 的兩格（man_00 48 步、man_03 122 步）是可用的量測，非加性側是零** | `runs/ours_lo/*__history.json`；`docs/RESULTS_TABLE1.md` 的逐格表 | 現行 |
| 3.24 | **Table 1 的距離判準分不出「編輯被擋下來」與「輸出被毀掉」，而三個基準方法恰好落在該區分的不同位置。** 逐檔判讀（六類各一張）：`pg_encoder` 一律產生低頻的模糊褪色、細節全失，多數列已無從判斷編輯有沒有成功（`dog_00__pg_encoder_edit_def.png` 是看得出擋下的一張：prompt「a cat」而輸出仍是模糊的狗）；`pg_diffusion` 保留結構、疊上雜訊與重影，逐張看多半沒擋住（`cat_00` → 狗、`horse_00` → 有條紋的斑馬）；`semantic` 保留構圖、產生色帶撕裂，`man_00__semantic_edit_def.png` 是清楚擋下語意改變的一張（未防禦編輯是塗口紅盤髮的女性，防禦後仍是短髮男性）。該判準把 `pg_encoder` 排第一（編輯 LPIPS 0.6363）、`semantic` 排最後（0.5593），而前者的高分是用全域由模糊換來的 | `runs/figs/2026-08-04_l1_three_attacks.png`、`runs/figs/compare.html` | 現行。**單一評測種子的圖**（`_edit_def.png` 存的是第一個種子），指標是 20 種子平均，兩者的粒度不同 |
| 3.25 | **保護反而讓編輯成功，有兩張逐檔可查的直接證據。** `woman_00__pg_diffusion_edit_def.png`：prompt 是「a man」，**未防禦編輯仍是女性（編輯本來失敗）**，而免疫後的編輯是短髮有鬍髭的男性——編輯因為有保護才成功。`bird_00__pg_diffusion_edit_def.png`：prompt「a butterfly」，未防禦仍是鳥，免疫後是整隻蝴蝶。方向與 3.4 一致（ICIP 2025 量到 61.82–67.79% 的受保護影像更服從 prompt），差別在本專案是在**基準論文自己的協定與預算上**、且有影像而非只有分數 | `runs/lo_baseline/`；E31 規格 §2.1 | 現行。單一評測種子 |
| 3.26 | **本專案在基準論文自己的協定上獨立複現了 3.4 的現象，且證據比原文更直接。** `runs/l3_criterion_axes/`：`pg_diffusion` 21/24 格、`pg_encoder` 16/24 格的 Δsiglip 為正，兩者的平均都是 **+0.0203**。對照 E25 定下的 edit_effect 基準（「有編輯」對「什麼都沒做」為 +0.0276 ± 0.0237），即「保護反而幫了攻擊方」的效果量約是「有沒有編輯」本身的 **74%**。ICIP 用的是跨資料集的百分比，本專案是同一份資料、同一組評測種子、逐元素相同的 ε，且在 κ = 0.06 這個文獻標準預算上 | `runs/l3_criterion_axes/axes_by_attack.csv` | 現行 |
| 3.27 | **本專案的兩個位置與兩個 PhotoGuard 變體 在判準上的側寫相反。** 把 L3 的三類分解套到 `runs/ours_lo/`（7 格 × 20 種子）：site PF 的 Δsiglip **−0.00463**、site S **−0.00146**，**兩者皆為負**（即免疫後的編輯較不服從 prompt），而兩個 PhotoGuard 變體 是 +0.0066 與 +0.0091（皆為正，即更服從）。劣化軸上本專案是 **−0.095／−0.065**（幾乎無劣化），`pg_encoder` 是 **+5.75**。<br>即：**PhotoGuard 用大量劣化換到一個方向錯誤的語意效果；本專案用幾乎零劣化換到一個方向正確但很小的語意效果**（|Δsiglip| 只有 E25 對照標準差 0.0237 的 1/5）。這與 3.22（保真側成功、防禦側無效）一致，並補上了語意軸的符號 | `runs/l3_ours_lo/` | 現行。**描述性質**：L4 的 7 格中 5 格跑滿步數上限、site S 的 lr 錯了（6.16），不可用於效果量的結論 |
| 3.28 | **site S 停在 LPIPS 0.108 不是因為任何一道約束，而是目標函數推不動它。** 閘門（horse_00、`suppress`、60 步、τ_lpips = 0.55、lr = 0.1 即其校準值、位移上界放寬到 8.0 px）逐項排除：LPIPS 0.1077 遠低於預算 0.55；**位移統計為平均 1.217 px、最大 5.386 px、p99 3.619 px，上界 8.0 沒有碰到**；acut 與 chroma 兩道 hinge 係數設為 0。四個候選有效約束全部排除之後，剩下的是**最佳化本身停了**——`attn_div` 由 0.98470 走到 0.99004（+0.0053），而同一個目標在 site PF 上走到 +0.0095。<br>**即：平滑位移場對 cross-attention 的擾動能力有上限，而那個上限與失真預算無關。** 這是本專案第一次把 site S 的弱勢歸因到參數化本身而不是設定 | `runs/gate_S/`；`runs/logs/gate_S.log` | 現行。**單張影像、單一目標函數**。位移最大值 5.386 px 也回頭確認了 L4 用的 1.5 px 上界**確實會綁住**（3.23 的另一個成因） |
| 3.32 | **在同一個可辨失真上、以同失真隨機為對照，非加性位置的最佳化沒有貢獻——甚至比隨機差。** 本專案第一次對研究問題取得結構正確的量測（horse_00、`suppress`、60 步、256²、5 個評測種子，兩個條件共用逐元素相同的 ε）：<br><br>　　site PF：Δsiglip(opt) −0.05674、Δsiglip(rand) −0.04187、**優勢 −0.01487**、Cohen **d = −1.06**、5 個種子 **4 個同向**<br>　　site S：Δsiglip(opt) −0.00212、Δsiglip(rand) −0.00956、**優勢 +0.00744**（正值＝最佳化較差）、d = +0.234、5 個種子 **只有 1 個同向**<br><br>「優勢」= Δsiglip(最佳化) − Δsiglip(同 LPIPS 的隨機)，**已對各 site 自己達到的失真正規化**，故兩者停在 0.535 與 0.108 仍可比（1.19 的作法）。**加性位置的最佳化取得了增益，非加性位置沒有。** | `runs/gate_compare.csv`；`runs/gate_suppress/`、`runs/gate_S/` | 現行。**n = 1 影像、5 個評測種子、grid_size = 32、單一目標函數。** 容量軸從未掃過（3.29），故這是「grid 32 下的答案」不是「非加性的答案」 |
| 3.33 | **容量軸有效：`grid_size` 128 在第 10 步就達到 grid 32 第 40 步的 `attn_div`，LPIPS 是後者的 1.9 倍。** grid 32：step 10 div 0.98756 / lpips 0.0414，step 40 div 0.98952 / lpips 0.0932。grid 128：step 10 div **0.98953** / lpips **0.0789**。**這直接支持 3.28 的歸因**——site S 在 grid 32 的上限來自控制點密度，不是最佳化器、不是失真預算、不是 lr | `runs/logs/gate_S.log`、`runs/logs/gate_S_g128.log` | 現行。**訓練軌跡不是最終判準**，優勢要看 `gate_compare.py`；此列只釘住「容量參數確實會動」 |
| 3.34 | **容量不改變起點梯度，只改變軌跡。** grid 32 / grid 128 的 step-0 `|g|` 分別為 1.258e-03 與 1.228e-03（差 2%）。這排除掉「grid 128 只是起點條件較好」這個競爭解釋——3.33 的軌跡差異來自最佳化過程實際用掉了多出來的控制點 | `runs/logs/gate_S.log`、`runs/logs/gate_S_g128b.log` | 現行 |
| 3.35 | **容量軸掃完了：`grid_size` 32 → 128 沒有讓 site S 的判定變號，反而更差。** 優勢由 +0.00744 變成 +0.01140（正值代表最佳化不如同失真隨機），同向數皆為 1/5。機制：grid 128 兩個條件的絕對效果都變大（opt −0.00212 → −0.00310；rand −0.00956 → −0.01449），但**隨機條件增加得更多**——容量提高使實際用掉的失真由 0.108 升到 0.178，而隨機雜訊把那份額外預算換成效果的效率高於最佳化。**這推翻 3.28 的歸因**：site S 的劣勢不是控制點密度不足 | `runs/gate_S_g128b/`、`runs/gate_compare.csv` | 現行。與 `runs/gate_S` **只差 `grid_size`**（6.24 的重取樣核已對齊）。仍是單張影像、單一目標函數、5 個評測種子 |
| 3.30 | **site S 在語意軸上是零。** 閘門實測（horse_00、`suppress`、60 步、256²、5 個評測種子）：擾動 LPIPS 0.1077、**L∞ 0.5439**、Δsiglip **−0.00212 ± 0.02459**（標準差是均值的 **12 倍**，判為非語意失敗）、Δniqe +0.2368 ± 1.0192、編輯 LPIPS 0.3403。L∞ 0.5439 對 LPIPS 0.1077 再次確認 2.16——空間變形的 L∞ 天生巨大（κ 的 9.1 倍）而可辨失真很小 | `runs/gate_S/summary.csv` | 現行。**n = 1 影像、5 個評測種子**。隨機對照條件待補 |
| 3.31 | **`edit_lpips / pert_lpips` 這個「效率」比值不可跨擾動量比較，它本身強烈依賴擾動量。** 2026-08-04 實測同一張影像、同一個 site PF、同一個目標函數：擾動 LPIPS 0.1385 時比值 **2.52**，0.5352 時只有 **0.99**；隨機條件同樣由 2.18 掉到 1.03。編輯 LPIPS 會飽和而擾動 LPIPS 不會，故比值隨預算單調下降。<br>**記這一條是因為本輪差點寫進帳**：site S 在擾動 0.1077 上的比值 3.16 一度被拿去對 site PF 在 0.5352 上的 0.99，得出「非加性效率高三倍」。那正是**不匹配失真的比較**——本專案三個月來的核心問題，在自己的判讀裡又出現一次。正確的對照是同一個擾動量上的加性位置：pert 0.1385 時 opt 2.52、rand 2.18，與 site S 的 3.16 同一量級 | `runs/gate_suppress/`、`runs/_smoke_gate2/`、`runs/gate_S/` | 現行 |
| 3.29 | **site S 的容量參數 `grid_size` 從來沒有被掃過，全部 24 次都是 32。** 掃過全庫 `env.json`：`e13`（4 次）、`e14`（4）、`e15`（3）、`e16`（1）、`e21`（6）、`e22`（3）、`e23`（1）共 22 個 run 的 `--ranks` 全部是 `[32]`，加上 `runs/ours_lo/` 的 `rank: 32` 與 2026-08-04 閘門寫死的 32。**「非加性比較弱」這個判斷因此完全建立在容量軸的單一點上。**<br>這與 3.28 合起來很重要：3.28 把 site S 的上限歸因到「參數化的擾動能力」，而**參數化的容量正好有一個沒被動過的參數**。`WarpResidual` 已支援 `grid_size=None`（逐像素自由位移，即 stAdv 的原始設定），其 docstring 也已寫明該設定需要 TV 懲罰否則會出現撕裂 | 全庫 `env.json` 掃描（2026-08-04）；`src/residual/site_warp.py` | 現行。**這是目前對研究問題最直接、變因最少的下一步**：單一變因、四級（32／64／128／None） |

## 4. 注入位置與參數化

| # | 結論 | 出處 | 狀態 |
|---|---|---|---|
| 4.1 | A 類（site L/E/W）共用 VAE 重建誤差下限，且該下限高於加性運作點 | E17–E19 | 現行 |
| 4.2 | latent 最佳化可把下限由 0.1434 砍到 0.0760，但有約 0.075 的真實下限，離門檻 0.063 差約 19%；步數與 lr 都不是瓶頸 | E17、E18 §1 | 現行 |
| 4.3 | site S 的鈍化幾乎全部來自重取樣：bilinear 只保留 85.0% 銳利度，bicubic 為 99.9%，而實際量到的 site S 為 85.2% | E20 §5.2、E19 | 現行 |
| 4.4 | site S 的飽和不是位移上界造成的。放寬 `max_disp` 到 6.0 後解幾乎沒變 | E22 | 現行 |
| 4.5 | `max_disp` 必須與 `warp_resample` 一起報告，兩者交互作用 | E21–E22 §5 | 現行 |
| 4.6 | site C 的有效約束恆為色度 hinge，與學習率無關（六個值跨 3 倍範圍）。這是參數化的固有性質——它的作用機制就是改色度矩陣 | E29 §3 | 現行 |
| 4.7 | 像素殘差的秩是湧現的（clamp 之後），不等於設定值，須實測不可假設 | spec §7.2、`tests/test_pipeline.py` | 現行 |

## 5. 目標函數

| # | 結論 | 出處 | 狀態 |
|---|---|---|---|
| 5.1 | `untargeted` 模式最大化的正是 `edit_shift`，而該量已被判定與防禦成功不對應。E25 之後判準改了，目標函數沒有跟著改 | E29 §5.1 | 現行 |
| 5.2 | `runs/` 中 59 個有記錄的 `env.json` **全部**是 `untargeted`。`targeted` 與 `crossattn` 從未在真實 SD 上產生過資料 | E29 §5.4 | 現行 |
| 5.3 | `crossattn` 的 `divergence` 模式在 φ=0 時梯度精確為零（KL 在最小值處），最佳化離不開起點。任何「與未防禦參照的散度」形式都有這個性質 | E20 §9 | 現行 |
| 5.4 | `suppress` 模式的最佳點不在 φ=0，起步梯度非零，是三個目標中唯一直接對著「讓編輯不服從 prompt」 | E25 §4.1、`src/models/attention.py` | 現行 |
| 5.5 | `targeted` 的 `L_def = LPIPS(y_def, y_target)` 是最小化、本來就有界，`margin` 對該模式不起作用 | E31 規格 §5.1 | 現行 |
| 5.6 | 防禦 hinge 的 margin 若太小會成為有效約束：E27 實測 w=7.5 下 site C 的 `edit_shift` 已達 0.44–0.49，逼近預設的 0.5 | E27 §2 | 現行 |
| 5.7 | **基準論文的損失就是 cross-attention 抑制**，但與本專案的 `attention_content_suppression` 有三處實質不同：(a) 它有由原圖注意力二值化而來的**空間遮罩**，只在該區內施力，我們對全部 query 位置取平均；(b) 它跨層雙三次上採樣後**相加**保留空間維度，我們逐層壓成純量再平均；(c) 它取聚合反應的 **L1**，我們取「1 − 內容 token 質量」；(d) **2026-08-04 補**：它以 **c_a**（防禦方選定要保護的詞）為文字輸入，我們以 `cfg.prompt_edit`（**攻擊方寫的編輯 prompt**）的內容 token 為對象（`optimize_crossattn` 的 `emb_edit = sd.encode_text(cfg.prompt_edit)` 與 `token_span(tokenizer, cfg.prompt_edit)`）。兩者在威脅模型裡屬於不同的人。用攻擊方的 prompt 是**更強的白盒假設**，本專案的結果因此是該線的上界——Attention Attack 正是因為防禦方不知道攻擊方的 prompt，才改用原圖的自動 caption 當代理 | Lo et al. 式 (3)(4)(5)；`src/models/attention.py`、`src/defense/optimize.py::optimize_crossattn`；SURVEY §3 | 現行。四處差異都保留，但 (d) 必須在報告中明講，否則會被讀成同一個方法 |
| 5.8 | 基準論文的三個攻擊（PhotoGuard encoder／PhotoGuard diffusion／semantic）**恰好對應本專案三個 `defense_mode`**（targeted／untargeted／crossattn）的著力點。E31 目標函數軸的設計因此可以直接改寫成該表的三欄 | 新規格 §3.1 | 現行 |
| 5.9 | 逐 timestep 各自反傳再平均，與「T 個損失加總後反傳一次」梯度相同但**記憶體差 T 倍**。記憶體效率是基準論文的主要賣點（其 Figure 6），故實作必須照其逐項反傳的寫法 | Lo et al. Algorithm 1、Figure 6；`src/defense/linf_attack.py` | 現行 |
| 5.10 | **`untargeted` 的防禦項在兩條分支逐元素相同時梯度精確為零。** `L_def = max(0, m − LPIPS(y_def, y_orig))`，而 LPIPS 在 y_def = y_orig 處是最小值，故 ∂L_def/∂y_def = 0。φ = 0 且淨化算子為 identity 時兩條分支正是逐元素相同。這與 5.3 判定 `divergence` 失效的是**同一個機制**，且 5.3 的原文已寫下一般原則（「任何『與未防禦參照的散度』形式都有這個性質」）——只是從未套用到 `untargeted`，而 `untargeted` 是 59 個有記錄的 run 的 **100%**（5.2） | `tests/test_zero_gradient.py`；`runs/ours_lo/man_0*__PF__history.json` 的 step 0 為 `grad_norm = 0.000e+00`、`edit_shift = 0.00e+00` | 現行 |
| 5.11 | **該目標函數是靠淨化 EOT 脫離起點的。** `eot_pairs("rotate", step, 1, 3)` 第 0 步取 identity、第 1 步取 blur、第 2 步取 jpeg。實測 `runs/ours_lo/` 的 site PF：step 0 梯度 0.000e+00、step 1 為 4.457e-01。也就是說**抗淨化這個附帶目標同時是主目標函數的啟動器**。直接後果：`--no_purify_train`（2026-08-04 為了讓兩個條件在同一個問題上而加，見 6.20）會讓 `untargeted` 永遠停在 φ = 0。腳本已改為在該組合下直接拒絕執行。**直接的實測示範**：`l4_crossattn_probe.py` 的 `untargeted` 條件只傳了 `[Purifier("identity")]`，於是每一步都落在零梯度點上——連續兩步都是 `|g| = 0.000e+00`、`shift = 0.0000`、`L_fid = 0.0000`，φ 完全沒有動，而損失一直顯示 1.0000（= margin），看起來像是「有損失但下不去」 | 同上；`runs/logs/l4_crossattn_probe.log`；`scripts/run_ours_lo_eval.py` | 現行 |
| 5.12 | **兩個 site 的起步機制不同，且差三個數量級。** site S 的 step 0 梯度不是零而是 4.7e-03～4.9e-04，來源是 `site_warp.py` 的 `grid_sample` 數值底線 5.78e-05——φ = 0 時 `needs_graph` 為真故不走零位移短路。即 PF 由淨化算子提供一個乾淨的推力（4.5e-01），S 由數值雜訊提供一個小三個數量級的推力。**這是加性／非加性之外的一個不對稱，與參數化本身無關**。<br>**換成 `suppress` 之後這個不對稱消失。** 起點梯度是 `∂L/∂x_def · ∂x_def/∂φ` 的乘積：`untargeted` 在 φ=0 時第一項精確為零（5.10），site S 只靠第二項乘上 `grid_sample` 的數值底線才有非零值；`suppress` 的第一項本來就非零，故 site S 得到的是由影像梯度決定的**正常**梯度（`site_warp.py` 的 docstring 早已寫明「位移場是自由參數，在零點的梯度不為零」）。實測 `runs/gate_S/` 的 site S + `suppress` step 0 為 1.258e-03，與 site PF 的 5.113e-02 同屬正常量級而非數值雜訊 | `runs/ours_lo/*__history.json`；`runs/logs/gate_S.log`；`src/residual/site_warp.py:144-165` | 現行 |
| 5.13 | **兩個從未跑過的目標函數都沒有 5.10 的缺陷。** `encoder`（`min ‖E_vae(x_def) − z_target‖²`，z_target = 0）在 φ = 0 時 `E_vae(x) ≠ 0`，實測起步梯度 7.99e-02；`suppress` 的最佳點不在 φ = 0（5.4），實測起步梯度 1.19e-02。即**唯一有此缺陷的，正是被用了 100% 的那一個** | `runs/logs/l4_crossattn_probe.log` | 現行 |
| 5.14 | 5.11 使 `scripts/e31_train_probe.py` 印出的 `shift` 失去意義：該腳本同樣只傳 `[Purifier("identity")]`，故它量的每一格 φ 都停在 0。**成本數字（256² 為 178.04 s/step、峰值 5628 MB）仍然有效**——前向與反傳照跑，只是參數沒更新；但同一行印出的 `shift` 不是任何東西的量測 | `scripts/e31_train_probe.py:67`；6.7 | 現行 |

## 6. 實驗協議與工程

| # | 結論 | 出處 | 狀態 |
|---|---|---|---|
| 6.1 | 固定步數的網格是錯的協議：兩個 site 的 φ 量綱與學習率都不同，「同樣跑 N 步」對兩者不是同一件事。τ=0.05 由 25 步改到 100 步後，site S 對 site P 的比值由 1.14× 反轉為 0.85× | E21–E23 §5.4 | 現行 |
| 6.2 | 有效約束診斷是常設步驟。已踩過四個誤判的有效約束（L∞ hinge、步數、`max_dev` 硬上界、防禦 margin），每一個都會讓整批網格變成無效資料 | E13、E21–E23、E27 §2 | 現行 |
| 6.3 | 只認係數非零的 hinge。`gamma_psnr = 0` 的 PSNR 罰則曾被誤判成有效約束 | `optimize.py::active_constraint_keys` | 現行 |
| 6.4 | 噪聲的訓練／保留分離做對了：`EVAL_SEED_OFFSET = 10000`，兩者以 `noise_split` 併列報告 | E29 §6 | 現行 |
| 6.5 | TF32 使同一份程式在不同機器上精度與速度都不同。E27 的成本基準是在 TF32 開啟下量的（峰值 10317 vs 10320 MB 的指紋比對） | E28 §4、E29 §2 | 現行 |
| 6.6 | 本機 RTX 2050 4 GB 跑得動**無梯度**的 512² SDEdit：峰值 4873 MB（靠 Windows 共享記憶體外溢）、單次 222.5 s | `runs/logs/e31_local_probe.log` | 現行 |
| 6.7 | 本機**跑不動任何規模可用的訓練**：256² 是 178.04 s/step、峰值 5628 MB，即 H100 在 512² 上的 75 倍。單張影像跑滿 150 步是 7.4 小時，而網格是 12 個點 × 2 張影像 = 24 次最佳化，即上限約 178 小時 | `runs/logs/e31_train_probe.log` | 現行 |
| 6.8 | 本機 GPU 工作不可並行，CPU 密集工作也不可與 GPU 工作並行——實測後者使單張 SDEdit 由 222 s 拉長到 30 分鐘以上 | E31 本機階段 §4.1 | 現行 |
| 6.9 | Windows 上所有腳本都要 `PYTHONIOENCODING=utf-8`。cp950 編不了 `²`，症狀是腳本在**印出結果時**才炸，前面的計算全部白做 | E31 本機階段 §4.1 | 現行 |
| 6.10 | `scripts/run_defense.py --help` 原本是壞的：兩個 help 字串含未跳脫的 `%`，argparse 會拿去做 `%` 格式化。2026-08-03 修好 | E31 本機階段 §6 | 現行 |
| 6.11 | `.gitignore` 的 `runs/` 區塊曾有一條 `runs/*/**` 讓 git 停止遞迴而靜默漏掉 273 個檔案。改動該區塊必須用 `git status --porcelain --ignored` 確認 | commit `1942e38` | 現行。**2026-08-04 差點重演**：該區塊是「`runs/**` 全部排除 + 逐副檔名放行」，而當天新增的 `torch.save(state_dict, "*__phi.pt")` 的副檔名不在放行清單上，會被靜默丟掉。**`git status --ignored` 抓不到它**——檔案還沒被寫出來，掃描的結果是空的。有效的檢查是 `git check-ignore -v <預計要寫出的路徑>`，在寫檔之前跑。已加 `!runs/**/*.pt` |
| 6.12 | **`LossConfig` 的預設值不是本專案要的**：`beta_linf = 100.0`（配 `tau_linf = 0.06`）與 `alpha_lpips = 1.0`。不覆寫就等於用 L∞ 綁定，對 site S 是致命的——位移在 L∞ 上天生昂貴而人眼看不出來，正是本專案主張不用 L∞ 的理由。實測：S 停在 `pert_linf` 0.0750、`pert_lpips` 0.0034（預算的 1/29）。`e29c` 兩者都是 0.0 | `runs/ours_lo_linfbound/`；commit `b61f682` | 現行 |
| 6.13 | **`plateau_stop` 會在約束仍被違反時停止**。條件一只檢查「罰項曾非零」，不檢查「已回到預算內」。實測 site PF 第 31 步停止時 LPIPS 罰項仍有 34（失真約 0.44，預算 0.10 的 4.4 倍），觸發原因是 `edit_shift` 正在下降——那是最佳化在把失真拉回預算，卻被判成沒進展。加 `require_feasible`（預設關閉以保留既有 run） | `runs/ours_lo_earlystop/`；`tests/test_plateau_stop.py` | 現行 |
| 6.14 | **lr 0.03 是為 site P（低秩 r=16）校準的，用在 site PF（全秩）會震盪**：保真罰項在 29→76→27→43→54 之間來回，不收斂，必撞步數上限。唯一跑過 PF 的 `e8` 用 lr 0.008；改回後 PF 在 48 步收斂到預算內 | `runs/ours_lo_earlystop/`；`runs/e8_rank_tau0.10/env.json` | 現行 |
| 6.15 | `pkill -f "<pattern>"` 的中括號技巧只避免**模式字串自身**被匹配。若同一條 ssh 指令的其他部分（例如 heredoc 寫出的啟動指令）含有目標的完整字串，正則仍會匹配到自己的 shell，整條指令在第一步就死掉，症狀是「看起來沒反應、日誌像舊 run 還在跑」。殺行程與啟動必須分成兩次呼叫 | 2026-08-03 遠端操作 | 現行 |
| 6.16 | **一個 lr 不可套用到全部 site。** φ 的量綱逐 site 不同（P/PF 是像素值、S 是位移像素、C 是矩陣偏離量），Adam 每步位移約為 lr，故同一個值代表三種不同的步長。E14 在同一判準下校準出 S = 0.1、P = 0.008，相差 12.5 倍，E21/E22 全部照此執行；`runs/ours_lo/` 卻兩個 site 共用 0.008。**site S 因此跑在校準值的 1/12.5** | `runs/ours_lo/protocol.json`、`docs/RESULTS_E13-E23.md` §2、`runs/e21_S*/env.json`；修正見下方 before/after | 現行 |
| 6.17 | **τ_acut 與 τ_chroma 必須隨 LPIPS 預算而變，`LossConfig` 的 0.04／0.8 是 τ_lpips = 0.05 的值。** 這是 2.9／2.10 的判定與 p14 的量測，但 `run_ours_lo_eval.py` 從未讀那張表。`runs/ours_lo/` 跑在 τ_lpips = 0.10（該預算的值是 0.0598 與 1.2965），實測後果：man_00/PF 的鈍化 hinge 啟動 31/48 步、man_01/PF 的色度 hinge 啟動 25/150 步、man_02/PF 31/150 步——**兩道副約束確實成了有效約束** | `runs/ours_lo/*__history.json`、`runs/p14_budget_thresholds/thresholds.csv` | 現行 |
| 6.18 | **`plateau_stop` 的條件一使「一次都沒碰到約束」的格子結構上不可能提早停。** `require_constraint` 要求觀察窗內某一步的懲罰大於零；三道 hinge 全程為零時該條件恆為 False，函式永遠回傳「不停」，該格必定用盡 `steps`。故「跑滿上限」對這種格子**不是**未收斂的證據，而是準則沒有定義。site S 的三格全部落在這裡 | `optimize.py::plateau_stop`、`runs/ours_lo/*__S__history.json` | 現行 |
| 6.19 | `scripts/run_defense.py::load_images` 有兩個無症狀的缺陷，2026-08-04 修好：(a) `list(prompts.get(cls))` 對 `data/lo_aligned` 的 `{content, prompts}` 格式回傳的是**鍵名**，φ 會對著 `"content"`／`"prompts"` 兩個字串最佳化；(b) `root.rglob("*.png")` 會把 `data/lo_aligned/overview.png` 當成待防禦影像。後者 `run_lo_baseline.load_dataset` 早已因同一理由改掉，前者只寫在 `run_ours_lo_eval.py` 的 docstring 裡而沒有修 | `tests/test_site_config.py` | 現行 |
| 6.20 | **本專案的條件在訓練期做淨化 EOT，基準的三個攻擊不做，而評測路徑兩邊都不量淨化。** `default_train_set()` 是 identity／blur σ=1.0／JPEG q=75，`purify_mode` 預設 `rotate` 且 `n_eot = 1`，故 2/3 的梯度步是在被淨化過的輸入上求的。抗淨化是本專案相對基準論文的加項（規格 §3.2），不是缺陷，但它使兩個條件不在同一個問題上，且該加項在 L4 的判準裡完全沒有回報 | `src/purify/ops.py`、`scripts/run_lo_baseline.py::evaluate`；`--no_purify_train` 於 2026-08-04 加入 | 現行 |

| 6.21 | **`stop_tol` 與監看量綁在一起，換監看量就必須換它——這是同型缺陷的第九次。** `plateau_stop` 比較的是「每步的**絕對**改善量」，而 `stop_tol = 1e-4` 是為 `edit_shift` 校準的（E23 實測平均每步 5.4e-4，取低一個量級）。`optimize_crossattn` 改監看 `attn_div`（E31，見 5.3 的處置）時**沿用了同一個值**。實測（同一張影像、同一個 site PF、60 步）：`edit_shift` 總改善 0.3665、平均每步 7.8e-03；`attn_div` 總改善 0.0095、平均每步 1.6e-04，**相差 39 倍**。後果：開啟 `stop_on_plateau` 時 crossattn 會在**第 30 步（共 60 步）**判定收斂，砍掉後半段 53% 的改善，而且回報的是「已收斂」。<br>`attn_div` 的範圍為何這麼小：它是「1 − 內容 token 的注意力質量」，而內容 token 的質量本來只有約 **1.5%**（BOS/EOS/padding 吸走大部分），故可達的改善上限就是那 1.5%。這也是本專案的變體與論文式 (5) 的一個實質差異——後者取聚合反應的 L1、保留空間維度，動態範圍大得多（5.7）。<br>**處置**：`OptimConfig.stop_tol` 改為 `None`（依監看量取 `MONITOR_TOL`），未校準的監看量直接拋出。**沒有可重現性代價**——`crossattn` 從未在真實 SD 上跑過（5.2） | `runs/gate_suppress/horse_00__PF__history.json`；`src/defense/optimize.py::MONITOR_TOL`；`tests/test_zero_gradient.py` | 現行 |
| 6.22 | **改動一支正在執行的腳本不會影響那次執行。** Python 在 import 時把原始碼載入記憶體，之後改檔案對已啟動的行程無效。2026-08-04 實測：04:26 為 `gate_suppress.py` 加了 `torch.save(state_dict, "*__phi.pt")`，而 `runs/gate_S/` 的行程 04:02 就已啟動，**該次執行沒有存下 φ**，事後只能用影像空間的近似縮放。<br>這與「編輯執行中的檔案會不會弄壞它」是相反的擔憂——不會弄壞，但**也不會生效**，而後者比較容易忘。要讓改動生效必須重跑 | 2026-08-04 操作 | 現行 |
| 6.23 | **同一類缺陷的第十次：`gate_compare.py` 以 `site` 單獨分組。** 該分組鍵為「一個 site 一個 run」而寫；一旦同一個 site 有多個設定（grid 32 與 grid 128），兩者會被平均成一個「site S」數字，而那個平均正好抹掉要量的效應，且輸出看起來完全正常。改法與前九次相同：不寫死是哪個軸，改由 `protocol.json` 自動比出同 site 各 run 之間**真正動到的鍵**（`variant_axes`），缺鍵記為 `<未記錄>` 而不是當成與對方相同 | `scripts/gate_compare.py` | 現行。**這次是靠讀出來的，不是靠失敗**——與 6.16/6.17/6.21 設計的「未校準就拋出」不同，此處沒有可拋出的時機，因為錯誤分組在語法上完全合法 |
| 6.24 | **`--warp_resample` 的預設值與 `WarpResidual` 的類別預設不一致，使容量掃描一次動到兩個變因。** `WarpResidual.resample` 預設 `bilinear`；`gate_suppress.py --warp_resample` 預設 `bicubic`（今晚新增）。`runs/gate_S`（04:02 啟動）的 `protocol.json` 沒有這個鍵，證明當時 `build()` 尚未傳入該參數，故它用的是 `bilinear`；05:16 啟動的 `runs/gate_S_g128` 則記錄了 `bicubic`。兩者相比時 `grid_size` 與重取樣核同時改變 | `runs/gate_S/protocol.json`、`runs/gate_S_g128/protocol.json`、`src/residual/site_warp.py:82,174` | 05:41 中止該 run，改以 `--warp_resample bilinear` 重跑為 `runs/gate_S_g128b`。**`runs/gate_S_g128` 保留為中止紀錄，不刪除**。判斷依據是不對稱性：若讓 bicubic 跑完，null 結果仍乾淨，但正結果無法歸因，而正結果才是對專案有後果的那一個 |

### 6.16 的 before/after

    修訂前（scripts/run_ours_lo_eval.py line 169）
        ap.add_argument("--lr", type=float, default=0.03)
        ...
        cfg = OptimConfig(steps=args.steps, lr=args.lr, ...)

    修訂後（同檔 line 66-110、303）
        SITE_LR = {"P": 0.008, "PF": 0.008, "S": 0.1, "C": 0.3}
        ap.add_argument("--lr", default="")     # 留空 = 逐 site 取校準值
        ...
        cfg = OptimConfig(steps=args.steps, lr=lr_map[site], ...)

`scripts/drivers/ours_l2.sh` 原本不傳 `--lr`（吃到 0.03），現在明確不傳並在
註解寫明理由。未校準的 site 直接以 SystemExit 拒絕，不共用別人的 lr。

### 6.17 的 before/after

    修訂前（scripts/run_ours_lo_eval.py，loss_cfg 建構處）
        loss_cfg = LossConfig(margin=..., tau_lpips=args.tau_lpips,
                              beta_linf=..., alpha_lpips=...)
        # tau_acut 與 tau_chroma 取 dataclass 預設的 0.04 / 0.8

    修訂後（同檔 budget_thresholds() 與 loss_cfg 建構處）
        t_acut, t_chroma = budget_thresholds(args.tau_lpips, root)
        loss_cfg = LossConfig(..., tau_acut=args.tau_acut,
                              tau_chroma=args.tau_chroma, ...)

查表而非寫死：`runs/p14_budget_thresholds/thresholds.csv` 是量測結果。預算
不在表上時拋出而不內插——2.13 已量到 acut 軸的分離度隨預算塌掉。

---

## 7. 已推翻的結論

**不刪除**。每一條附上當時的依據與推翻它的資料。

| # | 當時的結論 | 當時的依據 | 推翻它的資料 |
|---|---|---|---|
| 7.1 | site L 有防禦效果 | E2 主網格 | `e2_phi0` 對照：φ=0 時數字相同，那是 VAE 來回而非防禦 |
| 7.2 | site S 對 site P 有 1.15× 的增益 | E15 主網格 τ=0.05 | E20 撤回（匹配失真不成立）；E21 進一步量出該 net 的 67.3% 來自模糊 |
| 7.3 | site S 的飽和是位移上界造成的 | E16 的推論 | E22：放寬 `max_disp` 到 6.0 後解幾乎沒變 |
| 7.4 | cross-attention 目標「142 項測試全過即可用」 | 測試通過 | E20 §9：`divergence` 在 φ=0 梯度精確為零，先前通過靠的是 SSIM 在恆等點的 1.22e-10 浮點殘渣 |
| 7.5 | E2–E23 的防禦效果數字 | 那批 `results.csv` | E26：攻擊端從來沒有 classifier-free guidance，等同 w=1 |
| 7.6 | 本專案的失真預算比文獻低 5–8 倍 | 跨論文的 LPIPS 比較 | E31 §3：只在 LPIPS 上成立，L∞ 反而是文獻的 6 倍 |
| 7.7 | ΔE76／ΔE00／`dchroma` 可作為色度約束 | 色差的國際標準 | E28 §1：等 LPIPS 下分不出加性雜訊（2.44）與可見色偏（2.79） |
| 7.8 | NLPD／VIF／GMSD／HaarPSI 可作為鈍化約束 | 文獻聲譽 | E20 §5.2：四條件探針顯示它們對雙三次變形收費更高，量的是位移 |
| 7.9 | 3.11「本專案的加性實作在 κ 上落後 PhotoGuard，第一層尚未通過」 | `docs/RESULTS_TABLE1.md`（2026-08-03 之前） | 3.17：該判定拿 `e8_rank_tau0.05` 去比論文公布值，但 `e8` 屬於 3.2 判定作廢的 w = 1 批次（攻擊方沒有 classifier-free guidance，等於在防禦一個不存在的攻擊）。同一份資料上重測，本專案的 PhotoGuard 實作編輯 LPIPS 是 0.6363 而非論文的 0.4131，門檻本身也算錯了 |
| 7.10 | 3.19「綁住 site S 的不是 LPIPS 而是鈍化約束」 | `runs/ours_lo/summary.csv`：site S 停在 LPIPS 0.0163（預算的 16%）卻跑滿步數，故推論「一定有另一道約束先綁住它」 | 該批的 `*__S__history.json`：三格 × 150 步裡 `fid_pen_acut` **全部為 0**，`fid_pen_lpips` 與 `fid_pen_chroma` 亦然。**沒有任何一道約束綁住它**。真正的原因是 6.16——lr 0.008 是 site P/PF 的校準值，site S 的是 0.1，該條件跑在 1/12.5 的步長上；而依 6.18，三道 hinge 全程為零時 `plateau_stop` 結構上不可能觸發，跑滿上限是必然而非量測結果。修正後的判定見 3.23 |

---

## 8. 死路清單

不要重走。每一條都附排除的理由與時間。

| 方向 | 為何排除 |
|---|---|
| `net_lpips`／`edit_shift` 當防禦成功的判準 | E25、E29 兩次量到它與編輯是否失敗不對應 |
| 對抗性強健的感知度量（E-LPIPS／R-LPIPS／LipSim） | 解的是相反的失效 |
| NLPD、VIF、GMSD、HaarPSI 當保真約束 | 量的是位移不是鈍化（E20） |
| ΔE76／ΔE00／`dchroma` 當色度約束 | 量的是量值不是空間連貫性（E28 §1） |
| low rank | 使用者 2026-07-30 明確排除 |
| site L／E／W（A 類） | VAE 重建誤差下限高於加性運作點（E17–E19） |
| site S | 使用者 2026-08-01 決定不放進重跑 |
| 固定步數的網格 | E21–E23 §5.4 |
| 再調 site C 的學習率 | 六個值涵蓋 3 倍範圍，有效約束不變（E29 §3） |
| 在 τ ≤ 0.10 下用 `untargeted` 目標再跑網格 | E29 已在最寬鬆的點上量過 |
| **E31 的 12 格網格**（τ_lpips × strength × 目標函數的盲搜） | 2026-08-03 排除。基準論文 Table 1 提供了現成的座標與強基準，盲搜運作點不再必要。由新規格 §5 的 L1 取代 |
| 在第一層（重現）通過之前質疑第 1 類判準 | 2026-08-03 排除。ρ = −0.207 的資料保留在 `runs/p16_criterion_correlation/`，但重現落後基準時提出會被讀成「沒調好就怪尺」（新規格 §7）。**2026-08-04 部分解除**：3.15 之後兩個 PhotoGuard 變體 已重現，該顧慮對這兩個不再成立，且 3.24 的逐圖判讀是在**基準自己的協定與預算上**取得的直接證據，不是拿自己的失敗去質疑尺。`semantic` 那一個仍未重現，涉及它的比較維持不提出 |

---

## 9. 尚未回答的問題

| # | 問題 | 現況 |
|---|---|---|
| 9.0 | **在 κ = 0.06 上，我們的加性實作能不能追平 PhotoGuard 與 semantic attack？** | **最優先。** L1 待跑（新規格 §5）。落差來源清單見新規格 §6 |
| 9.1 | `targeted` 與 `suppress` 兩個目標在真實 SD 上有沒有效？ | L1 會同時回答——它們就是 Table 1 的 encoder attack 與 semantic attack |
| 9.2 | 文獻預算（LPIPS ≈ 0.28）下防禦會不會成立？ | 降級。基準論文用的是 L∞ 預算不是 LPIPS 預算，該問題屬於第二層（L4） |
| 9.3 | strength = 0.3 會不會改變 E29 的否定結果？ | 降級。`runs/e31_strength_sweep/` 已給出與該推論相反的量測（3.7），且基準論文未公布 strength |
| 9.4 | 感知劣化的門檻定在哪？ | `runs/p11_degrade_ladder/compare.html` 待使用者判讀。屬於改良判準（L3），第一層用不到 |
| 9.5 | 非加性在正對照成立後能否以較低的可辨失真達成同樣效果？ | 第二層（L4）。第一層通過前不可問 |
| 9.6 | inpainting 威脅模型是否著力點更大？ | 未做。需新寫攻擊路徑並改變威脅模型定義。注意 SDA 的動機是**全域擾動在遮罩式編輯上失效**，兩種設定的失效機制不同，不可互相引用（SURVEY §5） |
| 9.7 | 「只攻第一個去噪步」這一類在全域 SDEdit 上有效嗎？ | 未做。DiffusionGuard（噪聲範數）與 SDA（self-attention query）兩個成員都只測 inpainting。成本結構與 `crossattn` 相同（SURVEY §3） |
| 9.8 | 本專案的 `crossattn` 有沒有把力氣攤太平？ | `optimize.py` 的 `t_list` 均分於整個 `[0, t_edit]`，而文獻的成功案例集中在最高的那個 t。這是一行改動即可檢驗的假設（SURVEY §3） |
| 9.9 | 無參考品質指標能不能代替 MLLM 判準？ | SIFM 量過 MLLM 與人類的一致率 74%（人類彼此 76%），但沒人量過 NIQE 這類指標的。`runs/p11_degrade_ladder/` 是為此準備的材料 |
| 9.10 | w=7.5 下三類判準的相關性？ | 既有資料只有 3 格，太少。E31 的網格會補上 12 格（`runs/p16_criterion_correlation/`） |
| 9.11 | 正例出現時，換幾個評測噪聲種子還站得住嗎？ | 未做。目前每格 2 張影像 × 1 個 held-out 種子。DiffHammer（NeurIPS 2024）主張單次評測系統性低估風險，應改 N 次評測。評測那一半可在本機跑，成本線性（SURVEY §9） |
| 9.12 | 基準論文的 strength 與 guidance_scale 是多少？ | 論文與補充材料都沒寫。可考慮直接問指導者——這是最省時的路徑 |
| 9.13 | 我們的加性落後基準，是優化器（Adam + 平台停止 vs PGD sign × 100 步）造成的嗎？ | **已回答：不是。** L1 用 PGD sign × 100 步重跑，兩個 PhotoGuard 變體 達到或超過論文值（3.15）。問題本身隨 3.11 一起作廢 |
| 9.14 | **site S 在自己的校準學習率（0.1）與逐預算門檻下，能走到哪裡？** | **最優先。** L4 的非加性位置目前零格可用（3.23），現有的「非加性弱六倍」不是量測結果而是設定錯誤。修正已落實在 `run_ours_lo_eval.py`，待雲端重跑 |
| 9.15 | 訓練期的淨化 EOT 讓本專案的條件付出多少代價？ | 未量。`--no_purify_train` 已加入，一次 A/B 即可回答（6.20） |
