# 交接 —— 2026-08-08

> **新 session 從這份開始讀。** 它取代 `HANDOVER_2026-08-07.md`（該份描述的是
> 段 0 正在跑的狀態，已完全過時）。讀完這份就能接手。
>
> 要引用 2026-08-07 的任何數字，讀 `RESULTS_2026-08-07.md`。

---

## 1. 一句話現況

**第一階段（img2img）已完整結束，含兩個批次**：

| 批次 | 內容 | 結果 |
|---|---|---|
| `v14` + `b3` | strength 0.6、`tau_acut` 0.04 | 三層主張都不成立 |
| **`v14r`** | strength 0.4、`tau_acut` 0.16 | **門檻的修正成立，工作點的修正不成立** |

`v14r` 的兩個變因結果相反：`tau_acut` 綁死訓練是真缺陷且已修好（被罰的訓練格
6/6 → 0/6），而 strength 的處置不成立——其唯一證據 `RESULTS_2026-08-07` §6c.2
經複核是**單一影像的效果被外推**，同一方法在另一張圖上符號反轉。
**strength 0.3／0.4／0.5／0.6 已全部量過，不要再調它。**

同時查出既有主判定 `effect_siglip` 量錯了東西，已換成類別 margin 的二元判定，
並把文獻的全部判準納入分三層重測——**同一批資料，三個判準給出三個相反的結論**。
判定的部分另立短版交接：**`HANDOVER_METRICS_2026-08-08.md`**（給 inpainting
session，讀完就能用）。

**第二階段改 inpainting**（使用者 2026-08-07 定案）——批次 `ip3` 由另一個
session 執行中。

實驗報告頁（各含架構圖、變因說明、真實影像對照、圖表、適用範圍）：

- `v14r`：`https://claude.ai/code/artifact/40ffb2b0-d25a-4504-9362-23dafdfde944`
- `v14`／`b3`：`https://claude.ai/code/artifact/4eaadbf2-3c2d-45d2-8a2e-b1c83b01d2d9`

### 遠端狀態（2026-08-07 23:07 確認）

tmux **無 server**、我們的 python 行程**全數結束**、監控已全部斷開。
GPU 上剩下的行程是他人的工作。`~/wacv_runs` 的產物已全部回收，可自由清理。

---

## 2. 這一輪的結論

### 2.1 主張階層的結果

| 層級 | 主張 | 結果 |
|---|---|---|
| **主** | 非加性抗淨化勝過加性 | **無法判定**——分母（identity 那一格的效果）整批在雜訊裡 |
| 次 | 抗編輯持平或小輸 | **不成立**——匹配預算下加性有效、非加性沒有 |
| 三 | 保真受控 | 資料齊全，見 `compare.html` |

**六個分片全部完成、5880 列、零失敗。** v14 的 20 個（影像 × 條件）組合
沒有一個通過 `mean ≥ 3σ`，`effect_abs` 落在 −0.007 ~ +0.004 而標準差
0.002–0.013，**標準差普遍大於均值**；b3（SDXL）合併後同一個型態
（`RESULTS_2026-08-07` §6a.0b）。**兩組合計 `retention_usable` 為 True 的是
0 列**，判定腳本因此連續四次拒絕出表。

這使適用範圍明確：不是某一張圖、某一個模型、某一種參數化的問題，而是
**strength=0.6 的全圖 img2img 這個工作點上任何方法都量不到防禦效果**。

真正的比較在 `RESULTS_2026-08-07` §6c.2：匹配預算 τ=0.20、strength 0.4、
10 seed 下 Mist +0.0145、PhotoGuard-c +0.0134，而 N1／N2／N3 在零或負值。

### 2.2 根因（三層，只有第二層是我們的缺陷）

1. **威脅模型的 strength 選錯點。** 攻擊的區間在 0.5 就飽和，防禦效果在
   0.4 之後崩掉；0.4 是唯一同時滿足兩者的點，而我們定在 0.6。段 0 的
   `calibrate_strength` 取「編輯效果最大」，**由建構上就會選到防禦最無力的
   一端**——其 docstring 早已預告這件事。
2. **訓練被 `tau_acut = 0.04` 綁死。** 六個訓練格的 `fid_acut` 全部精確頂在
   0.04015–0.04038，即三個非加性條件**從未在 LPIPS 0.20 上最佳化過**，
   停在 0.052–0.174 就被段 2 放大上去。
3. **baseline 也失效，但不是我們把預算給少了。** 原始強度（k=1，未縮放）下
   Mist 在 LPIPS 0.62 仍是 +0.0014。用它們自己的判準（輸出位移）我們**有**
   重現——Mist 的 `edit_lpips` 是 0.72；我們問的是更嚴的「編輯有沒有失敗」。
   **這個落差必須寫進論文**，否則讀者會以為 baseline 在自己的設定下也這麼弱。

---

## 3. 下一批：inpainting

### 3.1 為什麼

- **strength 這個自由度消失。** 五篇 baseline 的原始碼**都沒有** attack-time
  strength（三篇是 inpainting 沒有這個概念，Mist 走 t~U(0,1000)，DIA 走固定
  10 步 DDIM）。0.6 是我們自己定的，而我們選錯了。
- **三篇回到原生形式。** PhotoGuard-c、AdvPaint、PromptFlare 原作**就是**
  inpainting，目前全部標著 `modified_from_paper=True`。
- **防禦方重新有著力點。** 未遮罩區域是直接給模型的，生成內容必須與脈絡
  一致；防禦擾動污染的是脈絡。全圖 img2img 在 strength 0.6 下模型可整張重畫。

### 3.2 怎麼起

**第一步：確認權重。** `hf_cache` 目前只有 SD v1.4 與 SDXL，
`runwayml/stable-diffusion-inpainting` 尚未下載（約 4 GB）：

```bash
source ~/env.sh
$PY -c "from diffusers import StableDiffusionInpaintPipeline as P;         P.from_pretrained('runwayml/stable-diffusion-inpainting',                           safety_checker=None, requires_safety_checker=False)"
du -sh $HF_HOME
```

**第二步：乾跑，確認格點與遮罩的規模**（不耗機時）：

```bash
cd ~/WACV && BATCH=ip1 bash scripts/shard.sh calib --dry-run bird_03 cat_02 dog_03
```

**第三步：正式跑。**

```bash
BATCH=ip1 bash scripts/shard.sh calib  bird_03 cat_02 dog_03   # 段 0，單卡，2–3 h
BATCH=ip1 bash scripts/shard.sh fanout bird_03 cat_02 dog_03   # 段 1–3，三卡，5–7 h
BATCH=ip1 bash scripts/shard.sh merge  bird_03 cat_02 dog_03   # 合併 + 段 4
```

`ip*` 的 profile 已在 `shard.sh`：`runwayml/stable-diffusion-inpainting`、
`--wrapper sd_inpaint`、`--mask-mode attention_box --mask-tau 0.5`、
**不帶 `--strength`**（`run_stage` 會在 `--mask-mode` 下把它清成 None，
明給兩者會直接擋下）。

### 3.2a 段 0 一跑完就要看的三件事

1. **`<批次>/masks/` 的疊圖**——遮罩有沒有蓋住那個物件。這是新引入的變因，
   蓋錯的話後面全部白跑，而純遮罩看不出對位，要看 `*_overlay.png`。
   `masks.csv` 有涵蓋率；**涵蓋率若 > 0.6 或 < 0.05 要停下來重新判讀**。
2. **`lr.N2` 是否落在 0.02–0.1**（前兩批都選中 0.1）。
3. **`calib_summary.json` 的 `strength`**——inpainting 下該掃描退化為單點，
   那是正確行為（攻擊強度由遮罩面積決定），但別把它的建議值當真。

### 3.3 程式已完成的九個 commit

| commit | 內容 |
|---|---|
| `3dc0385d` | `SDWrapper.inpaint`（9 通道、逐步貼回、可微分）；`src/data/masks.py` |
| `798b901c` | `SDWrapper.edit`：唯一分派點 |
| `242af05e` | 接進評測管線；遮罩進 `config_hash` 且隔離 img2img 的雜湊 |
| `8d79a7f1` | 訓練期的代理編輯鏈也走 `edit`；修好心跳誤報 |
| `048615fb` | PhotoGuard-c 還原 |
| `c208371f` | AdvPaint 還原 |
| `5da4c937` | PromptFlare 還原；修正 `grad_outside_mask` |
| `1c924839` | 遮罩落盤存證；`ip*` profile；擋掉 strength 外洩 |
| `2c9718e0` | `RESULTS_2026-08-07` 的 §6a–6c、§10 |

**測試基準：741 passed / 1 xfailed**（原 688）。

### 3.4 三個不要重新踩的設計點

**`--mask-mode` 沒有預設值。** `config_hash` 吃整個 `base_config`，多一個鍵
就改變**每一格**的雜湊。故 `mask` 鍵只在 inpainting 時出現，img2img 的雜湊
逐位不變（有測試釘住）。

**兩個旗標不可互相代替**：

| | `needs_mask`（原作的攻擊需不需要遮罩） | `grad_outside_mask`（原始碼有沒有把梯度乘 (1−mask)） |
|---|---|---|
| PhotoGuard-c | False | **True** |
| AdvPaint | **True** | False |
| PromptFlare | **True** | **True** |

前兩篇取值相反，用錯會對錯的論文施加梯度遮罩且**沒有症狀**。

**訓練期與評測期必須是同一種攻擊。** `optimize` 的代理編輯鏈原本直接呼叫
`sdedit`，在 inpainting 下會變成「防禦對著 img2img 最佳化、評測跑 inpainting」，
兩者在報表上都叫「編輯」、數字也都合理。現在兩條鏈都走 `edit`，有原始碼檢查釘住。

### 3.4a 起跑前必須先處理：約束會在到達預算之前就擋下我們的方法

**這是第二階段最可能重演第一階段失敗的地方，於 2026-08-08 事前查出。**

#### 為什麼 inpainting 救不了這件事

`L_fid` 的四道 hinge 比的是 `x_def` 對 `x_base`，而位移場的 `pixel_residual`
是純 `grid_sample`、**不經過模型**，像素路徑的 `x_base` 就是原圖。也就是說
**`L_fid` 的計算裡沒有任何一項與「攻擊是 img2img 還是 inpainting」有關**。
第一階段那個「訓練被 `tau_acut` 綁死」的問題會原封不動搬過去。

由 `runs/tau_threshold_probe_v14.csv` 線性內插，各條件的 `acut` 撞到 0.04
時的 LPIPS：

| 影像 · 條件 | acut=0.04 的 LPIPS | 綁定的是 |
|---|---|---|
| bird_03 · R | **0.066** | `tau_acut` |
| bird_03 · N1 | **0.106** | `tau_acut` |
| bird_03 · N2 | **0.137** | `tau_acut` |
| dog_03 · R | **0.092** | `tau_acut` |
| dog_03 · N1 | **0.149** | `tau_acut` |
| dog_03 · N2 | **0.193** | `tau_acut` |
| N3（兩張圖） | 在 τ=0.20 已是 0.138–0.141（**3.5 倍**） | `tau_acut`，更早 |
| **PhotoGuard-c** | 到 τ=0.35 都沒撞到 | **`tau_lpips`，拿到完整預算** |

五個非加性條件全部在 **0.066–0.193** 撞牆，而主表在 0.20。加性 baseline 不受
這道約束——`acut` 量的是局部銳利度偏差，位移場天生產生它，加性雜訊不會。

#### inpainting 另有一個第一階段沒有的問題：遮罩讓我們白付預算

inpainting 下防禦方的著力點**只在未遮罩的脈絡**（遮罩內攻擊方會整片覆寫）。
但目前：

- 位移場**對整張圖生效**，包括遮罩內（`site_warp` 與 `objective` 都
  **完全不知道遮罩的存在**，已查證）；
- `L_fid` **對整張圖收費**；
- 故遮罩內的擾動 = **零防禦價值 + 全額保真成本**。

而 PhotoGuard-c 與 PromptFlare 的原始碼**本來就把梯度乘 `(1 − mask)`**
（已忠實實作為 `grad_outside_mask`），一分預算都不會花在那裡。遮罩涵蓋率
若為 0.3，我們等於先丟掉三成預算才開始比賽——**而那是它們論文的設計，
不是我們的 bug**。

#### 兩個處置（需使用者裁決，兩者都進 `config_hash`）

**A. 門檻改為正比於 τ**

```
tau_acut   = 0.8 × τ_train    → τ=0.20 時 0.16
tau_chroma = 16  × τ_train    → τ=0.20 時 3.2
```

錨點是使用者在 τ_lpips=0.05 上的原判讀（0.04 = 0.8 × 0.05）。該規則同時
重現兩次人眼判讀：τ=0.20 時無條件觸發，τ=0.35 時 N3 的 0.343／0.355 仍
觸發——正是 `RESULTS_2026-08-06` §10.1 判為「明顯壞掉」的那一點。
**不改的話第二階段會重演第一階段。**

**B. 位移場在遮罩內歸零**

`site_warp.displacement` 乘上 `(1 − mask)`，防禦根本不去動攻擊方會覆寫的
區域。這是最貼近 PhotoGuard／PromptFlare 原生作法的形式，做完之後
「非加性 vs 加性」才真正對等——目前我們是在一個自己造成的劣勢下比賽。

實作小（`site_warp` 加一個可選遮罩、`build_module` 傳進去），但改變
`module_params` 因而進 `config_hash`；本來就是新批次，無額外成本。

> **若只做 A 不做 B**：訓練跑得到預算上限，但仍有約遮罩涵蓋率比例的預算
> 花在無效區域。若兩者都不做：預期第二階段再次得到「量不到效果」，七小時
> 機時白花。

#### 裁決與實作（2026-08-08，使用者定案：A + B 都做）

| | 實作位置 | 進 `config_hash` 的形式 |
|---|---|---|
| A | `objective.scaled_thresholds(τ)`；`run_stage.main` 在未明給時導出並印出 | `loss_params.tau_acut` = 0.16、`tau_chroma` = 3.2（τ_train=0.20） |
| B | `WarpResidual(gate=…)` 與 `WarpResidual.coarse_gate()`；`shard.sh` 的 `ip*` 帶 `--warp-mask-gate` | `module_params.warp_mask_gate`，**只在開啟時出現** |

四個實作上的選擇，各自的理由寫在程式註解裡：

1. **閘乘在粗網格的 `flow` 上，不是乘在上採樣後的位移場上。** `attention_box`
   產生的是硬邊矩形，乘在全解析度上會讓位移量在一個像素內由 `max_disp`
   跳到 0——那正是本模塊靠粗網格避掉的撕裂瑕疵。乘在粗網格上，過渡帶自然
   展開為 `size / grid_size` 個像素（512²、`grid_size=32` 下為 16）。
   `tests/test_warp_mask_gate.py` 有一條專門釘住這件事的斷言，少了它兩種
   寫法在其餘全部斷言上都通過。
2. **`phi.pt` 存粗網格上的閘（32² = 4 KB）而非整張遮罩（512² = 1 MB）。**
   `rebuild_module` 拿不到 `entry`，閘不隨 φ 落盤的話段 2／段 3 會用一個
   沒有閘的位移場，而數字仍然合理。逐格落盤且全部入版控，故取小的那個。
3. **兩個鍵都只在開啟時出現於 dict**，與 `base_config` 的 `mask` 鍵同理由：
   img2img 既有批次的雜湊必須逐位不變。有測試釘住。
4. **`preflight` 的門檻警告改判準**。before：「等於 `LossConfig` 的預設值」。
   after：「偏離 τ_train 的比例規則」。舊判準在處置 A 之後只會在 τ_train
   恰為 0.05 時觸發，而那正是唯一不需要警告的情形——判準與症狀反了。

**套用範圍是 site warp 的三個條件（N1／N2／R）。** N3 走生成路徑，其擾動經
VAE 解碼後不是逐像素定域的，無法以同一方式加閘；這項不對稱要寫進論文。

測試基準由 741 升到 **763 passed / 1 xfailed**（新增 22 條）。

**段 0 因此多一個要看的東西**：`warp_reach.csv`。閘會降低位移場可達的
LPIPS（開閘比例即 1 − 涵蓋率），而 `--warp-max-disp 8.0` 是在 SDXL/1024²
**無閘**下定出的。若 `covers_train_tau` 為 False，或 τ=0.35 這個報告點
不可達，段 2 會拋出，必須先調 `max_disp`。

### 3.5 尚未做的

- **段 0 的 `calibrate_strength` 在 inpainting 下退化為單點**（攻擊強度由
  遮罩面積決定）。行為正確，但 `STRENGTH_GRID` 的掃描變成純浪費，應改為
  量**遮罩面積**的影響——那才是 inpainting 的攻擊強度軸。
- **`purify_advantage.py` 的判準在 inpainting 下要重新確認**：它依
  `retention_usable`（`mean ≥ 3σ`）篩選，若第二階段仍然量不到效果，
  同樣會拒絕出表。那時要看的是 §6c 那種 strength／遮罩面積掃描，不是硬跑格點。
- **inpainting 的 `warp_max_disp` 尚未校準**。段 0 的 `warp_reach` 會量，
  但 512² inpainting 的可達 LPIPS 未知，可能需要調整。
- **`tau_acut` / `tau_chroma` 的門檻**仍是 τ=0.05 上定出的絕對值。建議改為
  正比於 τ（`0.8 × τ`、`16 × τ`，錨點是使用者在 τ=0.05 的原判讀），見
  `RESULTS_2026-08-07` §5.2。兩者都在 `config_hash`。
- **AdvPaint 的 `masked_image_latents` 取 sample 還是 mode 未查證**
  （`RESULTS_2026-08-07` §10.4）。目前取後驗抽樣。

---

## 4. 機器與連線

```
ssh -p 10101 nelson0314@server.basiclab.lab.nycu.edu.tw     # basic-1，8 張 RTX 3090
ssh -p 10102 …                                              # basic-2，同一個 NFS home
```

上機第一件事 `source ~/env.sh`。GPU 6 長期被他人佔用。密碼由使用者提供，
**不得寫入任何入庫檔案**。

監控（本機執行）：

```bash
bash scripts/watch_remote.sh wacv-ip1-bird_03 wacv-ip1-cat_02 wacv-ip1-dog_03
```

它現在以**逐格紀錄**判定推進，不是 log 行數——後者每 50 格才印一次，b3 一格
20 秒以上，會連續誤報「可能卡住」。

---

## 5. 資料保全

`runs/` 是唯一的證據來源。回收：

```bash
ssh … 'cd ~/wacv_runs && tar czf ~/done.tgz <目錄>'
scp -P 10101 …:done.tgz .  &&  tar xzf done.tgz -C runs/
git add -A runs/ && git status --porcelain --ignored runs/ | grep '^!!'   # 必查
```

**單一 commit 的檔案數控制在約 15000 以內。** 2026-08-07 一次 32545 個物件
的推送連續失敗兩次（先 `send-pack: unexpected disconnect`、再 HTTP 500），
拆成兩個 commit 之後兩批都順利。處置是 `git reset --soft` 再分批 commit ——
工作區與索引不動，資料完整。

**`<批次>_merged` 不必整包搬。** 它的條件目錄是各分片的逐位元複製
（`shard.sh merge` 就是 `cp -r`），分片已回收時只需傳 `grid.csv`、
`compare.html`、`attention.html`、`_cells/`、`calib/`，本機再照同一規則
複製。b3 那次由 6.5 GB 降到 1.8 GB。

最後一行是 `CLAUDE.md` 要求的。正常只該看到舊實驗 e15／e21 的 48 個
`residual.npy`。**2026-08-07 曾在此查出一個誤留的 `runs/base.tgz`**——那是
拉檔時的暫存壓縮檔，被 `.gitignore` 靜默排除，正是這道檢查要抓的東西。


---

## 6. 第一階段的產物清單（全部已入版控）

| 路徑 | 內容 |
|---|---|
| `runs/v14`、`runs/v14_{bird_03,cat_02,dog_03}` | SD v1.4 / 512² 的段 0–3 |
| `runs/v14_merged` | 2940 列 `grid.csv`、**`compare.html`**、`attention.html` |
| `runs/b3`、`runs/b3_{bird_03,cat_02,dog_03}` | SDXL / 1024² 的段 0–3 |
| `runs/b3_merged` | 同上，SDXL |
| `runs/v14_bird_03/strength_sweep_defense.csv` | strength 掃描（原生強度） |
| `runs/v14_bird_03/strength_sweep_tau0.2.csv` | strength 掃描（匹配預算、10 seed） |
| `runs/tau_threshold_probe_v14.csv` | 兩道 hinge 在 τ=0.20 上的逐條件實測 |

**實驗報告頁**（架構圖、變因說明、真實影像對照、圖表、與參考論文的全部差異）：
`https://claude.ai/code/artifact/4eaadbf2-3c2d-45d2-8a2e-b1c83b01d2d9`

---

## 7. 第一階段查出並修掉的六個缺陷

全部在 GPU 上驗證過。列在此供第二階段比對——其中三個是「不會報錯」的那一類。

| # | 缺陷 | 為什麼危險 | commit |
|---|---|---|---|
| 1 | `lr_probe.csv` 遺漏 `diverged` 欄 | 發散的事實進不了報告 | `0a55715` |
| 2 | mist／dia_r 的 VAE 呼叫未 checkpoint | 1024² 下兩個 baseline 全部 OOM | `4cccee7`、`7441ce8` |
| 3 | `shard.sh` 的 `[exit 0]` 掩蓋失敗 | **監控把失敗回報成「收工」**，輪詢的一方會停止關注 | `7441ce8` |
| 4 | 生成路徑下限是全域常數 | 逐圖差很多（0.133–0.240），cat_02 的兩個分片卡死 | `7ce8598` |
| 5 | AdvPaint 的 GT 與迭代走不同前向 | **損失量到的是路徑差異而非擾動效果，曲線照樣會下降** | `c208371f` |
| 6 | PromptFlare 的 `grad_outside_mask` 判為 False | **擾動會花在攻擊方整片覆寫的區域，無症狀** | `5da4c937` |

第 3、5、6 項都是「跑起來很正常、數字也合理」的那一類。第二階段若出現
「結果看起來合理但說不出所以然」，先回頭查這三類型態。


---

## 8. 階段一重做（v14r）—— 2026-08-08 交接

**本節給接手 v14r 的 session。第二階段（inpainting）由另一個 session 負責，
不要動它的批次與卡。**

### 8.1 第一件事：搬到新分支

目前兩個 session 共用 `claude/e20-fidelity-constraint` 這一條分支與**同一個
工作目錄**，實際造成過三種干擾（見 `RESULTS_2026-08-08` §3）：對方的 commit
出現在自己的 `git log`、對方未提交的編輯使完整 `pytest` 出現非回歸的失敗、
`shard.sh` 被雙方各自修改。

接手後**先開新分支**：

```bash
git checkout -b claude/stage1-redo-v14r      # 由目前的 HEAD 分出
```

之後只 push 自己這一條。若要更徹底，用 `git worktree add` 另開一個工作目錄
——共用工作目錄沒有任何機制能防止互相污染。

**已知的既有狀態**：`src/defense/optimize.py` 與 `tests/test_inpaint.py` 可能
帶有另一個 session 的編輯中內容。commit **一律用明確路徑**，不要 `git add -A`。
測試門檻用 `pytest tests/test_grid.py tests/test_objective.py
tests/test_executors.py`（171 passed），完整 pytest 的 `test_inpaint.py`
失敗屬對方 WIP。

### 8.2 正在跑什麼

| 項目 | 內容 |
|---|---|
| 批次 | `v14r`（SD v1.4 / 512² / fp32） |
| 變因 | **strength 0.4**（原 0.6）、**tau_acut 0.16**、**tau_chroma 3.2**（原 0.04 / 0.8） |
| 段 0 | **已完成**：03:58–08:51（4 h 53 m），`[exit 0]`。結果見 `RESULTS_2026-08-08` §4 |
| 段 1–2 | **已完成**：09:51–13:32，三個分片全部 `[exit 0]`、零失敗格。結果見 `RESULTS_2026-08-08` §6 |
| 段 3 | **已完成**：14:02–17:54，三個分片 `[exit 0]`、零失敗格 |
| 合併 | **已完成**：3330 格，`grid.csv` 2940 列。**結論見 §8.6 與 `RESULTS_2026-08-08` §7** |
| 格點 | 3294 格（train 21 / rayscale 84 / control 285 / eval 2940） |
| 預估 | 段 0 約 4.8 h（實測 4.9 h）、段 1–3 三卡平行約 7.5 h |

段 0 跑完接。**注意 `fanout` 預設一口氣跑完 train／rayscale／eval**，而
§8.3 第 3 項要求在段 2 之後停下來，故本輪用 `STAGES` 覆寫分兩次跑
（該覆寫於 commit `19a70c5b` 加入，預設值即原本寫死的三段）：

```bash
STAGES="train rayscale" BATCH=v14r bash scripts/shard.sh fanout bird_03 cat_02 dog_03
# —— 人眼判讀 τ=0.20 的 x_def ——
STAGES="eval"           BATCH=v14r bash scripts/shard.sh fanout bird_03 cat_02 dog_03
BATCH=v14r bash scripts/shard.sh merge  bird_03 cat_02 dog_03
```

監控（本機執行）。`shard.sh calib` 的 session 命名可能已被另一個 session
改過，先 `tmux ls` 確認實際名稱：

```bash
bash scripts/watch_remote.sh calib:v14r          # 段 0（log 指向 v14r，session 仍查 wacv-calib）
bash scripts/watch_remote.sh wacv-v14r-bird_03 wacv-v14r-cat_02 wacv-v14r-dog_03
```

### 8.3 跑完之後要看什麼（順序不可換）

1. **段 0 的 `lr.N2` 是否仍落在 0.02–0.1。** 門檻放寬改變了損失地景，探測
   結果與第一階段不同是**預期的**，不要當成異常——那正是重跑段 0 的目的。
   → **已通過**（`lr.N2 = 0.1`，與前兩批相同）。全部四個條件的探測結果與
   兩點附帶記錄見 `RESULTS_2026-08-08` §4.2。
2. **段 1 的 `train.csv`，`fid_acut` 的最大值。** 第一階段精確頂在
   0.0401–0.0404。本批若改頂在 **0.16**，代表約束仍綁定、只是位置移了；
   要看到末端 `fid_lpips` 接近 0.20 而 `fid_acut` 明顯低於 0.16，才是真的解開。
   → **已通過，且是完全反轉**：六個位移場格的 `max fid_acut` 為 0.0398–0.1036、
   **被 acut 罰過的格數 0/6**（v14 是 6/6），六格的 `max fid_lpips_rel` 全部
   到達 0.2002–0.2012。**N3 是例外**，其失效機制不同（早停於防禦損失，不是
   保真門檻）。完整逐格表與兩個新觀察見 `RESULTS_2026-08-08` §6.2–6.4。
3. **τ=0.20 的 `x_def` 先用人眼看過再跑段 3。** 放寬門檻買到訓練自由度，
   代價可能是同一 LPIPS 下可見失真變差；使用者先前判「τ=0.20 可接受」是在
   **受約束的 φ** 上做的。段 1 約 1 h（**實測約 3 h**，成本集中在
   `photoguard_c` 那一格）、段 3 要 7 h，**值得先看再決定**。
   → **已通過**（2026-08-08 14:00，使用者於 `runs/v14r_vs_v14_tau0.2.html`
   上判讀）：「位移場類型的失真較大，但**提高門檻前後的差距看不太出來**；
   apa 提高門檻後的失真人眼看依舊非常不明顯。」即放寬門檻買到了訓練自由度
   而未付出可見失真的代價。段 3 據此於 14:02 起跑。判讀的兩項衍生記錄
   （LPIPS 的不可通約性是逐 site 而非「加性 vs 非加性」；apa 仍有預算餘裕）
   見 `RESULTS_2026-08-08` §6.6。

### 8.4 已裁決、不要再重開的事

- **ST-LPIPS 不採用**（3.67× 分離，判準事前宣告）。細節見
  `RESULTS_2026-08-08` §1。
- **不要再找「公正的失真指標」**。五個指標一致顯示兩類在同一 LPIPS 上的
  人眼可辨失真本來就不相等，那是實情不是度量瑕疵。LPIPS 維持為運作約束。
- **`RESULTS_2026-08-07` §6 的方向已更正**：LPIPS 對非加性是**寬鬆**而非
  嚴苛。論文若引用該論點必須用更正後的方向。
- **重做只跑 v14，不跑 b3**：SDXL 的攻擊在 strength 0.6 的區間只有 0.038，
  降到 0.4 會趨近於零。
- **處置 B（遮罩內歸零）不適用於本重做**——img2img 沒有遮罩。

### 8.5 一個尚未處理的潛在缺陷

`Calibration.REQUIRED_CONTEXT` 是 `(model, resolution, guidance, steps, gpu,
precision)`，**不含 `strength`**，而學習率是**透過該 strength 的編輯鏈**探測
出來的。換 strength 而沿用校準表不會觸發 `CalibrationMismatch`。要嘛把
`strength` 加進 `REQUIRED_CONTEXT`，要嘛每次換 strength 都明確重跑段 0
（本批採後者）。

### 8.6 v14r 的結論（2026-08-08 18:00，本批已完整結束）

段 0–4 全部完成，零失敗格，證據全部入版控。**兩個變因的結果相反。**

| | 結論 | 依據 |
|---|---|---|
| **確立** | `tau_acut` 0.04 → 0.16 **確實解開了訓練約束** | 被 acut 罰過的格數 **6/6 → 0/6**，六個位移場格全部到達 LPIPS 0.20（v14 有五格達不到）。`RESULTS_2026-08-08` §6.2 |
| **確立** | 放寬門檻**沒有**付出可見失真的代價 | 使用者人眼判讀，§6.6 |
| **否定** | strength 0.6 → 0.4 **不是**對的處置 | 四個 τ × 七個條件共 28 格，**沒有任何正效果達到 t ≥ 2**；三個加性 baseline 也一樣。§7.3 |
| **仍無法判定** | 主張一（抗淨化） | `retention_usable` 為 0 列，第五次拒絕出表。§7.2 |

**最重要的一項**：根因 1（「strength 選錯點」）的唯一證據
`RESULTS_2026-08-07` §6c.2 經複核**本來就落在雜訊裡**——Mist +0.0145 的
標準差是 **0.0326**（t=1.41），PhotoGuard-c +0.0134 的標準差是 **0.0257**
（t=1.65），而該表**只列了均值**。詳見 `RESULTS_2026-08-08` §7.4。

**下一輪不要再調 strength。** 0.3／0.4／0.5／0.6 都量過了，沒有任何一點
產生可與雜訊區分的正效果。問題不在工作點的位置。

**兩個根因必須分開評價**：本輪修好了一個真缺陷（`tau_acut`），同時證偽了
另一個（strength）。修好的那一個要保留進第二階段——`objective.scaled_thresholds`
已是現行行為，`ip*` profile 也已帶著它。

---

## 9. 第二階段（inpainting）—— 2026-08-08 04:30 現況

**本節給接手 inpainting 的 session。第一階段重做（v14r）由另一個 session
負責，見 §8——不要動它的批次（`v14r`）與卡（GPU 0）。**

分支仍是 `claude/e20-fidelity-constraint`，與 v14r 共用（§8.1 建議由 v14r
那一側移出）。commit 一律用明確路徑，不要 `git add -A`。

### 9.1 正在跑什麼

| 項目 | 內容 |
|---|---|
| 批次 | **`ip3`**（SD inpainting / 512² / fp32） |
| 影像 | **`bird_02 cat_02 horse_02`**（不是第一階段那三張，理由見 §9.3） |
| 段 0 | 2026-08-08 04:08 起於 **GPU 1**，tmux `wacv-ip3-calib`，log `~/wacv_runs/ip3/calib.log` |
| 格點 | 3294 格（train 21 / rayscale 84 / control 285 / eval 2940） |
| 遮罩涵蓋率 | bird_02 0.401、cat_02 0.314、horse_02 0.275 |

段 0 跑完接（**先看完 §9.4 再跑**）：

```bash
BATCH=ip3 bash scripts/shard.sh fanout bird_02 cat_02 horse_02
BATCH=ip3 bash scripts/shard.sh merge  bird_02 cat_02 horse_02
```

監控（本機執行）：

```bash
bash scripts/watch_remote.sh wacv-ip3-bird_02 wacv-ip3-cat_02 wacv-ip3-horse_02
```

`ip1`／`ip2` 是同一天的前兩次嘗試，**都沒有跑出結果**，只留下遮罩證據
（`runs/ip1_maskprobe/`、`runs/ip2_maskprobe2/`、`runs/ip2_maskprobe3/`）。

### 9.2 §3.4a 的兩個處置都已實作並生效

裁決與實作細節見 §3.4a 末的「裁決與實作」。上機驗證到的兩點：

- 段 0 的 log 第一行印出 `[thresholds] tau_acut = 0.16 / tau_chroma = 3.2
  （由 τ_train=0.2 依比例導出）`，即處置 A 生效且值進了 `config_hash`。
- 處置 B 的 `--warp-mask-gate` 在 `ip*` profile 內，只作用於 site warp
  的三個條件（N1／N2／R）。

### 9.3 遮罩：三次嘗試才定案，中間兩次的證據都留著

| 批次 | 設定 | 涵蓋率 | 為什麼不用 |
|---|---|---|---|
| ip1 | `attention_box` τ0.5、原三張圖 | 0.483 / 0.314 / **0.875** | dog_03 超過 0.6 停止線。疊圖確認**對位正確**——那隻狗本身佔滿畫面 |
| ip2 | `attention` τ0.5、原三張圖 | 0.058 / 0.143 / 0.360 | 數字合格但**疊圖不合格**：破碎斑塊而非物件剪影，dog_03 還蓋到背景 |
| **ip3** | `attention_box` τ0.5、**改選圖** | 0.401 / 0.314 / 0.275 | 採用 |

選圖是**規則**不是挑選：涵蓋率落在 [0.15, 0.45]、每類取一張，並以疊圖確認
框含住物件。判準必須在批次實際使用的 seed 上量——遮罩由一次加噪前向決定，
`dog_02` 在 seed 0 與 7 下量到 0.4475 與 0.8750，差一倍。全 24 張在
`--seed 20260805` 上的結果在 `runs/ip2_maskprobe3/coverage_seed20260805.csv`：
**人像 8 張全部 0.92–1.00，整組不可用**。

### 9.4 段 0 跑完要看的（取代 §3.2a）

1. **`<批次>/masks/*_overlay.png`**——已確認過一次（涵蓋率如上），但仍要看
   一眼落盤的那三張與探測時是否一致。
2. **`calib/warp_reach.csv` 的 `lpips_at_bound`**。這是本批**新增的風險**：
   處置 B 的閘使位移場只能動 `1 − 涵蓋率` 的面積（0.60–0.72），而
   `--warp-max-disp 8.0` 是在 SDXL/1024²、**無閘**下定出的。判準是
   `TAUS` 最大的那點 0.35 必須可達，**不是** `--tau-train`。達不到就要調
   `max_disp` 並重跑段 0。
3. **`lr.N2` 是否落在 0.02–0.1**（前兩批都選中 0.1）。門檻由 0.04 放寬到
   0.16 改變了損失地景，結果與第一階段不同是**預期的**。
4. **`calib_summary.json` 的 `strength`**——inpainting 下該掃描退化為單點，
   行為正確，但別把建議值當真。

### 9.5 本階段查出並修掉的三個缺陷

| # | 缺陷 | 症狀 | commit |
|---|---|---|---|
| 1 | `_build_attn_step` 用 `cfg.strength`，inpainting 下是 `None` | 段 0 第一次上機即 `TypeError`，N1 的注意力前向**從未在 inpainting 下跑過** | `b0c15c2a` |
| 2 | 同一函式只餵 4 通道，而 inpainting UNet 是 9 通道 | 修好第 1 項之後才會出現的第二個 | 同上 |
| 3 | `shard.sh calib` 的 tmux session 名不帶批次 | 兩個批次的段 0 不能並存；v14r 佔著 `wacv-calib`，ip2 起不來 | `65eae9b1` |

第 3 項的訊息只說「名字重複」，看不出是另一個批次佔著；呼叫端若把輸出接進
pipeline 連退出碼都收不到。`set -e` **確實**在該處中止（實測 rc=1），故它不是
靜默失敗——但若 `set -e` 沒生效，其後的 `tmux has-session` 會拿別的批次的
session 判定為成功，那才會是 §7 第 3 項那一型。名字帶批次之後兩條路都不成立。

### 9.6 尚未處理

- **`Calibration.REQUIRED_CONTEXT` 不含遮罩設定**，與 §8.5 的 `strength`
  同一型：換遮罩而沿用校準表不會觸發 `CalibrationMismatch`。目前靠「每個
  批次各自跑段 0」迴避。
- **N3 沒有加閘。** 它走生成路徑，擾動經 VAE 解碼後不是逐像素定域的，
  無法以同一方式歸零。這項不對稱要寫進論文。
- §3.5 的四項仍然成立。

### 9.7 段 1 的三張圖：涵蓋率之外還要過可編輯性（2026-08-08 09:20 定案）

第一次段 0（04:08–06:43）跑到 N3 的階段一才死，但它留下的
`editable_filter.csv` 顯示 **cat_02 的編輯效果是負的**（SigLIP −0.028）：
在那個遮罩下 inpainting 攻擊反而讓影像離 prompt 更遠。在編輯本來就無效的
影像上量免疫效果沒有意義——那正是第一階段「分母在雜訊裡」的成因。
`filter_editable` 只在**全部**影像都不通過時才停，單張不通過會照跑。

處置：段 0 直接跑**涵蓋率窗內的全部六張**，讓可編輯性也成為選圖判準，
段 1 再取三張。學習率只探測第一張（`calibrate_lr` 取
`next(iter(res.images.values()))`），故成本只多了逐影像的那幾項。

| 影像 | 涵蓋率 | 可編輯性 SigLIP | 通過 | `warp_reach` |
|---|---|---|---|---|
| **bird_02** | 0.401 | **+0.0283** | ✓ | 0.364 |
| cat_02 | 0.314 | −0.0279 | ✗ | 0.434 |
| **horse_02** | 0.275 | **+0.0346** | ✓ | 0.439 |
| **cat_01** | 0.234 | **+0.0471** | ✓ | 0.456 |
| horse_01 | 0.234 | −0.0389 | ✗ | 0.446 |
| horse_00 | 0.205 | +0.0415 | ✓ | 0.390 |

**段 1／2／3 用 `bird_02 cat_01 horse_02`**（每類一張）。horse 這一類取
horse_02 而非效果更高的 horse_00：後者的框落在畫面左半、馬在右緣外，它的
「編輯效果」（CLIP +0.0906，全表最高）多半來自把背景重畫成馬的紋理，而不是
改到那匹馬。取它會讓分母虛高，是**對我方有利但站不住**的選擇。

`cat_02` 因此掉出，本階段與第一階段**沒有共同影像**。可以接受：威脅模型的
工作點整個換掉了，逐圖對照本來就不成立。

段 0 的六張全部留在 `calib/` 裡照實報告，不挑選。

**`warp_reach` 的判準已通過**：加閘之後六張都 ≥ 0.35，`TAUS` 最大那點可達。
bird_02 的 0.3638 只多 4%，最緊；該量測用的是 R（隨機位移），而先驗實測
「同振幅下最佳化解的可辨失真是隨機的 2–3 倍」，故 N1／N2 更寬鬆。

### 9.8 共用工作目錄的實際干擾

另一個 session 依 §8.1 把工作目錄切到 `claude/stage1-redo-v14r`，而**分支是
整個工作目錄共用的**：本 session 其後的兩個 commit 因此落在那條分支上，
`git push origin claude/e20-fidelity-constraint` 推的是本地那條**還停在舊位置**
的分支，於是回傳 0 卻什麼都沒送出去（`Everything up-to-date`），遠端
`git pull` 也就「Already up to date」。症狀是遠端跑的仍是舊碼。

處置：本 session 之後一律用明確 refspec 推

```bash
git push origin HEAD:claude/e20-fidelity-constraint
```

並在遠端 `git log --oneline -1` 核對 SHA，不看 push 的回傳碼。

### 9.9 段 0 的結果與段 1–3 的起跑（2026-08-08 14:12）

段 0 於 08:54–13:5x 執行（約 5 小時），`[exit 0]`。

| 校準項 | 值 | 判準 |
|---|---|---|
| `lr.N1` | 0.1 | — |
| `lr.N2` | **0.1** | 落在 §9.4 的 0.02–0.1 內（前兩批亦選中 0.1） |
| `lr.N3_stage1` | 0.0001 | 五個候選中最小的；0.1 那個發散 |
| `lr.N3_stage2` | 0.005 | — |
| `stop_tol.edit_shift` | 2.486e-04 | — |
| `stop_tol.shared_mass` | 3.292e-06 | — |
| `warp.min_lpips_at_bound` | 0.3638 | `covers_train_tau: true`，τ=0.35 可達 |
| 可編輯性 | 6 張過 4 張 | — |
| `strength.recommended` | 0.3 | **不採用**，inpainting 下該掃描退化為單點 |

`lr.N3_stage1` 的五個候選中，0.1 在第 50 步使 `x_gen` 出現非有限值而中止
（`lr_probe.csv` 的 `diverged=True`），0.02 末端損失衝到 1411.6 但仍為有限，
故未標 diverged。兩者都由 `align` 還原第 0 步的 φ。選中最小的 0.0001 代表
**階段一幾乎不動**——段 1 的 `align` 曲線要看它是否達得到該影像自己的重建
下限，達不到就是容量天花板而非步數不足（E9 的既有結論）。

段 1–3 於 14:12 起跑：`bird_02` → GPU 0、`cat_01` → GPU 4、`horse_02` → GPU 5，
tmux `wacv-ip3-<影像>`。同時間另一 session 的 `v14r` 三片在 GPU 1/2/3。

起跑後五分鐘的三片 N1 訓練都在下降，且 **`L_fid` 為 0.0000**——保真約束在
前 30 步完全沒有啟動。第一階段是**一開始就頂死**在 `tau_acut=0.04` 上的，
這是處置 A 生效的第一個跡象。**但這還不是結論**：要看的是段 1 結束時
`train.csv` 的 `fid_acut` 最大值與末端 `fid_lpips`——

- `fid_acut` 若精確頂在 **0.16**，代表約束仍綁定、只是位置移了；
- 要看到末端 `fid_lpips` 接近 0.20 而 `fid_acut` 明顯低於 0.16，才是真的解開。

### 9.10 加寬的學習率探測：格點上緣不是瓶頸（2026-08-08 18:24–20:15）

**起因**：段 0 選中的 `lr.N1` 與 `lr.N2` **都是 0.1**，而候選格點
`[1e-4, 1e-3, 5e-3, 2e-2, 1e-1]` 的上緣就是 0.1——argmin 落在邊界通常代表
格點不夠寬。前兩批也都選中 0.1，同一型態出現三次。

同時，段 1 的 N1／N2 停在 LPIPS 0.103–0.178（主表在 0.20），而末段的每步
改善量**已低於平台停止的容差**：

| 影像 · 條件 | 監看量 | 前 50 步／步 | 末 50 步／步 | 容差 |
|---|---|---|---|---|
| bird_02 · N1 | shared_mass | 2.95e-05 | **2.77e-06** | 3.29e-06 |
| horse_02 · N1 | shared_mass | 5.07e-05 | **4.96e-06** | 3.29e-06 |
| bird_02 · N2 | edit_shift | 4.75e-04 | **7.22e-05** | 2.49e-04 |
| horse_02 · N2 | edit_shift | 1.29e-03 | **1.24e-04** | 2.49e-04 |

也就是說**它們早就收斂了**。之所以還跑滿 250 步，是因為平台停止有一個前置
條件「約束必須至少啟動過一次」，而這兩張圖的失真從頭到尾沒碰到任何門檻，
`engaged` 恆為 False，停止準則永遠不會觸發。不是「還在進步」，是「不動了
但停止準則沒資格開口」。**延長步數不會有用。**

**探測方式**（一次性診斷，程式不入版控、跑完刪除）：把 `run_stage` 的段 0
入口換掉、內部仍呼叫 `executors._probe_lr`，其餘旗標與 `ip*` profile 逐字
相同（遮罩涵蓋率同為 0.401，可驗證），故與 `ip3/calib/lr_probe.csv` 的
0.1 那兩列可直接比。影像 bird_02，60 步。

| 條件 | lr | 末端 LPIPS | `L_def` | `L_fid` |
|---|---|---|---|---|
| N1 | 0.1 | — | 0.05654 | 0 |
| N1 | **0.3** | 0.151 | **0.05582** | 0 |
| N1 | 1.0 | 0.2026 | 0.05883 | 0.2617 |
| N1 | 3.0 | 0.2025 | 0.05918 | 0.2512 |
| N2 | 0.1 | — | 0.80864 | 0 |
| N2 | **0.3** | 0.138 | **0.80130** | 0 |
| N2 | 1.0 | 0.1993 | 0.82880 | **0** |
| N2 | 3.0 | 0.1986 | 0.85205 | **0** |

**結論一：0.3 略優於 0.1，但幅度在 1% 上下**（N1 −1.3%、N2 −0.9%），
不足以解釋「非加性量不到效果」。

**結論二：在固定 60 步下，把失真用滿預算會讓防禦變差。** N2 的 lr=1.0 與
3.0 是最乾淨的對照——`L_fid` 恰為 **0**，即失真停在 0.1993／0.1986 而未違規
——而防禦項比 lr=0.3 差 3.4% 與 6.3%。N1 同向。

**結論三（判準上的偏差，須記錄）**：`_pick_best` 取 `L_def + L_fid` 最小者，
而 `gamma_lpips = 100` 使「超出預算 0.0026」要付 0.26 的懲罰——比 N1 的整個
`L_def`（0.056）還大四倍。60 步的探測因此**系統性偏好停在預算內的學習率**，
而正式訓練有 250 步、有時間把失真拉回邊界內讓懲罰歸零。這一點不改變上面
兩個結論（N2 的兩個高 lr 候選 `L_fid` 本來就是 0），但改格點時要記得。

**尚待確認的混淆項**：大學習率同時做了兩件事——走得更遠（用到預算）與
**在固定步數內收斂得更差**。60 步分不出是哪一個。主批次提供了另一個錨點：
bird_02 的 N2 在 lr=0.1、250 步下 `L_def = 0.79780`（LPIPS 0.1028），優於
任何 60 步結果。故決定性的一問是「lr=1.0 跑滿 250 步會怎樣」，已於 20:2x
起跑於 GPU 1（`~/wacv_runs/ip3lr250/lr_wide250.csv`，N2 的 lr=1.0 與 0.3）。
