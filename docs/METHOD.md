# 本方法：紋理重相位

程式：`src/residual/texture_rephase.py`（算子）、`src/defense/param_pgd.py`
（參數化與最佳化迴圈）。

## 構造

把影像切成重疊區塊、加窗做 FFT，旋轉相位並乘上可學的幅度增益，再重疊相加
回像素域。允許的幅度由兩個由原圖決定的閘給出，其中頻率閘按**人眼的可見度
定價**而不是二值放行：

```
x_def = OLA( irfft2( rfft2(w·P_b) · exp(i·g_b·m_ω·θ_b) ) · w ) / OLA(w²)
```

512² 上：32×32 區塊、hop 16、Hann 窗，共 1089 個區塊 × 32×17 個頻格，
約 59 萬個參數（同尺寸的加性 δ 是 78.6 萬）。

### 三個由構造保證的性質

1. **`θ = 0` 時輸出逐位元等於原圖**（float64 誤差 5.6e-16）。由 `OLA(w²)`
   正規化保證，該式是 Griffin & Lim (1984) 的最小平方解，依賴 NOLA 而非 COLA。
   換窗型或換 hop 都不會靜默破壞這個恆等。
2. ~~**幅度譜逐位保留**~~：可學幅度增益定案開啟後，此性質**不再成立**。
   相位仍是乘上模長為 1 的複數，但幅度另乘一個可學的 `exp(g)`。
   新穎性主張隨之由「只旋轉相位」改寫為**頻譜重參數化**。
3. **輸出為實數**：`fx = 0` 與 `fx = N/2` 兩行的閘一律取 0，避免破壞
   rfft2 的共軛對稱。

### 兩個閘

兩者都由**原圖**算一次即固定，**不參與最佳化**。閘決定「擾動被允許出現在
哪裡」，不改變損失或更新規則。

| 閘 | 是什麼 | 效果 |
|---|---|---|
| `g_b` 紋理閘 | 結構張量 coherence 導出的紋理度，`(1−coh²)^p·clamp(energy/ref)` | 邊緣（coherence 高）與平坦區（能量低）皆為 0 |
| `m_ω` 徑向頻率閘 | 帶通遮罩 × **知覺權重**：`(r ∈ [r_min, r_max]) · w(ω)^γ`，`w` 是 ITU-T T.81 Annex K 亮度量化表按頻率重採樣到 rfft2 格點並正規化 | 低頻帶著區塊的位置與結構，動它會在重疊相加後產生接縫；而通帶內每一格的價錢不同——人眼對 r=0.15 的敏感度比 r=0.9 高一個數量級，二值閘對兩者開同一個價 |

## 定案參數

| 參數 | 值 | 說明 |
|---|---|---|
| `block` | 32 | 64 已測，同失真下較差 |
| `hop` | 16（`block/2`） | 決定殘差的紋理有多粗。更小的 hop 讓每個像素被更多區塊覆蓋，接縫被平均掉，**待測** |
| `r_min` | 0.12 | 放行低頻已測，未淨化與抗淨化兩邊都較差 |
| `r_max` | ∞（高通） | 帶通已測，`0.7` 時擋下率 1/9，已否決 |
| 紋理閘分位數 | 0 | 使能量因子恆為 1。**邊緣因子仍在**，故不是「閘全開」。0.5 的對照只在舊損失／二值閘／100 步上做過，現行設定下**待測** |
| `gate_edge_power`（`p`） | 1.0 | 0 與 0.5 在同 radius 下失真較高但擋下率也較高，等失真對照**待測** |
| `θ_max` | 由強度旋鈕決定，**封頂在 π** | 相位是週期量 |
| `gl_iters` | 0（關閉） | 開啟已測，降低可達上限 |
| `pixel_gate_sigma` | 0（關閉） | 逐像素閘已測，同失真下防禦掉 16% |
| `gain_ratio` | **> 0（定案開啟）** | 同失真下兩軸皆正；唯一能拆掉相位週期性天花板的旋鈕 |

## 可調旋鈕

全部集中在 `PhaseParam.__init__` 與 `scripts/ip2p_run.py` 的旗標。
每一個都會逐列寫進結果 CSV。

| 旗標 | 作用 | 現況 |
|---|---|---|
| `--radius` | 強度。同時驅動 `θ_max`（封頂 π）與 `gain_max`（不封頂） | 主要旋鈕 |
| `--r-min` / `--r-max` | 徑向頻率閘的下界與上界 | 已否決 `r_max` 上界 |
| `--quantile` | 紋理閘的能量參考分位數。0 = 該因子恆為 1 | 待測 |
| `--gate-edge-power` | `(1−coh²)` 的指數。0 = 不壓制邊緣 | **待測**：0.5 在較高失真上拿到 11/13，等失真對照未做 |
| `--steps` | 防禦端的 PGD 步數。**DCT-Shield 走 `--dct-steps`，預設 1000** | 預算差十倍，待測 |
| `--block` | 區塊大小 | 已否決 64 與 16 |
| `--hop` | 重疊步長。決定殘差的紋理有多粗 | **待測**：只在含加性項的探針裡量過，該批不可引用 |
| `--gain-ratio` | 可學幅度增益的上界對半徑的比例 | **定案開啟** |
| `--gl-iters` | Griffin–Lim 迭代投影輪數 | 已否決 |
| `--pixel-gate-sigma` | 逐像素紋理閘的高斯 σ | 已否決 |
| `--loss` | `encoder_target` 或 `latent_norm` | **定案 `latent_norm`** |
| `--freq-weight` | 頻率閘的知覺定價（`binary`／`jpeg_luma`） | **定案 `jpeg_luma`** |
| `--freq-weight-power` | 定價力道 `w ** gamma`，0 = 退回二值閘 | 0.35 在帶內優於 0.25，**待補等失真掃描再定案** |
| `--gain-weight` | 增益的閘（`shared`／`jnd`） | 已否決 `jnd` |
| `--phase-channels` | 動哪些通道（`rgb`／`y`） | 已否決 `y` |
| `--purify-aware` | 把可微分淨化算子放進最佳化迴圈 | 已否決 |

## 構造上的三個限制

這三條決定了方法能做到什麼、做不到什麼，是所有改良提案的前提。

1. **擾動的振幅不是自由變數。** 幅度譜逐位保留 ⇒ 平坦區的 AC 幅度接近零 ⇒
   相位怎麼轉都轉不出東西。**「把擾動鋪到平坦背景」在這個構造下做不到**，
   除非在頻譜上加一項，而那等於退回加性。
2. **相位是週期量 ⇒ 失真有天花板。** θ 加到 π 就繞回去。這是構造不出「等
   LPIPS 錨點」的直接原因。可學幅度增益能拆掉這一條。
3. **重疊相加會部分抵銷。** 相鄰區塊的相位各自獨立旋轉，方向不一致時在
   相加時互相抵銷；付出的失真留下了，淨效果被削掉一部分。

## 內部對照組

不是獨立方法，是本方法的消融。

| 條件 | 是什麼 | 回答什麼 |
|---|---|---|
| `phase_rand` | 同一個算子，相位偏移隨機取樣、不經最佳化（即 RPN） | 效果來自最佳化還是來自「動了相位」本身 |
| `add` | 加性 δ 走同一個 encoder-target 損失 | 參數化本身的貢獻 |
| `gain_only` | 凍結相位、只學幅度增益 | 幅度能不能取代相位 |
| `phase_gain` | 相位與幅度都學 | 兩者是相加還是互相抵銷 |

## 文獻定位

訊號處理骨架全部是教科書內容：切塊、加窗、FFT、重疊相加是 STFT
（Allen & Rabiner 1977），`OLA(w²)` 正規化是 Griffin & Lim (1984) 的最小平方
解，隨機相位保留紋理外觀是 Random Phase Noise（Galerne et al. 2011）。

**新穎性主張的邊界**：不能宣稱首次把相位擾動用於對抗攻擊，也不能宣稱首次做
「非加性的頻域操作」（AdvDrop 是前例）。可主張的是**加窗重疊區塊的頻譜相位
旋轉 ＋ 兩個由原圖決定的閘 ＋ 擴散編輯防護 ＋ 抗淨化評測**這個組合。

逐篇出處見 [reference/BIBLIOGRAPHY.md](reference/BIBLIOGRAPHY.md)。
