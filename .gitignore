# 第三方論文 PDF（本機參考用，不入庫）
refs/

# 模型權重
*.ckpt
*.safetensors

# 實驗輸出：預設不追蹤，但**結果本身要進版控**。
# TWCC 容器會被刪除，runs/ 的數據與圖是唯一的證據來源，一律入庫。
#
# 2026-07-31 修正：原規則第三行為 `runs/*/**`，它會排除逐圖子目錄本身
# （例如 runs/e15_S_tau0.05/car_00__S__r32/）。git 一旦排除某目錄就不再
# 遞迴進入，後面的 `!runs/**/*.json` 因此永遠不會生效，導致 143 個
# history.json / spectrum_rank.json 被靜默漏掉。改法是先用 `!runs/**/`
# 把「所有層級的目錄」重新納入，讓 git 得以遞迴，再由副檔名決定檔案。
runs/**
!runs/**/
!runs/**/*.csv
!runs/**/*.json
!runs/**/*.md
!runs/**/*.log
!runs/**/*.png
# 2026-07-31 追加 *.html 與 *.txt：E20 的人眼比對頁
# （runs/p1_iso_lpips_probe/compare.html）是使用者判定的依據，各腳本的
# summary.txt / report.txt 是判定結果的可讀形式，兩者都是證據而非中間產物。
!runs/**/*.html
!runs/**/*.txt
# 2026-08-04 追加 *.pt：`gate_suppress.py` 存下每一格的 φ（state_dict）。
# 它不是中間產物——沿射線縮放位移場需要 φ 本身，只有防禦圖 PNG 的話
# 就只能在影像空間做，而那對 site S 是交叉淡入不是「更大的位移」
# （見 src/metrics/ray_scale.py::solve_k）。體積：site S 的位移場
# 32×32×2 約 8 KB，site PF 的全秩像素殘差在 256² 約 786 KB。
#
# 逐圖的 residual.npy 仍然排除：那是低秩外積展開後的結果，可由 φ 重算，
# 且 e8 那一批單目錄就 24 MB。兩者的差別是「能不能重建解」而不是副檔名。
!runs/**/*.pt

# Python
__pycache__/
.pytest_cache/
.ipynb_checkpoints/

# 資料集的候選池。29 MB，且完全可由 scripts/fetch_cc0_images.py 重新取得
# （固定 seed 的打散 + 固定查詢詞）。實際採用的影像在 data/_selected/
# 與 data/lo_aligned/，兩者都入版控並帶出處紀錄。
data/_raw/
