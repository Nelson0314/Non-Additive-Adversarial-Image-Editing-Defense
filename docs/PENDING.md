# 待裁定

**這一頁不是結論。** 每一筆都是量過但**尚未進入 [RESULTS.md](RESULTS.md) 或
[DECISIONS.md](DECISIONS.md)** 的東西，等使用者裁定之後才搬過去或刪掉。
證據路徑一律相對於 `runs/`。

## 〇、一個影響所有比較的量測事實：主讀數在 0.70 附近飽和

52 個 13 張的工作點裡，位移的**最大值是 0.7078**，前十名全部落在
0.6997–0.7078 這 1.2% 的區間內，橫跨四個參數化與 0.098–0.182 的失真範圍。

最乾淨的證據是純相位＋下限那組（`radius = π` 固定，只掃 `spectral_floor`）：

| floor | DISTS | PSNR | 位移 | 擋下 |
|---|---|---|---|---|
| 0.04 | 0.1286 | **25.36** | 0.7011 | 12/13 |
| 0.05 | 0.1383 | 24.54 | 0.7068 | 12/13 |
| 0.06 | 0.1482 | 23.86 | 0.7020 | 12/13 |
| 0.08 | 0.1649 | 22.52 | 0.7038 | 13/13 |
| 0.10 | 0.1818 | 21.36 | 0.7043 | 12/13 |

**失真漲 41%、PSNR 掉 4.0 dB，位移一動也不動。**

兩個後果，都會改變往後怎麼讀數字：

1. **`spectral_floor` 超過 0.04 是純付出、零收益。** 它已經在拐點上或過了。
   剩下要問的是它最小能多小（`ip2p_floor_only` 正在跑），不是能多大。
2. **位移在 0.70 以上不再有鑑別力。** 那個區間的「誰位移比較高」與
   「12/13 對 13/13」都在雜訊裡（擋下數本來就有 ±2/13）。**在飽和區唯一還
   有鑑別力的軸是失真。** 本頁與 `RESULTS.md` 凡是在該區間比位移的結論
   都要照這一條重讀——包含「等效果 0.70 錨點」那一列：它現在應該理解成
   「在讀數飽和處比失真」，那仍然是有效的比較，但不是「等效果」的原意。

## 〇之二、三批探索性實驗

**探索性質，不是要當成果。** 程式在 `src/defense/dct_nonadditive.py`、
`param_pgd.py` 的三個新旗鈕、以及分階段訓練那一組旗鈕，判準寫在各自的派工
腳本檔頭。本機測試 **787 passed / 1 skipped / 1 xfailed**。

**擋下數的代理不可混讀**：下列各表沿用該批當時記下的讀數，較早的幾筆是
SigLIP 代理，新補的幾筆標明是 CLIP 代理（門檻 0.8445，`EVALUATION.md` 的
現行標準）。兩個代理的門檻不可換算，**跨批比擋下數要回到人眼**。

### DCT 域非加性：學出來的旋轉平面（`runs/ip2p_dct_nonadd/`）

前一批固定配對旋轉（`ip2p_dct_rotate`）失敗後，把「配對」這個限制拿掉：
每個區塊學一個二維平面 `span(u,v)` 與一個角度。固定配對是它的特例。

| tag | DISTS | 位移 | 擋下 |
|---|---|---|---|
| `nd_plane_12` / `18` / `25` / `31` | 0.0483 / 0.0917 / 0.1285 / 0.1535 | 0.4484 / 0.5603 / 0.6382 / 0.6571 | 3 / 5 / 8 / 8 |
| `nd_gain_16`（**非正交**對照） | 0.0773 | 0.3514 | 0/10 |
| `nd_rand_25`（**隨機**對照） | 0.0044 | 0.0457 | 0/10 |
| 對照 `ours_ph_n` / `ours_pg_n` / `ours_pg_m` | 0.0497 / 0.0813 / 0.1453 | 0.5913 / 0.6372 / 0.7072 | 7 / 7 / 8 |

**P1 不通過**（等失真下是相位族的 0.76→0.93，隨失真收斂），但比前一批固定
配對的 0.60 大幅改善——同一個 DISTS 0.08，固定配對 0.2506、學習平面 0.5335，
**2.1 倍**。

**P3 不通過＝正面結果**：判準原本寫「非正交的 `gain` 勝過 `plane` 則保長是
負擔」。實測相反——等失真下**保長旋轉是非正交乘性的 1.49 倍**（0.5232 對
0.3514），擋下 5/10 對 0/10。**這是「非加性」在 DCT 域的第一個正面證據，
而且對照組是我們自己設計來打它的。**

**P4 通過且幅度很大**：同 θ 的隨機解只有位移 0.0457，最佳化是 0.6382，
**14 倍**。與 FND-004 那種「與隨機無法區分」完全不同型。

**P3 在第二個失真點上再次成立**：`nd_gain_11`（非正交乘性 g=1.1）是
DISTS 0.0548／位移 0.2902／CLIP 代理擋下 0/10。把 `plane` 族內插到同一個
失真得 **0.4652**——**保長旋轉是非正交乘性的 1.60 倍**（前一點是 1.49 倍）。
兩個失真點、同一個方向，**「保長是資產不是負擔」在 DCT 域站得住**。

**量化交付在 DCT 域一樣是純虧損，而且不必內插就看得出來**：
`nd_plane_25_qd`（學習平面 ＋ `--deliver-jpeg 0.85`）是 DISTS 0.1617／
位移 0.5679／擋下 6/10，而同族不交付的 `nd_plane_31` 是 **0.1535／0.6571／
9/10**——**失真更高而位移更低，兩軸皆被支配**（`DECISIONS.md` 拒絕外插，
這裡不需要外插）。自壓後的擾動保留率是 0.9486，與相位族的 0.94 同級，
所以**不是「交付沒學到格點」，是可行集變小這件事在兩個域上都要付錢**。
相位族付的是等失真下位移 −21%（`ip2p_deliver_jpeg`）。
**證據**：`ip2p_dct_nonadd/{nd_gain_11,nd_plane_25_qd}/results.csv`

**一個必須並記的保留**：等 DISTS 下本批多付 **3.4 dB PSNR 與 2.2 倍 L∞**
（0.9149 對 0.4216）。L∞ 逼近 0.95 表示殘差是稀疏高振幅尖峰。
**「0.93 幾乎打平」是樂觀讀法**，換到 PSNR 或 L∞ 對齊會明顯落後。

**兩個要撤回的推論**：(甲) 我曾推測固定配對輸在「28 個平面 vs 1 個平面」
——錯，一個**學出來的**平面打贏 28 個**固定**配對，決定效果的是平面能不能
**對齊該區塊自己的能量**。(乙) 天花板探針第一版用**隨機**平面量，θ=π 得
0.0124，依 N1 會當場誤殺整個方向；改成對齊平面是 **0.2313**，**低估 18.6 倍**。
**參數會選子空間的參數化，天花板必須用最好的子空間去量。**

### 位移場硬訓練（`runs/ip2p_warp_hard/`）

依 `ip2p_warp/DIAGNOSIS.md` 指名的病灶各加一個旗鈕（`--warp-init-std`、
`--step-size`、`--update adam`），**預設值不變、不給就逐位元等於現在**
（`tests/test_warp_hard_train.py` 釘住）。

`w_adam_rand`（Adam ＋隨機起點、grid 16、4000 步、`--radius 64` 實質不設限）：
DISTS **0.0577**、位移 0.1578、擋下 **0/10**。失真帶下界是 0.1286——
**連邊都沒碰到**，只比舊設定（0.0495）高 17%。

**W1 不通過。** 能下的結論：對 `latent_norm ＋ grid 16` 這個組合，走不到
失真帶不是預算、不是起點、不是更新規則、也不是步數問題，四者都排除了。

`w_adam_g64`（grid 64、4000 步）已跑完：DISTS **0.2793**、位移 0.4589、
CLIP 代理擋下 4/10。**細網格確實解開了「到不了失真帶」——而且是衝過頭**
（帶是 0.1286–0.1447，它是帶上緣的 1.93 倍）。這與
`budget_utilization.csv`「失真來自場的粗糙度不是幅度」一致。**但效率不成立**：
同一批十張上 `ours_pg_m` 在 DISTS 0.14528 就有位移 0.70715，也就是位移場付了
**1.92 倍的失真只換到 65% 的位移**。

**兩格合起來否掉的是兩種不同的可能性**：grid 16 是「到不了帶」，grid 64 是
「到得了但很不划算」。八格裡跑了兩格，而這兩格正是最粗與最細的端點。
**要不要據此收掉整批是裁定事項**，判準本身沒有規定端點通過就必須補中間。
**證據**：`ip2p_warp_hard/w_adam_g64/results.csv`

**`--update adam` 重開了 `GOAL.md` 列為已否決的方向**：那批證據已刪除且是在
**相位**參數化上做的，使用者已就本探索批次明確授權。**結果不可用來推翻或
恢復相位臂上的那個否決。**

### 不動點項（擴散淨化那一列）：**機制成立，帳不成立**

三張篩選批，先驗取攻擊方自己的編輯模型（不是評測用的淨化器——理由是白盒假設
給的是編輯模型，針對評測算子最佳化就是已否決的 co-adapt）。

| 條件 | DISTS | 未淨化位移 | identity | gridpure | 保留率 | 扣地板淨增益 |
|---|---|---|---|---|---|---|
| `ours_pg_m`（階段一） | 0.1267 | 0.7159 | 0.6365 | 0.3956 | **0.622** | **0.1945** |
| `fp_only` | 0.0567 | 0.3468 | 0.3144 | 0.2757 | **0.877** | **0.0746** |

**迎合擴散先驗確實把保留率由 0.622 拉到 0.877**——擾動幾乎不再被擴散淨化移除，
框架預測的機制成立。**但絕對效果掉 51%，淨增益只剩階段一的 0.38 倍。**

**F1 不通過**（淨增益沒有高於主線）、**F2 不通過**（未淨化掉 51%，遠超過量化
交付那一筆的 −21%）。等權的權重掃描（0.25／1／4）顯示這是**單調的取捨**，
解一路往「乾淨」那端塌，沒有中間的甜蜜點。

**框架因此有了第三種結局**：JPEG 是「可行且有效」、模糊是「結構性不可行」、
裁切是「不動點集合寬度為零」，擴散淨化是**「進得去但門票太貴」**。
中間權重的 gridpure 讀數在跑，用來確認取捨曲線上真的沒有交叉點。
**證據**：`ip2p_fixedpoint/manifold/`、`manifold_purify/`

### 整併版的定價方向：天花板說便宜 1.4 倍，最佳化之後是打平

十張。動作天花板（隨機方向、等殘差 RMS）量到依 JPEG 量化表定價的目標方向比
均勻取向便宜 **1.37–1.45 倍**。把它做成旗鈕（`--dct-plane-weight priced`）跑
三個角度之後，在兩條曲線的**重疊區間**（DISTS 0.199–0.258）比：

| 錨點 DISTS | 均勻 | 定價 | 定價／均勻 |
|---|---|---|---|
| 0.2000 | 0.6297 | 0.6463 | 1.026 |
| 0.2156 | 0.6557 | 0.6561 | **1.001** |
| 0.2375 | 0.6654 | 0.6699 | 1.007 |
| 0.2583 | 0.6719 | 0.6814 | 1.014 |

**打平。** 而且定價版最低的一個角度是 DISTS 0.1989，**仍然進不了失真帶**
（0.1286–0.1447），L∞ 一樣飽和到 1.000。

**一條方法上的教訓，值得單獨記**：天花板探針量的是**隨機方向**，而最佳化本來
就會挑方向——所以隨機方向的排序**不保證轉移到最佳化之後的設定**。本專案先前
踩過相反的一次（隨機平面把天花板低估 18.6 倍），這次是同一個機制的另一面：
**天花板高估了改良的幅度**。往後任何「某個取向比較便宜」的天花板結論，都要在
最佳化之後再驗一次才算數。

**後果**：三種目標方向（均勻、非零支撐、定價）全部進不了失真帶。整併這條路上
「換一個方向」已經試遍，**問題不在方向**。
**證據**：`ip2p_dct_unified/du_priced_{18,24,30}`、`integration_design/action_ceiling.csv`

### 不動點項的第一批：權重太小，等權的點在跑

三張篩選批。`fp_w1`／`fp_w4` 用 `raw` 相加，而主損失在乾淨影像上約 70–80、
不動點項已正規化成 1，所以那兩個權重**實際只佔約 1/70 與 4/70**——結果與階段一
幾乎逐格相同（DISTS 0.1246／0.1203 對 0.1267，位移 0.7166／0.7132 對 0.7159）。

**`fp_only`（判準 F3 的歸因對照，只留不動點項、拿掉對抗項）**：DISTS 0.0567、
位移 **0.3468**、擋下 1/3。也就是**單靠「讓圖在擴散先驗底下看起來乾淨」就買得到
位移 0.35**——遠低於主線的 0.72，但明顯不是零。這一格之後要用來扣掉「改善有多少
不是對抗項買的」。

已補 `--manifold-balance normalised`（兩項都由 1 起步，權重 1 才是等權），
三個等權點與 `fp_only`／`fp_w4` 的 gridpure 讀數都在跑。
**證據**：`ip2p_fixedpoint/manifold/`

### 不動點框架：四個算子的第四列量到了，核心預測 D1 通過

`runs/fixedpoint_framework/README.md`。主張是：擾動能不能撐過某個淨化算子，
取決於**交出去的影像能不能落在那個算子的不動點集合上**。

**D1（跑之前寫下的可證偽預測）**：JPEG 的量化格點不是擴散淨化器的不動點集合，
所以量化交付在 gridpure 上**不該**有優勢。十張、扣空白地板（0.2247）：

| 條件 | DISTS | identity | gridpure | 淨增益 |
|---|---|---|---|---|
| `ours_pg_m`（不交付） | 0.1453 | 0.6687 | 0.4422 | **0.2175** |
| `ours_ph_q`（JPEG 格點交付） | 0.0928 | 0.4657 | 0.3773 | **0.1526** |
| `dct_aj85` | 0.1118 | 0.6702 | 0.4901 | 0.2654 |

**同一組條件在 jpeg30 上是 2.19 倍，在 gridpure 上是 0.70 倍——優勢反轉。**
失真沒對齊，但那個不對齊在兩個算子上是同一個，結論卻反轉，所以反轉不是失真
造成的。**D1 通過。**

順帶：擴散淨化的破壞力遠小於模糊——本方法保留率 66%，模糊 σ1 只有 18%。
**證據**：`ip2p_fixedpoint/diffusion/`

### 模糊那一欄結案：可贏上界 1.54 倍，判準是 3

`runs/fixedpoint_blur/README.md`，十張、純 CPU。存活率／知覺代價偏好低頻
（最低帶是現行工作帶的 13.5 倍），編碼器敏感度偏好高頻（跨帶差三個數量級），
**兩條陡峭的曲線方向相反，乘積在整個頻域上是平的**——極大值落在 0.884–1.061，
正是本方法 60% 殘差能量已經在的地方，只高 **1.54 倍**。
**模糊底下沒有好的頻帶可以搬**，這是構造性的陳述不是嘗試失敗。

### 整併的動作方向：一項歸因要撤回，設計甲否決，定價勝出

`scripts/dct_action_ceiling.py`，十張、純 CPU、等殘差 RMS 下比：

| 目標方向 | 相對 `full` 的 DISTS |
|---|---|
| 非零支撐（設計甲） | **1.47–1.70 倍（更貴）** |
| 低頻一半 | 1.86–2.34 倍 |
| **JPEG 量化表定價** | **0.69–0.73 倍（更便宜）** |

**要撤回的歸因**：先前把整併版 L∞ 飽和歸因於「能量被丟進原本是零的高頻格子」。
**錯**——最便宜的 `priced` 恰恰把 80% 的能量放進原本是零的格子，而把該比例壓成
0 的 `support` 是最貴的之一。**決定價錢的是目標頻率的知覺代價，不是那一格原本
是不是零。**

**一個把設計合併掉的結構事實**：保長映射把係數向量送到等長的向量，兩者必張成
一個平面，所以「保長整數位移」（設計丙）與二維旋轉是同一族，買不到新的可行集。
真正的自由度只有目標方向怎麼選。

`--dct-plane-weight priced` 已實作（預設 `uniform` 逐位元等於加它之前，由
`tests/test_dct_unified.py` 釘住），三個角度已派工。
**證據**：`integration_design/action_ceiling.csv`

### 整併版（交付即參數）：等失真下與事後投影**完全打平**，中心預測被否證

五個角度、十張（`runs/ip2p_dct_unified/`）。把旋轉直接做在量化後的整數係數上、
交出去的就是那組整數——也就是拿掉「事後投影」那一步。

| 錨點 DISTS | 整併版（內插） | 對照 | 比值 |
|---|---|---|---|
| 0.1617（`nd_plane_25_qd`，事後投影交付） | **0.5660** | 0.5679 | **1.00** |
| 0.1285（`nd_plane_25`，浮點、不交付） | 0.5106 | 0.6382 | **0.80** |

**`runs/dct_phase_design/README.md` §3.1 的事前預測被否證**：該處主張交付要付的
那 21% 來自事後投影、參數住在交付格式裡就不該付。實測**完全打平**。
**那一筆不是投影造成的，是「被綁在 JPEG 格點上」本身造成的**，與在量化的哪一側
做參數化無關。

**而且 DISTS 打平不代表打平**：整併版在 DISTS 0.1450 上 PSNR 只有 18.33
（事後投影版在更高的 0.1617 上是 22.63），且五個角度的 **L∞ 全部是 1.000**
——有像素被從全黑推到全白。換到 PSNR 或 L∞ 對齊會明顯落後。

**機制已量出**：`zero_coef_frac = 0.8118` 逐張一致——交付品質 0.85 的量化把通帶
內 81% 的係數清成零，保長旋轉只能在剩下的**大**係數之間搬能量，而平面的另一個
方向多半落在原本是零的高頻格子上。`U2` 也隨之縮小：最佳化勝過同角度隨機只有
2.8 倍（浮點版是 14 倍）。

**後果**：「把參數搬進交付格式就能省下交付的代價」這條推理**不成立**。四個後續
設計與判準見 `runs/integration_design/README.md`。
**證據**：`ip2p_dct_unified/du_plane_{08,11,18,22,25}`、`du_rand_22`

### 分階段訓練：階段二 ＋ 信賴域（`runs/ip2p_stage2/`，篩選批在飛）

**這一批把 `RESULTS.md` 的「針對淨化最佳化沒有改善抗淨化」重開了，使用者已
就本探索批次明確授權。** 結果不論好壞**都不會自動用來推翻或恢復**那條裁定
——那條是對「從零開始的擴增訓練」下的。

改動只有兩處，兩處都針對前兩批的同一個死法（**沒有東西擋住未淨化那一側往下
掉**：三個 EOT 變體掉 10–25%，幾何 EOT 的 `g_rpi` 掉 14.4% 換回 0.0298）：

1. **不從零開始。** 階段一逐字等於現行主線 `ours_pg_m`，階段二以它為起點、
   用小步長在附近找一個比較耐洗的鄰居。
2. **信賴域。** 每 20 步量一次未淨化的增益 `L(原圖) − L(防禦圖)`，低於階段一
   終值的指定倍數就退回上一個通過的快照並把步長減半；步長掉到初始值的 1/32
   以下就停。**交出去的一律是最後一個通過檢查的快照**，故結果必定滿足約束。

算子**依序輪替而不是每步隨機**（`--stage2-order shuffle`：每輪洗牌後依序走
完）。理由是短期覆蓋：只跑幾百步時獨立隨機抽會讓某個算子連著被抽好幾次、
另一個一次都沒抽到；不用固定輪替當預設是因為那樣每一輪的最後一個算子恆是
「最後說話的那個」，配上 sign 更新容易走成週期性的來回。

三個工作點、三張影像（`images3.txt`，每個任務型態取字母序第一張）：
`s2_tight`（400 步／步長 0.2／信賴域 0.95）、`s2_hard`（800／0.5／0.80）、
以及 `s2_null`（與 `s2_hard` 逐字相同，但算子池只有 `identity`）。
**`s2_null` 是歸因對照**：第二段多跑 800 步本身就可能讓數字變好，沒有這一格
不管結果是好是壞都歸因不清。兩個有算子的點合起來給的是一條**取捨曲線**
（未淨化賠了多少 vs 淨化後賺了多少），不是一個點。

事前判準（S1–S4）逐字寫在 `scripts/stage2_screen.sh` 的檔頭。
**證據**：`ip2p_stage2/README.md`

### 一個影響全部排程的成本更正

**先前的機時估算錯了 10 倍。** `results.csv` 的 `total_seconds` 是**每張影像**
的秒數，不是整批總秒數。實測：防禦一個十張工作點 **60–85 分鐘**（不是 7 分鐘）；
位移場一格十張 4000 步 **約 4.2 GPU-h**（不是 26 分鐘）。
位移場八格 ≈ 33 GPU-h，在只有一兩張卡時不可行，**建議改成三張影像的篩選批**
（一格約 75 分鐘）先回答「哪一格走得動」，再擴到十張。

## 一、證據完整，可以裁定

### 降低 `Q_alg` 買不到低品質存活，反而更差——「對手把旗鈕設錯」這個質疑被擋掉

十張、扣同影像空白地板的淨增益。`Q_alg` 是 DCT-Shield **防禦方自選**的參數，
既有頭對頭全部固定在 0.85（§6.3 為品質 ≥ 85 而設），而我們拿品質 30 去攻擊它
——這是一句打不掉的質疑。補上 `Q_alg` 0.30 的等失真點之後，答案是**反向**的：

**低失真錨點（DISTS 0.093–0.099，四個 `Q_alg` 都有點落在這裡）**：

| 條件 | `Q_alg` | DISTS | jpeg90 | jpeg75 | jpeg50 | **jpeg30** |
|---|---|---|---|---|---|---|
| `dct_aj50_eps0.22` | 0.50 | 0.0931 | 0.4810 | 0.1383 | 0.0263 | 0.0516 |
| `dct_aj30_eps0.13` | 0.30 | 0.0986 | 0.4836 | 0.1625 | 0.0940 | **0.0115** |
| **`ours_ph_q`** | — | **0.0928** | 0.4007 | **0.3803** | **0.2323** | **0.1072** |

**在同一個失真上，本方法在 jpeg75／50／30 三格全勝**：對 `Q_alg 0.50` 是
2.8／8.8／2.1 倍，對 `Q_alg 0.30` 是 2.3／2.5／9.3 倍。對手只在 jpeg90 勝出。

**其餘失真點**：

| 條件 | `Q_alg` | DISTS | jpeg90 | jpeg75 | jpeg50 | **jpeg30** |
|---|---|---|---|---|---|---|
| `dct_aj85`（論文設定） | 0.85 | 0.1118 | 0.4872 | 0.4533 | 0.1197 | 0.0449 |
| `dct_aj75` | 0.75 | 0.1145 | 0.5126 | 0.4732 | 0.1011 | 0.0524 |
| `dct_aj50_eps0.40` | 0.50 | 0.1479 | 0.5975 | 0.5038 | 0.1242 | 0.0856 |
| `dct_aj30_eps0.25` | 0.30 | 0.1562 | 0.6078 | 0.5182 | 0.1664 | 0.0512 |
| `dct_aj30_eps0.42` | 0.30 | 0.2183 | 0.6218 | 0.5436 | **0.5046** | 0.2033 |
| `ours_pg_q20` | — | 0.1947 | 0.5520 | 0.5116 | 0.3326 | 0.2358 |

**高預算那一端要分開講，不可以一句話帶過。** 把 `Q_alg = 0.30` 那條曲線內插到
`ours_pg_q20` 的錨點 DISTS 0.1947：jpeg30 得 **0.1455**（我方 0.2358，**我方勝
1.62 倍**），jpeg50 得 **0.3761**（我方 0.3326，**對手勝 1.13 倍**）。
也就是**加預算確實買得到中品質存活，但買不到低品質存活**——與先前
`dct_aj85_eps2.2` 翻轉 jpeg50 的觀察同型，而那一格的結論不變。

**可主張的範圍因此收斂成一句**：jpeg30 的優勢**同時與預算無關、也與 `Q_alg`
旗鈕無關**（`Q_alg` 0.85／0.75／0.30 三個設定、eps 由 0.13 到 0.42 的整條曲線
上都成立）；jpeg50 在高預算端會翻轉，論文要照實列。

**在幾乎相同的失真上（0.0986 對 0.1118），把 `Q_alg` 由 0.85 降到 0.30 讓
jpeg30 的淨增益由 0.0449 掉到 0.0115——差 3.9 倍，方向與直覺相反。**
jpeg75 同樣崩掉（0.4533 → 0.1625）。

**機制是構造上的，不是調參沒調好。** `eps` 的單位是量化階，而低 `Q_alg` 的
量化階很大：要在 `Q_alg = 0.30` 留在失真帶內，eps 必須掉到 0.13，遠低於
DCT-Shield §4.2 抗 JPEG 條件要求的「|δ| 至少一個量化階」。擾動比它必須存活的
那一次四捨五入還小。**「降 `Q_alg` 以覆蓋低品質壓縮」與「留在等失真帶內」
在低 `Q_alg` 上互斥**——`scripts/dct_shield_eps_calibration.py` 的檔頭在跑之前
就由校準外推指出這個張力（保住 eps ≥ 1 在 `Q_alg` 0.50 要 DISTS 約 0.28、
0.30 要約 0.41），本批把它實測出來。

**後果**：`ours_ph_q`（DISTS 0.0928，七項失真指標全部低於 `dct_aj85`）在
jpeg75／50／30 三格**全部勝過對手的任何一個 `Q_alg` 設定**，對手只在 jpeg90
勝出。「壓得越狠我方越有優勢」由「拿錯設定比出來的」升格為**與對手旗鈕無關**。

### 對稱的另一半：**我方**降低交付品質**買得到** jpeg30，方向與對手相反

同一個旗鈕在兩邊的效果是反的，這是本批最值得寫進論文的一組對照。
`--deliver-jpeg QD` 往下放粗（十張，扣空白地板）：

| 條件 | QD | DISTS | identity | jpeg90 | jpeg75 | jpeg50 | **jpeg30** |
|---|---|---|---|---|---|---|---|
| `ours_ph_n` | 無交付 | 0.0497 | 0.5329 | 0.3539 | 0.2227 | 0.1026 | 0.0489 |
| `ours_ph_q` | 0.85 | 0.0928 | 0.4657 | 0.4007 | 0.3803 | 0.2323 | 0.1072 |
| `ours_ph_qd60` | 0.60 | 0.1072 | 0.3893 | 0.3307 | 0.2932 | 0.2433 | 0.1080 |
| `ours_ph_qd45` | 0.45 | 0.1112 | 0.3629 | 0.3039 | 0.2485 | 0.1924 | **0.1800** |

**事前寫下的判準是「看曲線的形狀不是單一格：低 QD 應該在 jpeg30 那端抬起來、
jpeg90 那端塌下去；只是整體平移就是死的」——形狀確實換了。** QD 由 0.85 降到
0.45：jpeg90 掉 24%、jpeg75 掉 35%、identity 掉 22%，而 **jpeg30 漲 68%**
（0.1072 → 0.1800）。這是取捨不是免費改良，但取捨的方向正是想要的那一個。

**與對手的不對稱要指名機制**：DCT-Shield 降 `Q_alg` 會讓 jpeg30 變差，因為
它的抗 JPEG 條件要求 |δ| ≥ 一個量化階，而低 `Q_alg` 的量化階大到讓 eps 必須
掉到 1 以下（見上一則）。本方法的擾動**沒有這個整數階條件**——它是透過可微分
JPEG 在該品質上最佳化出來的，所以降低交付品質換到的是「擾動長在更粗的格點上」
而不是「擾動小於它要存活的四捨五入」。**同一個旗鈕，兩邊的構造限制不同，
效果因此相反。**

**保留**：等失真比較是跨參數化內插的（QD 0.45 在 DISTS 0.1112，而 QD 0.85
家族在該高度要靠 `ours_pg_q` 內插得 jpeg30 約 0.1379），**兩個軸混在一起**，
嚴格的等失真對照要補同 QD 不同半徑的點。粗估 QD 0.45 在等失真上仍勝約 1.3 倍。
`ours_ph_qd35` 的抗淨化**沒有跑**（卡被別人接走），那一點會決定曲線在更低的
QD 上是繼續抬升還是回落。
**證據**：`ip2p_mainline_purify/ours_ph_qd{60,45}_*.csv`、
`ip2p_mainline/tables/net_gain.csv`

**尚未齊的部分**：`ours_ph_qd35` 的抗淨化未跑；`dct_aj85_eps1.5` 與
`dct_aj85_eps2.2` 仍是 6/10 影像（`net_gain.csv` 的 `n` 欄可查，**不可與
十張的列並列**）。
**證據**：`ip2p_mainline_purify/dct_aj30_eps0.13_*.csv`、`dct_aj30_eps0.25_*.csv`、
`ip2p_mainline/dct_shield_eps_calibration.csv`、`dct_shield_qalg_floor.csv`

### DCT 域的保長配對旋轉：兩條事前判準都不通過

把相位擾動搬進 JPEG 自己的 8×8 DCT 係數——同區塊內的係數配對做二維旋轉
（`θ = π` 恰好是聯合翻號，也就是 Ito & Kiya 2007 的「DCT 相位即正負號」的
連續化）。十張、1000 步、交付品質 0.85。

| tag | DISTS | 未淨化位移 | 擋下 | L∞ |
|---|---|---|---|---|
| `dct_rot_t11`（transpose θ1.1） | 0.0777 | 0.2506 | 0/10 | 0.5713 |
| `dct_rot_t15`（transpose θ1.5） | 0.1024 | 0.2924 | 0/10 | 0.7047 |
| `dct_rot_zz11`（**zigzag 對照**） | 0.0768 | 0.2484 | 0/10 | 0.5878 |
| `ours_ph_q`（現行量化交付） | 0.0928 | **0.4615** | 2/10 | 0.3608 |

**D1 不通過**：內插到 DISTS 0.0928 得位移 **0.2761**，只有現行量化交付
0.4615 的 **60%**；四個點的擋下全部 0/10。**必須撤回的預測**：設計文件主張
`post_int` 的可行集就是交付集、沒有事後投影，所以現行做法為交付付出的那 21%
不該存在——消掉投影損失並沒有換來位移，換到的是一個更小的可行集。

**D2 不通過**：`zigzag` 與 `transpose` 在等失真上差 **0.9%**，分不開。這是
事前排好的對照組，它否證的是配對規則的唯一依據——**「保長只有在兩軸價錢
相同時才有感知意義」沒有實證支持，論文不可以那樣寫**。

**死法與位移場不同，要分清楚**：`zero_pair_frac = 0.7538`，交付品質 0.85 的
量化把大部分高頻係數清成 0，而旋轉零向量還是零向量；名目 11.4 萬個自由度
實際只剩四分之一，現行方法是 59 萬個。L∞ 反而更高（0.7047 對 0.3608）——
少數還活著的大係數被轉很大的角度，失真集中成稀疏尖峰。而最佳化用掉了天花板
的 58%（位移場那邊只有 6–29% 且與隨機遊走無法區分）。**那邊是最佳化沒動，
這邊是動了但可行集太小。**

**抗淨化沒有跑，也不建議跑**（D1 逐字寫著不通過就不必再走）。
**證據**：`ip2p_dct_rotate`（含 README）、`dct_phase_design/ceiling.csv`

### 位移場：預算從頭到尾就夠，是最佳化沒有走

`corner`（L∞ 球的頂點）在 **amp = 4 px 的天花板就是 DISTS 0.1402**，落在失真帶
（0.1286–0.1447）內——而 `opt_r*` 用過的最小半徑正是 4。四個跑過的半徑，
可行集裡全都有帶內的點，**（甲）預算不夠被否證**。

實際達成 0.0143–0.0495，是同半徑天花板的 7–15%；反推等效位移 0.43→1.52 px
（用 `gauss` 反推；用 `coherent` 反推是約 7 px，故使用率讀成區間 6–29%）。
與無偏隨機遊走的預測 `α√N` 比值是 **0.40–0.84**，落在事前寫下的「與擲硬幣
沒有差別」區間內，`opt_r16` 的 0.40 已掉進振盪那一側。

**另一個構造事實**：`coherent`（整張平移，最平滑的場）放到 24 px 的天花板只有
**0.0982**，仍在失真帶之外。**失真來自位移場的粗糙度，不是幅度**——若最佳化
收斂到平滑解，加多少預算都到不了帶內。
**證據**：`ip2p_warp/DIAGNOSIS.md`、`budget_utilization.csv`

### 淨化沒有讓相位「跑掉」——三個算子各自壞在別的地方

在本方法自己的 STFT 域（32×32、hop 8）上量裝上去的相位偏移還剩多少
（十張、四條件、八算子、純 CPU）。**這一頁改變的是失效的歸因，不是失效與否**
——沒有跑任何 GPU 上的編輯，「blur 與 crop 三個方法都沒有防禦」不變。

| 算子 | 相位保留 `rho` | 殘差能量存活 | 對原格點 cos | 對「搬過的同一份」cos |
|---|---|---|---|---|
| blur σ1 | **0.967** | **0.089** | 0.489 | — |
| blur σ2 | 0.814 | **0.033** | 0.306 | — |
| jpeg30 | 0.687 | 0.836 | 0.349 | — |
| crop 10% | 0.599 | 0.661 | **0.006** | **0.9997** |

三個結論：

1. **模糊：相位原封不動，是載體被抽掉。** 高斯模糊是零相位濾波器（頻域乘的是
   實正數 `H(ω)`），**在原理上就動不了相位**；`rho = 0.967` 與此一致，而能量
   只剩 8.9%。先前把模糊那一格說成「擾動被破壞」是錯的歸因，正確的是
   「擾動被靜音」。兩者的下一步不同：後者只要把同一份相位資訊放到 `H(ω)`
   還沒壓掉的頻率上——而 `m_ω` 與 `H(ω)` 都是已知曲線，乘積的極大值可以直接
   解，不必掃參數。
2. **JPEG：相位被打散，而且是往上打散。** `surv_mag` 由 0.375 漲到 0.558、
   能量在 q75 是 2.16 倍——量化是在原偏移之上再疊一層，不是把它磨平。
3. **裁切：整份原封不動地通過。** 對「搬過的同一份」餘弦 **0.9997**、對原格點
   0.006。逐區塊 `rho` 掉到 0.599 是**索引對不上**，不是相位改變。
   **「裁切讓相位跑掉」不可寫進論文。**

**一個可能有用的副產品**：q30 上本方法的能量存活是 DCT-Shield 的 2.6 倍
（0.836 對 0.316）、方向 2.7 倍（0.349 對 0.131），而該格淨增益比是 2.39 倍。
相位保留這個**純 CPU** 的量在 JPEG 軸上排得出淨增益的順序，可當設計的預篩
代理。**保留**：四個條件上的一致，不是回歸，不可寫成定量預測。

**一個踩過的實作陷阱已修**：`purify.ops.crop_resize` 末行的 `.clamp(0,1)`
套在有號殘差上會把負值全推成 0，`cos_vs_warped` 因此被架高成 0.72；拿掉夾取
之後才是 0.9997。
**證據**：`phase_drift_diagnosis`（含 README、`summary.csv`、
`phase_retention_by_band.csv`、`residual_survival.csv`）

### 否決 `--floor-gate watson`
亮度遮蔽 × 對比遮蔽（Watson 1993）。等 DISTS 0.1447 下位移 0.6735 對 uniform
的 0.7012，掉 4%；DISTS 0.1188 下 0.6330 對 0.6756，掉 6%。原因可量：它把
66.7%／51.8% 的預算送進「乘法可達量高」的那一半，也就是乘法已經在動的地方。
**證據**：`ip2p_floor_gate`、`ip2p_residual_signature/allowed_budget_gini.csv`

### 加法下限佔了可用預算的 67.6%，並吃掉大部分空間選擇性
逐區塊的可用預算（**構造允許在哪裡花**，不是最佳化實際花在哪裡）：乘法那一半
單獨的 Gini 是 0.531，開了 `--spectral-floor 0.04` 之後合計掉到 0.163，而
DCT-Shield 由構造是 0。空間選擇性是本方法對它的主要構造差異。
**證據**：`ip2p_residual_signature/allowed_budget_gini.csv`

### 三條指紋差異在下限開著時仍然成立
逐區塊能量 Gini 差 4–6 倍、半 Nyquist 以上能量佔比 0.60 對 0.95、殘差沒有鎖在
8×8 格點上（1.27 對 1.78）。**分離最小的是 phase_share**，下限把它由 0.607
壓到 0.427。
**證據**：`ip2p_residual_signature/signature.csv`

### 人眼頭對頭打平，但失真多付 32%
純相位＋下限 r=π（DISTS 0.1286）與 DCT-Shield ε=1.8（DISTS 0.0976）都是
**11 擋下、1 邊緣、1 失敗**。等效果（位移 0.70）時前者要付 0.1279、後者 0.0930。
共同覆蓋的失真區間上效率是 6.08–6.27 對 7.05（86–89%）。
**證據**：`ip2p_axis_necessity/recognisability_phase_floor.csv`、
`ip2p_dct_band_extend/recognisability_dct_e18.csv`、
`ip2p_axis_necessity/equal_effect_anchor.csv`、`efficiency_overlap.csv`

### 兩軸都必要——但只在非加性設定下
純相位因 θ 封頂在 π，失真最高只到 DISTS 0.0950，而工作點在 0.1377：**它到不了
失真帶**，整條曲線擋下 0/13。純幅度到得了，但等失真下輸給兩者合用 10%。
**證據**：`ip2p_axis_necessity`

### 三個淨化算子壞掉的方式各不相同
模糊是**能量**問題（方向存活率每帶 0.88–1.00，半 Nyquist 以上能量剩 0.3%）；
JPEG 是**打散**問題（能量比 1 還大但方向由 0.68 掉到 0.05）；裁切縮放**兩者
都不是**——能量留 51–99%，對原網格的方向 0.000，對算子自己搬過的同一擾動卻是
0.995–0.996。擾動原封不動地通過了，只是被搬走。
**後果**：裁切的弱點不可能靠選頻帶解決（沒有一帶的方向存活率高於 0.02），
只有幾何上的不變性有機會。
**證據**：`ip2p_residual_signature/band_transfer.csv`

### 裁切吃掉的能量：本方法比 DCT-Shield **少**，但逐圖變異大四到九倍
`crop_resize(0.10)` 丟掉的那一圈佔殘差總能量的比例（13 張，不跑 GPU）：
本方法 0.227／0.263，DCT-Shield 0.334–0.339，均勻場的參照值是 0.359。
sd 則是本方法 0.093–0.141、DCT-Shield 0.016–0.023；`ours_nonadd` 的全距是
0.023 到 0.513。
**後果**：`SURVEY_ARCHITECTURE` H4（集中度在裁切下的變異數）的變異數那一半
成立，但**平均的方向是反的**——我們丟掉的能量比它少，效果卻掉得多得多，所以
H4 解釋不了「98.2% 對 13%」，只解釋得了「逐圖 0/5 為什麼那麼不穩」。這與
`band_transfer.csv` 一致：裁切的傷害來自 1.2488× 放大造成的對位破壞，不是能量。
**順帶更正**：H4 寫「裁切移除外圈 19% 的面積」是錯的。程式是**每邊各裁 10%**
（保留中央 410/512 的邊長），移除 **35.9%**；19% 對應的是整條邊只裁 10%。
**逐圖也對不上**：把該欄與同一張圖的 `crop_resize0.1` 扣地板淨增益做等第相關，
`ours_add` 是 −0.350、`ours_nonadd` 是 −0.083（n = 9，要 0.66 才顯著）。
所以 H4 連「解釋逐圖變異」都還沒站住。
**證據**：`residual_texture_alignment`

### 殘差對準影像內容這條軸分不開兩個方法，分得開的只有集中度
逐方格的殘差能量與影像梯度能量的等第相關（13 張，不跑 GPU）：本方法 0.894／
0.897，**DCT-Shield 也有 0.797–0.845**，而條件內的逐圖 sd 是 0.04–0.12、最小值
低到 0.49——差距小於離散度。同一批影像上 `block_gini` 卻差四到六倍
（0.49–0.65 對 0.10–0.13）。
**後果**：`SURVEY_ARCHITECTURE` 第三節 H1 若照字面讀成「它的殘差與這張圖無關、
我們的有關」，這一格直接否定。撐得住 H1 的機制只剩**冗餘**——同樣對準內容，
但它攤在很多方格上、我們擠在少數方格上。這也改寫候選一的目標：要的不是
「與內容無關的平穩場」，而是「對準內容但攤得夠開」。
**保留**：DCT-Shield 的殘差含它自己的 JPEG 地板，而量化誤差本身與紋理相關；
該地板佔它工作點失真的 2.5%（q0.95）／7%（q0.85），解釋不了 ρ ≈ 0.8，但是
一個已知汙染。**判定仍要看區塊置換實驗**（`runs/residual_permute`，待跑）。
**證據**：`residual_texture_alignment`

### 舊的 JPEG 頭對頭有兩個獨立的理由不可用，不只是 `q_alg` 錯
`dct_y_e14` 除了用 §5.4 的 `q_alg 0.95`（§三），它的失真是 **DISTS 0.0436**
——**不到本方法工作點的三分之一**（0.1286–0.1447），位移 0.5834、擋下 8/13，
整個落在失真帶外。新補的 `y_q85_e10`（§6.3 的 Y-only ＋ `Q_alg 0.85`）反過來
**幾乎是等殘差、等 PSNR 的對手**：RMS 0.0613 對 `ours_add` 的 0.0605、
PSNR 24.27 對 24.74，只有 DISTS 上便宜 28%。
**證據**：`ip2p_dct_antijpeg`、`ip2p_purify_headtohead/README.md`

### 等 DISTS 下本方法的 PSNR 比 DCT-Shield 的抗 JPEG 設定高 2.9 dB
把 `y_q85_e{10,14,20}` 內插到 DISTS 0.1286（＝純相位＋下限）：PSNR 22.44，
而本方法在同一個 DISTS 上是 25.36。原因在構造——`Q_alg = 0.85` 在做任何事之前
先付一筆 JPEG-85 的壓縮失真（DISTS 0.0072／PSNR 37.24），那一筆在 PSNR 上
比在 DISTS 上貴得多。位移（0.696–0.699）與擋下（11/13）三格都在飽和區，
不可解讀。**這是一個對本方法有利、且與抗淨化無關的讀數**，但它只在「對手被迫
用抗 JPEG 設定」時成立。
**證據**：`ip2p_dct_antijpeg`、`ip2p_purify_headtohead/README.md`

### 紋理閘的開度（`--quantile`）不是一個槓桿
`PENDING` 原本記為「從未量過」。掃兩個開度 × 三個半徑，內插到同一個 DISTS：

| 錨點 DISTS | quantile 0（現行，全開） | 0.30 | 0.60 |
|---|---|---|---|
| 0.0833 | 位移 0.6310 / PSNR 27.47 | 0.6322 / 27.43 | 0.6168 / 27.29 |
| 0.1172 | 0.6835 / 25.89 | 0.6887 / 25.76 | 0.6828 / 25.56 |

**三者在雜訊裡**（位移差 < 1.1%、PSNR 差 < 0.35 dB、擋下差全部小於 ±2/13）。
**方向要注意**：`--quantile` 越大閘越**選擇性**，而現行主線用 0，**已經在
最均勻的那一端**——往「更均勻」沒有空間可掃，往「更選擇性」掃了沒有差別。
併著既有兩格讀，「空間選擇性」在工作點上量不出價值：讓它變均勻（加性下限，
Gini 0.531→0.163）大幅變好、更均勻（`complement_rank`→0.114）幾乎免費、
收緊（本批）沒差。而空間選擇性正是本方法對 DCT-Shield 的主要構造差異。
**證據**：`ip2p_quantile_sweep`

### 候選二（乘性明暗場）：贏得過同失真隨機，但作為獨立方法無望
**步驟 3（必做）通過**：等失真下最佳化的位移是同失真隨機明暗場的 **1.62–1.65
倍**，沒有重蹈 `位移場`（FND-004）「與隨機無法區分」的覆轍。
**步驟 1 失敗**：兩族的失真範圍幾乎不重疊——明暗場最強的一點是 DISTS 0.0390
／位移 0.1184／擋下 1，而純相位＋下限最弱的一點是 0.0433／**0.5333**／6。
**在略低的失真上位移只有現行方法的 22%**（判準是 20%，擦線通過字面，實質差
4.5 倍）。上不去的原因在構造：`clamp(x·exp(m),0,1)` 的亮部先飽和，`m` 再大
殘差也不長，天花板比相位的 π 更低。
**還開著的路**是該節自己寫的退路——**疊加補丁**（頻帶與現行算子不相交，
失真近似可加）。但位移已在 0.70 飽和區，多加不會更高；**要問的是它能不能
提高抗淨化的模糊與裁切兩格，那要另跑一批才知道。**
**證據**：`ip2p_shading`

### AdvDrop：機制進得了失真帶，是最佳化到不了——**限制在我們換的目標函數**
掃描結果：eps 220 在 50 步是 DISTS 0.0114／位移 0.1088，500 步是 0.0378／
0.2241，擋下都是 0/13（工作點是 0.1286–0.1447／0.70／11）。四個 eps 在 50 步
下給出逐位相同的數字，因為**貼上界的比例是 0.0000**——eps 沒有咬到。

**但「機制上進不去」是錯的。** 把量化表整片推到 `q = 1 + ε`（該 ε 下最極端的
可行解，不最佳化、純 CPU，`ceiling.csv`）：

| eps | 20 | 60 | **100** | 150 | 220 |
|---|---|---|---|---|---|
| 天花板 DISTS | 0.0202 | 0.0851 | **0.1414** | 0.1984 | 0.2557 |

**eps = 100 是論文 §4.3 自己掃的最大值，天花板 0.1414 正落在失真帶內。**

**到不了的原因是目標函數在跟機制拔河**：更新是 `q ← q − 步長·sign(g)`，
而「梯度 < 0」的比例**一直低於 0.5**（0.37–0.48），也就是**對超過一半的 DCT
格子，多丟資訊會讓 `‖E(x′)‖₂` 變大**。q 還是往上爬，只因為想往下的被夾在
下界（32.5% 貼在下界），成了單向棘輪。梯度**沒有**消失（4.42e−6 → 2.48e−6），
所以不是軟四捨五入退火的問題。

**措辭要準確**：不可寫成「AdvDrop 對擴散編輯無效」，只能寫「AdvDrop 的機制
配上**本專案替換的** `‖E(x′)‖₂` 目標，收斂不到它自己可行集裡的帶內點」——
原文用分類交叉熵，這個威脅模型沒有分類器，替換已記在 `modification_note`。
**`RUN_QUEUE` 第四優先那一題仍然答得出來**：直接取 eps = 100 的天花板點
（DISTS 0.1414）跑抗淨化，那量的是「8×8 格點的非加性擾動抗不抗裁切」，
與最佳化收不收斂無關。
**證據**：`ip2p_advdrop_band`（含 `ceiling.csv`、`q_trajectory.csv`）、
`ip2p_advdrop_band_s500`

### 候選三（log-periodic）：不變性是真的，但它是寬度為零的窄峰
在對數極座標上以 `ln s` 為週期的場，對 `s = 1.2488` 的放大是構造上的不動點。
**步驟 0 兩個判準都通過**：方向存活率 **0.9726**（現行擾動是 0.015 以下，
高了 65 倍），等 RMS 下失真 0.1993（判準是不得高於現行殘差 0.1286 的兩倍）。
**步驟 1 否決**：拿為 s = 1.2488 造的場去測其他放大倍率——

| s' | 1.05 | 1.10 | 1.15 | **1.2488** | 1.30 | 1.40 | 1.50 |
|---|---|---|---|---|---|---|---|
| 方向存活 | −0.068 | −0.029 | −0.018 | **0.975** | −0.009 | −0.048 | −0.045 |

**只在評測算子恰好用的那一個倍率有值**，相鄰的 1.15 與 1.30 已經歸零。該節
第 6 點逐字寫著「若只在 s'=1.2488 有一個窄峰，先驗的辯護不成立，本候選退回
為對單一算子的 co-adapt」——而 co-adapt 正是已否決的 `針對淨化最佳化`。
**唯一可能的救法**是把單一週期換成一個帶（用峰高換峰寬），沒有量過，
該節也沒有設計這個變體。
**證據**：`log_periodic_probe`

### AdvDrop 構不到失真帶，而 `eps` 從頭到尾沒有咬到
四個 eps（60／100／150／220）給出**幾乎逐位相同**的結果：DISTS 0.0111–0.0114、
位移 0.108–0.112、擋下 **0/13**（本方法工作點是 0.1286–0.1447／0.70／11）。
根因由 `q_trajectory.csv` 逐步量出：50 步跑完，量化表由 1 走到平均 39.3，
**貼上界的比例是 0.0000**（上界 221）。梯度符號 44–48% 為負，`q` 走的是
**有微弱向上漂移的隨機遊走**，不是被梯度推著爬；真正的限制是步數 × 步長，
不是 eps。損失確實下降（0.6854 → 0.6598），最佳化沒壞。
**移植沒有問題**：`advdrop_reproduction` 用同一組設定在它自己的威脅模型上
重現到 96.0%。差別在目標——擴散編碼器對「少了一些高頻細節」比分類器耐受得多。
**後果**：`RUN_QUEUE` 第四優先問的「AdvDrop 抗不抗裁切」這一批答不了
（分母會塌陷）。要有答案得跑到 500 步（論文 §4.3 定向攻擊自己的步數），
外推 `q` 平均會到約 390，屆時 eps 才會咬到。
**證據**：`ip2p_advdrop_band`

### 乘性明暗場只比等 RMS 加性場便宜 1.33 倍，低於候選二自訂的門檻
`SURVEY_ARCHITECTURE` 候選二唯一的理論依據是「乘性比加性在同一頻帶便宜」，
該節自訂的判準是等 RMS 下 DISTS 要低 1.5 倍。實測（13 張 × 3 個場 × 4 個強度，
不跑 GPU）：工作點所在的 RMS 0.06 上是 **1.332**；且比值隨強度單調衰減
（RMS 0.01 時 2.146 → 0.06 時 1.363），逐圖 9/13 低於門檻、最小值 0.759——
**有些影像上乘性反而比加性貴**。四個軸（DISTS／LPIPS／SSIM／VIFp）上乘性較便宜，
但 **L∞ 反向且差 1.64 倍**（`exp` 在亮部放大變化量）。
等 RMS 的守門有過：兩邊 RMS 都是 0.060000、PSNR 都是 24.437。
**判準是軟門檻（原文寫「例如」），故裁定權在使用者**；要放行就是放行一個
「在最有利的低強度端有 1.5 倍、在工作點只有 1.33 倍、且不是每張圖都成立」的依據。
**證據**：`shading_field_cost`

### 量化交付：把輸出約束在 JPEG 的量化格點上，抗淨化全欄翻轉
`--deliver-jpeg QD` 做兩件事：最佳化前向套 `jpeg_roundtrip_ste`，**且交付的就是
壓縮圖**。前提是 JPEG 幾乎冪等（同品質重壓只改 0.02–0.17% 的係數）。
**與已否決的「針對淨化最佳化」差在交付什麼**——那三個變體把 JPEG 放進迴圈卻
交付未壓縮的圖，量化格點的性質完全沒拿到。

**生死判準通過**：自壓後擾動保留率 **0.76–0.94**，而隨機擾動只剩 **0.22**
（3.5–4.3 倍）。最佳化確實學會把擾動放在量化後還活著的位置。

**抗淨化（13 張，扣地板淨增益）**：

| 條件 | DISTS | blur | jpeg75 | jpeg30 | C&R串 | crop |
|---|---|---|---|---|---|---|
| 量化交付 QD.85 r2.0 | 0.1908 | **0.1876** | **0.4931** | **0.2375** | **0.1832** | **0.1098** |
| 現行主線 | 0.1447 | 0.1184 | 0.3618 | 0.1463 | 0.1271 | 0.0905 |
| DCT-Shield 抗 JPEG | 0.1043 | 0.0802 | 0.4324 | 0.0531 | 0.0888 | 0.0954 |

對 DCT-Shield：jpeg75 ×1.14、jpeg30 ×4.47、**blur ×2.34**、C&R ×2.06、
**crop ×1.15**——**先前輸掉的 blur 與 crop 兩格也翻過來了**，超出原本只指望
JPEG 的預期。只有 `adverse_cleaner` 略輸。

**人眼判定（13 張逐張看過，經 jpeg75 之後）**：量化交付 **5 擋下 2 邊緣**、
現行主線 **0**、DCT-Shield **1**。人眼差距比指標更大——淨增益量的是位移，
把「劣化」與「重畫」算成同一件事，人眼只認後者。
**反例要一起寫**：五張（#01/#04/#05/#07/#08）在三個條件下都沒擋下。

**四個保留**：(a) **這不是等失真比較**，量化交付貴 83%，`qd85_r09`
（DISTS 0.1252）的抗淨化正在跑，要等它定案；(b) **未淨化那側是純虧損**——
等失真下位移 0.70→0.556、擋下 11→3，這是取捨不是免費改良；(c) `gridpure`
那一欄沒跑；(d) QD 是防禦方自選的，攻擊方壓到低於 QD 時格點一樣失效。
**證據**：`ip2p_deliver_jpeg`（含 README 與 `sheets/`）

### 十張主線頭對頭：量化交付在低品質壓縮上勝出，但**只有 jpeg30 是預算無關的**

十張（`ip2p_fair_comparison/images10.txt`）、九個算子、三種子，淨增益一律扣
空白地板。完整記錄與全部數字在 `ip2p_mainline/README.md`，這裡只寫要裁定的。

**可以主張的**：`ours_ph_q`（純相位＋量化 r0.9）在**七個失真指標上全部低於**
`dct_aj85`（DCT-Shield §6.3 論文設定）——DISTS 0.0928 對 0.1118、RMS 0.0430 對
0.0609、L∞ 0.3608 對 0.4102、PSNR 27.40 對 24.32，SSIM／VIFp／LPIPS 同向——
而 jpeg50 是 1.94 倍（0.2323 對 0.1197）、jpeg30 是 2.39 倍（0.1072 對 0.0449）。
在對手的設計點 q ≥ 85 則相反（0.82／0.84 倍）。這一組不受「你贏是因為失真高」
的質疑，因為沒有任何一個軸支持那個說法。

**不可以主張的，以及一項必須撤回的推論**：`dct_aj85_eps2.2`（預算匹配，
DISTS 0.1962 對本方法最強點 0.1947，差 0.8%）跑到 6/10 影像時被撤回。就那
六張重算，**jpeg50 那一格反轉**——eps2.2 拿 0.4835、本方法 0.3259，而且是
**6/6 張全贏**（0.4154–0.5611 對 0.2580–0.4326），不是離群值。jpeg30 仍是
本方法勝（0.2413 對 0.1818，1.33 倍），jpeg75 接近打平（0.5051 對 0.4879）。

由此**必須撤回一項先前的推論**：曾由「未淨化位移隨 eps 幾乎持平
（eps 1.0→4.5 是 0.7067→0.7166，逐張配對 +0.0055 ± 0.0156，10 張裡 6 張為正）」
推論「DCT-Shield 加預算買不到東西」。**那句話只對未淨化那一側成立。** 在
jpeg50 上它對 eps 是單調的：0.1152（eps 1.0）→ 0.1983（eps 1.5）→ 0.4835
（eps 2.2）。加預算確實買得到中品質存活。

**要裁定的**：等失真下的主張範圍。目前資料支持的最強說法是「jpeg30 上的優勢
與預算無關」，jpeg50 需要補完 4 張才能定案（約 40 分鐘，兩個 process）。

**一個必須配的對照還沒跑**：上面比的都是 `Q_alg = 0.85`。DCT-Shield 的保證是
單向的（補充材料 D.4），預期被壓到 30 的防禦者照論文用法應設 `Q_alg = 0.30`。
現有最低的是 `dct_aj75`，它在 jpeg30 只有 0.0524（相對 aj85 的 0.0449 幾乎沒
進步），是好跡象但不足以下結論。對稱地，本方法也有同一個旋鈕（`--deliver-jpeg`
掃 0.5／0.35），`ip2p_deliver_jpeg` 的 `qd75` 族只跑過防禦、從未跑過抗淨化。
**正確的圖是兩邊各取旗鈕的最佳包絡逐一攻擊品質比較。**
**證據**：`ip2p_mainline/README.md` 第四、五節

### JPEG 以外三個方法都沒有防禦，這一點對三方都成立

blur σ1／σ2 與 crop 10%／15% 的空白地板是 0.1777／0.3098／0.5051／0.5812，而
全體十四個條件的淨增益上限只到 0.19／0.074／0.123／0.073。**算子自己造成的
位移是任何防禦淨增益的 1.5–8 倍。** 那幾欄分得出條件高下（最高的是本方法的
r2.0 量化點），但**都不足以擋下**，論文裡只能誠實列為失敗欄。
CVIU 2025 那篇專為抗 JPEG 設計的方法也完全沒有 blur／crop 的數字
（`reference/SURVEY_FREQUENCY.md` §1.13），這不是本專案獨有的失敗。
**證據**：`ip2p_mainline/README.md` 第六節、`frequency_retention`

### `--coarsen`（θ 空間平滑）已實作，但上機前的 CPU 檢查否證了它的機制

動機取自 IAM（arXiv:2402.16586）的內插平滑：把 θ／gain／floor 三個空間場存在
粗視窗網格上、前向雙線性升取樣，假說是相鄰視窗獨立旋轉會攤出寬頻能量而
JPEG 先丟掉那一段。`scripts/coarsen_probe.py` 固定隨機 θ、殘差歸一化到同一個
RMS，十張量下來：

| 設定 | coarsen | 高頻比例 | keep q75 | keep q50 | keep q30 |
|---|---|---|---|---|---|
| 相位＋加性下限 | 1 | 0.5227 | **0.5851** | **0.5221** | **0.4743** |
| 相位＋加性下限 | 6 | 0.5759 | 0.5234 | 0.4372 | 0.3900 |
| 純相位 | 1 | 0.1949 | **0.9506** | **0.8996** | **0.8371** |
| 純相位 | 6 | **0.1649** | 0.9287 | 0.8693 | 0.8132 |

**頻譜上的機制只有純相位成立**（高頻比例降 15%），主線設定（有加性下限）方向
相反；而**即使機制成立，JPEG 保留率在三種設定下全部微降**。量化交付版
（QD 0.85）同樣：q50 由 0.5924 掉到 0.5054、q30 由 0.4868 掉到 0.4020。

保留：探針用的是隨機 θ 不是最佳化過的 θ，而保留率不是目標函數。粗網格仍可能
作為正則化在同保留率下泛化更好。但方向在 5 個粗細 × 3 種設定 × 10 張上一致。
**建議：不要為它花 GPU 時間，除非有新的理由。** 程式與 22 項測試留著
（`--coarsen 1` 逐位元等於加它之前，由「關閉時不呼叫 `F.interpolate`」的樁釘住）。
**證據**：`ip2p_coarsen/probe.csv`、`probe_nofloor.csv`、`probe_qd85.csv`

### 文獻：三篇抗 JPEG 的擾動最佳化技巧，兩個可移植的旋鈕尚未試

`reference/SURVEY_FREQUENCY.md` §1.13–1.15 新增三篇逐條查證。兩個具體可移植項：

1. **多品質集成損失**（Shin & Song 2017）。可微分 JPEG 我們已經在用
   （`src/baselines/jpeg_codec.py` 的直通估計），但**單一品質會過度特化**是
   該篇的核心結論，而我們的 `--deliver-jpeg 0.85` 正是單一品質；症狀在資料上
   看得到（jpeg75 的 0.4370 掉到 jpeg30 的 0.2087）。集成只能放在**損失**上，
   交付仍是單一格點——把集成放到交付上就是已否決的 `--purify-aware`。
2. **訓練壓縮品質低於攻擊品質**（Lin et al., CVIU 2025）：它訓練 q = 35、
   評測 Q = 75。我方方向相反。`ip2p_deliver_jpeg` 的 `qd75` 族只有防禦讀數。

該篇的損失 `L_MSE + λ/PSNR` **符號有誤**（最大化 1/PSNR 等於把浮水印推得更
明顯），若移植須逐行比對公開程式碼並標 `modified_from_paper`。

## 二、證據不完整，等批次

| 待裁定 | 目前指向 | 缺什麼 |
|---|---|---|
| 含加性主線要不要關掉 `--gain-ratio` | 等效果 0.65 與 0.70 兩個錨點都顯示純相位較便宜（0.0956 對 0.1051、0.1279 對 0.1435） | `ip2p_phase_floor`（把純相位＋下限用 floor 推到帶上緣） |
| 要不要採用 `--floor-gate complement` | 效果上幾乎免費（位移 −0.4%、PSNR +0.28 dB），但**讓可用預算更均勻**（0.163 → 0.078），在空間選擇性上是往 DCT-Shield 靠 | `complement_rank`（把分配推到 75/25） |
| 只有加性下限、相位與幅度都不動 | 從未跑過。程式已補（`floor_only`），先前被一道在加性下限存在**之前**寫的自由度檢查擋住 | `ip2p_floor_only` |
| `r_min` 要不要離開 0.12 | FND-042 已記「拉高一致更好」，保留 0.12 的理由（既有批次的基準）早已不成立 | `ip2p_band_lower_bound` |
| `--freq-weight-power` 0.25 → 0.35 | RESULTS 自己註明要先補等失真掃描 | `ip2p_pricing_power` |
| 要不要採用 `--theta-budget` | 新實作，出處 arXiv:2602.06577，處理 FND-038 | `ip2p_theta_budget` |
| 隨機化幾何 EOT 能不能救裁切 | band_transfer 指出這是唯一可能有效的方向 | `ip2p_eot_geometry` |
| `--quantile` 單獨的效果 | 從未量過 | `ip2p_quantile_sweep` |

## 三、必須先修正的一項——**這一格決定整體判斷**

**本方法在 JPEG 上的優勢暫時不可引用。** `ip2p_purify_headtohead` 的 Y-only
點用的是 `--q-alg 0.95`，而 DCT-Shield 的抗 JPEG 設定是 §6.3 的 Y-only ＋
`Q_alg = 0.85`；它的保證是單向的（補充材料 D.4），0.95 由構造就擋不住品質 75
的壓縮。`RESULTS.md` 早已記過 SDEdit 線上 `dct_shield_y` 在 jpeg75 拿 +0.5185
而本方法 +0.1349，並明寫「頭對頭表不可只放 base 變體」。
**要跑兩步，只跑第一步等於沒跑：**

| 步 | 腳本 | 產出 |
|---|---|---|
| 一 | `scripts/dct_antijpeg_configs.sh "<卡號>"` | `runs/ip2p_dct_antijpeg/<tag>` 的防禦圖與失真讀數 |
| 二 | `scripts/purify_antijpeg.sh "<卡號>" "<tag> ..."` | 把那些工作點當條件加進抗淨化，出淨增益 |

第一步只產生防禦圖；抗淨化是 `phase_retention.py` 讀已存的防禦圖再跑一輪，
不做第二步的話 JPEG 那一欄仍然是空的。第二步的工作點**由參數給**——頭對頭
要的是等失真上可比的點，而各點的 fDISTS 要等第一步跑完才知道。第二步還要
空白地板齊（目前 6/13，見 `runs/ip2p_purify_headtohead/README.md`）：淨增益
是逐圖相減，缺地板的格子算不出來。

**為什麼這一格比其他都重要。** 目前的處境是：未淨化那條並列主張沒站住（等效果
下多付 1.38 倍失真、共同區間的效率是 DCT-Shield 的 86–89%、人眼擋下率打平但
多付 32% 的失真）。所以整篇只剩抗淨化這條主主張，而抗淨化裡：

- **模糊那一格是結構性的輸**：半 Nyquist 以上只有 0.3% 的能量存活，而本方法
  60% 的殘差能量在那之上；要救就得把能量往低頻搬，而低頻的 DISTS 代價是高頻的
  數千倍。這是取捨不是調參。
- **裁切那一格輸**，但成因已查明（是對位不是能量），且指向一個沒試過的做法
  （隨機化幾何 EOT）。
- **JPEG 家族是唯一明確贏的軸**——而它是拿錯的對照設定量出來的。

**若重測之後 JPEG 也輸，本方法就沒有任何一個軸贏得了 DCT-Shield**，論文的
重心必須從「我們比較強」改成別的（見下）。若贏，主主張就站得住。**在跑完
之前不要用任何 JPEG 的數字做規劃。**

### 重測的結果（**13 張定案**）：jpeg75 翻轉，jpeg30 仍是我們贏
`retention_table.py`，13 張全部有地板，扣地板淨增益：

| 條件 | DISTS | jpeg75 | jpeg30 | C&R 串接 | crop | blur | adv_cleaner |
|---|---|---|---|---|---|---|---|
| `y_q85_e10`（§6.3 抗 JPEG） | **0.1043** | **0.4324** | 0.0531 | 0.0888 | 0.0954 | 0.0802 | **0.4382** |
| `ours_add` | 0.1447 | 0.3618 | **0.1463** | **0.1271** | 0.0905 | 0.1184 | 0.3809 |
| `dct_e18`（§5.4） | 0.0976 | 0.1902 | 0.0319 | 0.0534 | **0.1098** | **0.1547** | 0.3469 |
| `dct_y_e14`（舊，q0.95） | 0.0436 | 0.0809 | 0.0147 | 0.0180 | 0.0577 | 0.0250 | 0.2525 |

**jpeg75 上它贏 1.20 倍，且失真便宜 28%。** 先前「勝 2.77 倍」是對著 q0.95、
失真只有三分之一的點量的（它在這張表上只有 0.0809）。
**但 jpeg30 我們贏 2.75 倍、C&R 串接贏 1.43 倍**——與 `Q_alg` 的單向保證一致：
0.85 是為了活過品質 ≥ 85 而設，品質 30 遠在設計點外。
**「JPEG 家族是唯一明確贏的軸」要改寫成「壓得越狠我們越有優勢，壓得輕它反而贏」。**
`adverse_cleaner` 也翻轉，`crop` 與 `blur` 維持它贏。
**證據**：`ip2p_purify_headtohead/net_gain.csv` 與該目錄 README

### 幾何 EOT 做到了設計要它做的事，但帳算不平
`g_rpi` 的 `crop` 淨增益 **0.1203 是全表最高，首次超過 DCT-Shield 的 0.1098**
（比 `ours_add` 的 0.0905 高 33%）。代價是未淨化位移由 0.6710 掉到 0.5741
（−14.4%）：絕對值上失去 0.0969、換回 0.0298，**帳面差三倍多**。依 `RUN_QUEUE`
第三優先自訂的判準（淨增益要升到足以補回那 18%），**不成立**。
**證據**：`ip2p_purify_headtohead/net_gain.csv`

### H1 被否證：區塊置換之後兩個方法掉一樣多
把防禦殘差做 32×32 區塊隨機置換再加回原圖（`SURVEY_ARCHITECTURE` 第三節指定
的判定實驗，預期「DCT-Shield 幾乎不掉、本方法掉光」）：

| 條件 | n | 原效果 | 置換後 | 保留率 |
|---|---|---|---|---|
| `ours_add` | 5 | 0.6443 | 0.4030 | **0.6348** |
| `dct_e18` | 5 | 0.6597 | 0.4124 | **0.6359** |

**兩者相差 0.0011**，逐圖也貼著走。效果既不是純統計驅動（它掉了三分之一），
也不是純實現驅動（我們留了三分之二）。
**後果**：**候選一的前提垮了**；而「98.2% 對 13%」本輪已排除三個解釋
（對準內容、裁切吃掉的能量、實現特定），剩下 H2（分母）與 **H3（放大把它的
近 Nyquist 能量搬回編碼器敏感帶）**，H3 是最值得追的一個。
**保留**：n = 5，只有 color 分片；置換後 DISTS 多數下降，不是等失真比較。
**證據**：`residual_permute`

### 純加性下限：等失真下位移一樣，PSNR 差 2.5–2.9 dB
`floor_only` 六點掃完（相位與幅度都不動）。內插到等 DISTS：

| 錨點 DISTS | 純加性 位移／PSNR | 對照 位移／PSNR | PSNR 差 |
|---|---|---|---|
| 0.1286 | 0.6968 / 22.86 | 純相位＋下限 0.7011 / 25.36 | **−2.50** |
| 0.1447 | 0.7013 / 21.85 | 含加性主線 0.7012 / 24.74 | **−2.89** |

**`DECISIONS.md` 撤回加性裁決時所依據的那句話（「PSNR 差 2.4–3.2 dB」）在現行
程式上成立**——先前的證據是一次性探針的近似，程式已刪除。位移在 0.70 飽和區
不可解讀，擋下數差異小於 ±2/13 雜訊，**唯一有鑑別力的軸是 PSNR，而它一致指向
乘法那一半**。反過來也成立：**純加性下限單獨就到得了失真帶並拿 12/13**，
所以「非加性」在防禦強度上不是必要的。
**證據**：`ip2p_floor_only`

### 建議補一個工作點：`q_alg 0.75` 且落在失真帶內
上一則的 jpeg30 優勢有一個公平性問題要先擋掉：**`Q_alg` 是防禦方自己選的**，
對手若知道攻擊方會壓到品質 30，大可把 `Q_alg` 一起調低。現有的 q0.75 兩點
（`y_q75_e10` DISTS 0.1622／PSNR 20.18、`y_q75_e14` 0.2029／17.73）**都在失真帶
之外**，不能拿來比。要嘛補一個 `eps ≈ 0.6` 的 q0.75 點把它拉進帶內再比，
要嘛就在論文裡明寫「它要覆蓋 jpeg30 得多付 0.058 DISTS／4.6 dB PSNR」。
**前者要一次防禦（約 2.1 小時）＋一次抗淨化（約 1.5 小時）。**

### 若 JPEG 重測輸了，還剩什麼
不是沒有東西。這個 session 的兩項診斷是可重用的工具、不是本方法專屬：

- `runs/ip2p_residual_signature/band_transfer.csv` 把淨化對擾動的破壞拆成三
  種——能量沒了（模糊）、方向被打散（JPEG）、只是被搬走（裁切）——並用「算子
  自己搬過的同一擾動」當控制組把第三種釘死。
- `allowed_budget_gini.csv` 區分**可用預算**與**實際花用**的空間集中度，說明
  加性下限如何吃掉方法的獨特性。

那是比較小但誠實的貢獻，現有材料撐得住。

## 四、工程

### CSV／JSON／MD 已取回，log 還沒
遠端有數個 `RESULTS.md` 引用為證據、卻從未入版控的 `runs/` 目錄。逐檔比對後
把**遠端有、本機沒有**的 219 個 `csv`／`json`／`md`／`txt` 取了回來（129 KB），
涵蓋 `ip2p_overlap_sweep`、`ip2p_band_calibration`、`ip2p_pricing_strength`、
`ip2p_untested_knobs`、`ip2p_luma_only`、`ip2p_perimage_budget`、
`ip2p_chain_headtohead`（原名 `ip2p_mainline_partial`，與本輪主線無關，已改名）等 30 個目錄。取回前逐檔確認過**沒有任何一個會覆蓋
既有檔**，取回後 `git status --porcelain --ignored` 確認沒有結果檔被 `.gitignore`
排除。尚未 commit。

`.log` 也取了 **392 個**（143 KB），同樣逐檔確認不覆蓋。**還沒取的**是在飛
批次的 36 個（`ip2p_floor_only`、`ip2p_purify_headtohead`），留到跑完再取，
免得抓到寫到一半的日誌。

### 一個 `.gitignore` 擋掉的量測檔
`runs/spectral_decomposition/radial.npz` 被排除——`runs/` 區塊的白名單只列
`csv`／`json`／`md`／`log`／`txt`，沒有 `npz`。它是一個不可重現的量測結果
（徑向頻譜），依 `DECISIONS.md` 應該保留。**要不要放行 `npz` 是裁定事項**：
放行會讓將來的二進位中間產物也一起進來，不放行就要把它轉成 CSV。
